/**
 * Zod-valid session fixtures for E2E (e.g. seeding IndexedDB via `page.evaluate`).
 * Matches `@riddle-rush/types` schemas used by `useIndexedDB` on read.
 */
export { gameSessionTestFixture } from '@riddle-rush/types/schemas';
