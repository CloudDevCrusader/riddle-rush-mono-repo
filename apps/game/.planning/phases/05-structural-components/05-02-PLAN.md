# Plan 05-02: GameModal Component with Focus Trap

## Metadata

```yaml
phase: 05
plan: 02
title: Create GameModal component with focus trap
wave: 1
depends_on: []
files_modified:
  - package.json
  - components/game/GameModal.vue
autonomous: true
estimated_time: 15min
```

## Objective

Create the GameModal component with Vue 3 Teleport, focus-trap for accessibility, and Vue Transition for animations. Supports two variants (default blue, danger red) and optional header bar.

## Context

From 05-CONTEXT.md:

- Two variants: danger (red header) and default (blue header)
- Header bar is optional — support headerless modals too
- v-model controlled visibility
- Backdrop click closes modal
- Medium backdrop opacity (50-60%)
- Animation: fade + scale, 250ms timing
- Full accessibility: focus trap + Escape key to close

From 05-RESEARCH.md:

- Use createFocusTrap from focus-trap library
- Vue 3 Teleport to body for DOM positioning
- Vue Transition component for animations
- ARIA: role="dialog", aria-modal="true", aria-labelledby

## Tasks

<task id="1" type="implement">
<title>Install focus-trap library</title>
<description>
Add focus-trap as a dependency:

```bash
pnpm add focus-trap
```

Verify installation in package.json.
</description>
<files>

- package.json (update)
  </files>
  </task>

<task id="2" type="implement">
<title>Create GameModal component</title>
<description>
Create components/game/GameModal.vue with:

**Props:**

- `modelValue`: boolean (v-model for visibility)
- `variant`: 'default' | 'danger' (default: 'default')
- `title`: string | undefined (optional header title)
- `showHeader`: boolean (default: true when title provided)

**Emits:**

- `update:modelValue`: [value: boolean]

**Structure:**

```vue
<Teleport to="body">
  <Transition name="modal">
    <div v-if="modelValue" class="modal-overlay" @click.self="close">
      <div ref="modalRef" class="modal-container" :class="containerClasses"
           role="dialog" aria-modal="true" :aria-labelledby="title ? 'modal-title' : undefined">
        <!-- Optional Header -->
        <div v-if="title" class="modal-header" :class="headerClass">
          <h2 id="modal-title" class="modal-header__title">{{ title }}</h2>
        </div>
        <!-- Content Slot -->
        <div class="modal-body">
          <slot />
        </div>
      </div>
    </div>
  </Transition>
</Teleport>
```

**Focus Trap Setup:**

```typescript
import { createFocusTrap } from 'focus-trap';

const modalRef = ref<HTMLElement>();
let focusTrap: ReturnType<typeof createFocusTrap> | null = null;

watch(
  () => props.modelValue,
  (isOpen) => {
    if (isOpen && modalRef.value) {
      focusTrap = createFocusTrap(modalRef.value, {
        initialFocus: false,
        escapeDeactivates: true,
        returnFocusOnDeactivate: true,
        onDeactivate: close,
      });
      focusTrap.activate();
    } else if (focusTrap) {
      focusTrap.deactivate();
      focusTrap = null;
    }
  }
);

onUnmounted(() => {
  if (focusTrap) focusTrap.deactivate();
});
```

**Styling:**

Overlay:

- position: fixed, inset: 0
- background-color: rgba(0, 0, 0, 0.55)
- z-index: 1000 (add --z-modal to design system if needed)
- display: flex, align-items: center, justify-content: center

Container:

- Use GamePanel styling as base (orange/gold border, rounded corners)
- max-width: 90%, max-height: 85vh
- overflow-y: auto

Header variants:

- default: Blue gradient (var(--gradient-btn-blue))
- danger: Red/orange gradient (var(--gradient-btn-red) or btn-orange)

**Transitions:**

```scss
.modal-enter-active,
.modal-leave-active {
  transition: opacity 250ms ease;
}
.modal-enter-from,
.modal-leave-to {
  opacity: 0;
}
.modal-enter-active .modal-container {
  transition: transform 250ms cubic-bezier(0.34, 1.56, 0.64, 1);
}
.modal-enter-from .modal-container {
  transform: scale(0.9);
}
.modal-leave-active .modal-container {
  transition: transform 200ms ease-in;
}
.modal-leave-to .modal-container {
  transform: scale(0.95);
}
```

</description>
<files>
- components/game/GameModal.vue (create)
</files>
</task>

## Verification

```yaml
type: automatic
criteria:
  - focus-trap is installed in package.json
  - GameModal renders centered on screen when modelValue=true
  - Modal backdrop has ~55% opacity
  - Clicking backdrop closes modal (emits update:modelValue false)
  - Escape key closes modal
  - Focus is trapped within modal when open
  - Modal animates on open/close (scale + fade)
  - Header shows blue gradient for default variant
  - Header shows red/orange gradient for danger variant
  - TypeScript passes (pnpm run typecheck)
commands:
  - pnpm run typecheck
```

## Must-Haves

From phase success criteria:

- [ ] GameModal renders with colored header bar, backdrop, and centered positioning
- [ ] Modal backdrop prevents interaction with underlying content
