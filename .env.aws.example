# AWS Deployment Configuration
# Copy this file to .env.aws and fill in your values
# Usage: source .env.aws && ./aws-deploy.sh production

# Required: Your S3 bucket name (must be globally unique)
# Examples: riddle-rush-prod-2024, my-company-riddle-rush
export AWS_S3_BUCKET=your-s3-bucket-name

# Optional: AWS region (default: us-east-1)
# Common regions: us-east-1, us-west-2, eu-west-1, ap-southeast-1
export AWS_REGION=eu-central-1

# Optional: CloudFront Distribution ID (get from CloudFront console)
# Format: E1234567890ABC
# Only needed if you're using CloudFront CDN
export AWS_CLOUDFRONT_ID=YOUR_CLOUDFRONT_ID

# Optional: Google Analytics ID
# Format: G-XXXXXXXXXX
export GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX

# Optional: App version for deployment metadata
# Format: semantic versioning (1.0.0, 1.2.3, etc.)
export APP_VERSION=1.0.1

# Optional: Environment name (production, staging, development)
# export ENVIRONMENT=production
