---
phase: 11.1
plan: 01
subsystem: game-config
tags: [scoring, player-limits, runtime-config, constants, e2e-tests]
requires: []
provides:
  - SCORE_INCREMENT=1 constant
  - MIN_PLAYERS/MAX_PLAYERS/DEFAULT_PLAYERS constants
  - Runtime config for player limits with env var overrides
affects:
  - Game scoring behavior (1 point per increment instead of 10)
  - Player setup page defaults (2 players, range 2-10)
  - E2E test expectations for scores and player counts
tech-stack:
  added: []
  patterns:
    - Runtime config for game parameters with env var overrides
key-files:
  created: []
  modified:
    - packages/shared/src/constants.ts
    - apps/game/nuxt.config.ts
    - apps/game/.env.example
    - apps/game/pages/players.vue
    - apps/game/tests/e2e/results.spec.ts
    - apps/game/tests/e2e/scoring-workflow.spec.ts
    - apps/game/tests/e2e/players.spec.ts
key-decisions:
  - SCORE_INCREMENT reduced from 10 to 1 for finer-grained scoring
  - Player range expanded to 2-10 (from 1-6) with default of 2
  - Player limits sourced from Nuxt runtimeConfig.public, overridable via NUXT_PUBLIC_* env vars
  - maxPlayers implemented as computed ref for reactivity
patterns-established:
  - Game configuration values flow from shared constants → Nuxt runtimeConfig → component usage
duration: 8min
completed: 2026-02-16
---

# Phase 11.1 Plan 01: Scoring Increment and Player Config Summary

**One-liner:** SCORE_INCREMENT reduced to 1, player limits (2-10) configurable via Nuxt runtime config with env var overrides

## What Was Done

### Task 1: Update shared constants and add runtime config entries

- Changed `SCORE_INCREMENT` from 10 to 1
- Changed `MAX_PLAYERS` from 6 to 10
- Added `MIN_PLAYERS = 2` and `DEFAULT_PLAYERS = 2` constants
- Added `minPlayers`, `maxPlayers`, `defaultPlayers` to Nuxt `runtimeConfig.public`
- Added `NUXT_PUBLIC_MIN/MAX/DEFAULT_PLAYERS` env vars to `.env.example`
- **Commit:** `0ce6f98f7`

### Task 2: Wire players page to runtime config

- Removed `MAX_PLAYERS` import from `@riddle-rush/shared/constants`
- Replaced hardcoded `minPlayers = 1` with `runtimeConfig.public.minPlayers`
- Replaced hardcoded `playerCount = ref(4)` with `ref(runtimeConfig.public.defaultPlayers)`
- Added `maxPlayers` as a computed ref from runtime config
- Updated template bindings and clamp/toast logic
- **Commit:** `edc2e8ffc`

### Task 3: Update E2E tests for SCORE_INCREMENT=1

- Updated `results.spec.ts`: score assertions from 10/20/30 to 1/2/3
- Updated `scoring-workflow.spec.ts`: score assertions from 30/10/40 to 3/1/4
- Updated comments to reflect new scoring math
- **Commit:** `7325cd407`

### Task 4: Update E2E tests for player count changes

- Updated `players.spec.ts`: total slots assertion from 6 to 10
- **Commit:** `404b69286`

## Task Commits

| Task | Name                                                   | Commit      | Type |
| ---- | ------------------------------------------------------ | ----------- | ---- |
| 1    | Update shared constants and add runtime config entries | `0ce6f98f7` | feat |
| 2    | Wire players page to runtime config for player limits  | `edc2e8ffc` | feat |
| 3    | Update E2E tests for SCORE_INCREMENT=1                 | `7325cd407` | test |
| 4    | Update E2E tests for player count 2-10 range           | `404b69286` | test |

## Decisions Made

| Decision                           | Rationale                                       |
| ---------------------------------- | ----------------------------------------------- |
| SCORE_INCREMENT = 1                | Finer-grained scoring per button press          |
| Player range 2-10, default 2       | More flexible player setup, reasonable defaults |
| Runtime config over direct imports | Enables env var overrides without code changes  |
| maxPlayers as computed ref         | Ensures template reactivity if config changes   |

## Deviations from Plan

None - plan executed exactly as written.

## Verification Results

- `pnpm run typecheck` - PASS (all 4 packages)
- `pnpm run lint` - PASS (via pre-commit hooks on each commit)
- Shared constants verified: SCORE_INCREMENT=1, MIN_PLAYERS=2, MAX_PLAYERS=10, DEFAULT_PLAYERS=2
- Runtime config verified: minPlayers, maxPlayers, defaultPlayers entries present
- Players page verified: No remaining MAX_PLAYERS import
- E2E test assertions updated to match new values

## Next Phase Readiness

No blockers. Plan 11.1-02 (GameScrollList refactoring) is being executed in parallel by another executor.

## Self-Check: PASSED
