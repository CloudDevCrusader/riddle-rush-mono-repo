---
phase: 11.1-scoring-and-player-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/constants.ts
  - apps/game/nuxt.config.ts
  - apps/game/.env.example
  - apps/game/pages/players.vue
autonomous: true

must_haves:
  truths:
    - 'Score +/- buttons on results page change score by 1 per press (not 10)'
    - 'Player count range is 2-10 with default 2 on fresh load'
    - 'Player limits are overridable via NUXT_PUBLIC_MIN_PLAYERS, NUXT_PUBLIC_MAX_PLAYERS, NUXT_PUBLIC_DEFAULT_PLAYERS env vars'
    - 'Toast message shows correct max player count from runtime config'
  artifacts:
    - path: 'packages/shared/src/constants.ts'
      provides: 'Updated SCORE_INCREMENT=1, MIN_PLAYERS=2, DEFAULT_PLAYERS=2, MAX_PLAYERS=10'
      contains: 'SCORE_INCREMENT = 1'
    - path: 'apps/game/nuxt.config.ts'
      provides: 'Runtime config entries for minPlayers, maxPlayers, defaultPlayers'
      contains: 'minPlayers'
    - path: 'apps/game/.env.example'
      provides: 'Documentation of new env vars'
      contains: 'NUXT_PUBLIC_MIN_PLAYERS'
    - path: 'apps/game/pages/players.vue'
      provides: 'Player page using runtime config instead of hardcoded values'
      contains: 'useRuntimeConfig'
  key_links:
    - from: 'apps/game/pages/players.vue'
      to: 'apps/game/nuxt.config.ts'
      via: 'useRuntimeConfig().public.minPlayers/maxPlayers/defaultPlayers'
      pattern: 'runtimeConfig\.public\.(min|max|default)Players'
    - from: 'apps/game/pages/results/[[gameId]].vue'
      to: 'packages/shared/src/constants.ts'
      via: 'SCORE_INCREMENT import'
      pattern: 'SCORE_INCREMENT'
    - from: 'apps/game/nuxt.config.ts'
      to: 'packages/shared/src/constants.ts'
      via: 'import for fallback defaults'
      pattern: 'MIN_PLAYERS|MAX_PLAYERS|DEFAULT_PLAYERS'
---

<objective>
Update scoring increment from 10 to 1, add configurable player count limits via Nuxt runtime config with env var overrides, and update the players page to use runtime config values instead of hardcoded constants.

Purpose: Score buttons should adjust by ±1 (not ±10) for fine-grained manual scoring. Player count limits (min/max/default) must be deployment-configurable via environment variables, with sensible defaults of 2-10 players and default 2.
Output: Updated shared constants, runtime config entries, env var documentation, and players page wired to runtime config.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@packages/shared/src/constants.ts
@apps/game/nuxt.config.ts
@apps/game/.env.example
@apps/game/pages/players.vue
@apps/game/pages/results/[[gameId]].vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update shared constants and add runtime config entries</name>
  <files>
    packages/shared/src/constants.ts
    apps/game/nuxt.config.ts
    apps/game/.env.example
  </files>
  <action>
    **In `packages/shared/src/constants.ts`:**
    - Change `SCORE_INCREMENT = 10` to `SCORE_INCREMENT = 1`
    - Change `MAX_PLAYERS = 6` to `MAX_PLAYERS = 10`
    - Add `MIN_PLAYERS = 2` (new export, place near MAX_PLAYERS)
    - Add `DEFAULT_PLAYERS = 2` (new export, place near MAX_PLAYERS)
    - Keep `SCORE_PER_CORRECT_ANSWER = 10` unchanged — this is the auto-scoring value, not the manual increment

    **In `apps/game/nuxt.config.ts`:**
    - Add import: `import { MIN_PLAYERS, MAX_PLAYERS, DEFAULT_PLAYERS } from '@riddle-rush/shared/constants'`
    - Add three new entries to `runtimeConfig.public` (inside the static section, before the IIFE):
      ```
      minPlayers: Number(process.env.NUXT_PUBLIC_MIN_PLAYERS) || MIN_PLAYERS,
      maxPlayers: Number(process.env.NUXT_PUBLIC_MAX_PLAYERS) || MAX_PLAYERS,
      defaultPlayers: Number(process.env.NUXT_PUBLIC_DEFAULT_PLAYERS) || DEFAULT_PLAYERS,
      ```
    - Use `Number()` wrapper to ensure numeric type from env vars

    **In `apps/game/.env.example`:**
    - Add a new section after the "FEATURE FLAGS & TOGGLES" section:
      ```
      # ===========================================
      # GAME CONFIGURATION
      # ===========================================
      # Player count limits (defaults: min=2, max=10, default=2)
      NUXT_PUBLIC_MIN_PLAYERS=
      NUXT_PUBLIC_MAX_PLAYERS=
      NUXT_PUBLIC_DEFAULT_PLAYERS=
      ```

  </action>
  <verify>
    Run `pnpm run typecheck` from repo root — must pass with no errors.
    Run `grep -n 'SCORE_INCREMENT\|MIN_PLAYERS\|MAX_PLAYERS\|DEFAULT_PLAYERS' packages/shared/src/constants.ts` — confirm values 1, 2, 10, 2.
    Run `grep -n 'minPlayers\|maxPlayers\|defaultPlayers' apps/game/nuxt.config.ts` — confirm three runtime config entries exist.
  </verify>
  <done>
    SCORE_INCREMENT is 1. MIN_PLAYERS=2, MAX_PLAYERS=10, DEFAULT_PLAYERS=2 exported from shared constants. Runtime config reads NUXT_PUBLIC_MIN_PLAYERS, NUXT_PUBLIC_MAX_PLAYERS, NUXT_PUBLIC_DEFAULT_PLAYERS env vars with fallbacks to shared constants. .env.example documents the new variables.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire players page to use runtime config for player limits</name>
  <files>
    apps/game/pages/players.vue
  </files>
  <action>
    **In `apps/game/pages/players.vue`:**

    1. **Remove the MAX_PLAYERS import** — delete line `import { MAX_PLAYERS } from '@riddle-rush/shared/constants'`

    2. **Replace hardcoded values with runtime config** — the file already has `const runtimeConfig = useRuntimeConfig()` on line 84. Use it:
       - Replace `const minPlayers = 1` with `const minPlayers = runtimeConfig.public.minPlayers as number`
       - Replace `const playerCount = ref(4)` with `const playerCount = ref(runtimeConfig.public.defaultPlayers as number)`
       - Create a computed for max: `const maxPlayers = computed(() => runtimeConfig.public.maxPlayers as number)`

    3. **Update all references to `MAX_PLAYERS`** in the script section:
       - `clampPlayerCount`: change `Math.min(MAX_PLAYERS, ...)` to `Math.min(maxPlayers.value, ...)`
       - `changePlayerCount` toast: change `t('players.max_players', [MAX_PLAYERS])` to `t('players.max_players', [maxPlayers.value])`

    4. **Update template references to `MAX_PLAYERS`**:
       - Line 32: change `{{ playerCount }} / {{ MAX_PLAYERS }}` to `{{ playerCount }} / {{ maxPlayers }}`
       - Line 38: change `:disabled="playerCount >= MAX_PLAYERS"` to `:disabled="playerCount >= maxPlayers"`

    5. **Update minPlayers template reference** (line 26):
       - The `:disabled="playerCount <= minPlayers"` already works since minPlayers is still a const

    **Do NOT change** the stepper logic, syncPlayerList, startGame validation, or any styling.

  </action>
  <verify>
    Run `pnpm run typecheck` from repo root — must pass.
    Run `pnpm run lint` from repo root — must pass.
    Grep to confirm no remaining `MAX_PLAYERS` import: `grep -n 'MAX_PLAYERS' apps/game/pages/players.vue` — should return 0 results.
    Grep to confirm runtime config usage: `grep -n 'runtimeConfig.public.minPlayers\|runtimeConfig.public.maxPlayers\|runtimeConfig.public.defaultPlayers' apps/game/pages/players.vue` — should find all three.
  </verify>
  <done>
    Players page reads min/max/default player counts from Nuxt runtime config. No hardcoded player limits remain. Default player count is 2 (was 4). Max is 10 (was 6). Min is 2 (was 1). All values overridable via env vars.
  </done>
</task>

</tasks>

<verification>
1. `pnpm run typecheck` passes across entire workspace
2. `pnpm run lint` passes across entire workspace
3. Shared constants: SCORE_INCREMENT=1, MIN_PLAYERS=2, MAX_PLAYERS=10, DEFAULT_PLAYERS=2
4. Runtime config has minPlayers, maxPlayers, defaultPlayers entries
5. Players page imports from runtime config, not shared constants for player limits
6. Results page score +/- uses SCORE_INCREMENT=1 (verified via import chain — no code change needed in results page)
</verification>

<success_criteria>

- Score increment is 1 per button press (constant changed from 10 to 1)
- Player count defaults to 2-10 range with default 2
- All three player limits are configurable via NUXT*PUBLIC*\* env vars
- TypeScript and lint checks pass
  </success_criteria>

<output>
After completion, create `.planning/phases/11.1-scoring-and-player-config/11.1-01-SUMMARY.md`
</output>
