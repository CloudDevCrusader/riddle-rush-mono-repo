---
phase: 21-refactor-and-fix-e2e-and-unit-tests
plan: 09
type: execute
wave: 4
depends_on: ['01', '01A', '01B', '02', '03', '04', '05', '06', '07']
files_modified:
  - .planning/phases/21-refactor-and-fix-e2e-and-unit-tests/21-E2E-VERIFICATION.md
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
  - TEST-05
user_setup: []

must_haves:
  truths:
    - Full E2E test suite runs with 100% pass rate
    - Zero intermittent failures across all tests
    - All new tests from this phase pass
    - Test suite execution time is acceptable
  artifacts:
    - path: '.planning/phases/21-refactor-and-fix-e2e-and-unit-tests/21-E2E-VERIFICATION.md'
      provides: 'E2E test pass rate verification report'
      contains: '100% Pass Rate'
  key_links:
    - from: '21-E2E-VERIFICATION.md'
      to: 'apps/game/tests/e2e/'
      via: 'verified E2E test suite'
      pattern: 'pnpm run test:e2e'
---

<objective>
Verify 100% E2E test pass rate across the full test suite. After adding new tests and fixing existing issues, this plan runs the complete E2E test suite multiple times to confirm 100% pass rate with no intermittent failures. This addresses ROADMAP Success Criteria #3: "E2E test suite passes with 100% success rate (no intermittent failures)."

Purpose: Achieve ROADMAP Success Criteria #3: "E2E test suite passes with 100% success rate (no intermittent failures)"
Output: E2E verification report confirming 100% pass rate
</objective>

<execution_context>
@/Users/markuswagner/projects/riddle-rush-mono-repo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markuswagner/projects/riddle-rush-mono-repo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-refactor-and-fix-e2e-and-unit-tests/21-CONTEXT.md
@.planning/phases/21-refactor-and-fix-e2e-and-unit-tests/21-RESEARCH.md

# E2E test infrastructure

@apps/game/playwright.config.ts
@apps/game/tests/e2e/
</context>

<interfaces>
<!-- E2E verification expectations -->

From ROADMAP Success Criteria #3:

```typescript
// E2E test suite requirements:
interface E2ESuccessCriteria {
  passRate: 100 // Must be 100% (no failures allowed)
  intermittentFailures: 0 // Zero flaky tests
  testCount: number // Total number of tests
  executionTime: number // Acceptable execution time (< 10 minutes for full suite)
  retryOnFailure: boolean // No retries to mask failures
}
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run full E2E test suite baseline</name>
  <files>.planning/phases/21-refactor-and-fix-e2e-and-unit-tests/21-E2E-VERIFICATION.md</files>
  <read_first>
    - apps/game/playwright.config.ts
  </read_first>
  <action>
    Run full E2E test suite to establish baseline pass rate:

    1. Navigate to game app directory:
       ```bash
       cd apps/game
       ```

    2. Run full E2E test suite (single run, no retries):
       ```bash
       pnpm run test:e2e --workers=1 --retries=0
       ```

    3. Capture results:
       ```bash
       pnpm run test:e2e --workers=1 --retries=0 > /tmp/e2e-run-1.txt 2>&1
       ```

    4. Parse results:
       - Count total tests run
       - Count passed tests
       - Count failed tests
       - Count flaky tests (if Playwright detects)
       - Measure execution time
       - Identify any test-specific issues

    5. Create initial verification report section:
       ```markdown
       ## E2E Test Suite Verification - Run 1

       | Metric | Value |
       |---------|-------|
       | Total Tests | {count} |
       | Passed | {count} |
       | Failed | {count} |
       | Pass Rate | {percentage}% |
       | Flaky Tests | {count} |
       | Execution Time | {seconds}s |
       | Issues Found | {list} |

       ### Failed Tests
       {if failures, list each test with error}

       ### Recommendations
       {if failures, suggest fixes}
       ```

  </action>
  <verify>
    <automated>cd apps/game && pnpm run test:e2e --workers=1 --retries=0 2>&1 | tail -30</automated>
  </verify>
  <done>
    - Full E2E test suite executed
    - Results captured and parsed
    - Baseline report section created
  </done>
</task>

<task type="auto">
  <name>Task 2: Run E2E test suite multiple times to verify consistency</name>
  <files>.planning/phases/21-refactor-and-fix-e2e-and-unit-tests/21-E2E-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/21-refactor-and-fix-e2e-and-unit-tests/21-E2E-VERIFICATION.md
  </read_first>
  <action>
    Run E2E test suite 3 times to verify consistency and eliminate intermittent failures:

    1. Run second execution:
       ```bash
       cd apps/game
       pnpm run test:e2e --workers=1 --retries=0 > /tmp/e2e-run-2.txt 2>&1
       ```

    2. Run third execution:
       ```bash
       cd apps/game
       pnpm run test:e2e --workers=1 --retries=0 > /tmp/e2e-run-3.txt 2>&1
       ```

    3. Parse all three runs:
       - Compare pass rates across runs
       - Identify tests that fail in some runs but not others (intermittent)
       - Check for timing-related failures (timeout vs assertion)
       - Verify all new tests from this phase pass consistently

    4. Update verification report:
       ```markdown
       ## Multi-Run Consistency Check

       | Run | Total | Passed | Failed | Pass Rate | Time |
       |------|--------|--------|--------|------------|------|
       | 1 | {count} | {count} | {count} | {percentage}% | {s}s |
       | 2 | {count} | {count} | {count} | {percentage}% | {s}s |
       | 3 | {count} | {count} | {count} | {percentage}% | {s}s |

       ### Consistent Failures (failed all 3 runs)
       {list}

       ### Intermittent Failures (failed in 1-2 runs only)
       {list}

       ### New Tests Status
       - Plan 01 (Unit tests): {status}
       - Plan 01A (Unit tests): {status}
       - Plan 01B (Unit tests): {status}
       - Plan 02 (E2E refactor): {status}
       - Plan 03 (Data-testid): {status}
       - Plan 04 (Mobile E2E): {status}
       - Plan 05 (Mobile tests): {status}
       - Plan 06 (Integration): {status}
       - Plan 07 (Multi-round): {status}
       - Plan 08 (Game mode): {status}
       ```

  </action>
  <verify>
    <automated>cd apps/game && for i in 2 3; do echo "=== Run $i ===" && pnpm run test:e2e --workers=1 --retries=0 2>&1 | grep -E "passed|failed" | head -5; done</automated>
  </verify>
  <done>
    - E2E test suite run 3 times
    - Results from all runs parsed
    - Consistency analysis completed
  </done>
</task>

<task type="auto">
  <name>Task 3: Analyze and categorize any remaining failures</name>
  <files>.planning/phases/21-refactor-and-fix-e2e-and-unit-tests/21-E2E-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/21-refactor-and-fix-e2e-and-unit-tests/21-E2E-VERIFICATION.md
  </read_first>
  <action>
    Analyze any remaining failures and categorize by root cause:

    1. If failures exist, categorize by type:
       ```markdown
       ## Failure Analysis

       ### Timing Issues
       Tests that fail due to timeout or race conditions:
       - {list tests with timeout errors}

       ### Selector Issues
       Tests that fail due to element not found:
       - {list tests with selector errors}

       ### Assertion Failures
       Tests that fail due to incorrect expectations:
       - {list tests with assertion errors}

       ### Data Issues
       Tests that fail due to incorrect test data or state:
       - {list tests with data errors}

       ### Environment Issues
       Tests that fail due to environment configuration:
       - {list tests with environment errors}
       ```

    2. For each failure, determine if:
       - Can be fixed in this phase (quick fix)
       - Requires code change (bug fix)
       - Requires test update (test is wrong)
       - Is environment-specific (ignore)

    3. Create action plan for failures:
       ```markdown
       ## Failure Action Plan

       | Test | Issue | Action | Priority |
       |-------|--------|--------|----------|
       | {name} | {description} | {fix description} | {P1/P2/P3} |
       ```

    4. If all tests pass, document success:
       ```markdown
       ## Final Result: SUCCESS

       All E2E tests pass with 100% success rate.
       No intermittent failures detected across 3 runs.
       ROADMAP Success Criteria #3 achieved: "E2E test suite passes with 100% success rate (no intermittent failures)"
       ```

  </action>
  <verify>
    <automated>grep -c "100% Pass Rate\|Final Result: SUCCESS" .planning/phases/21-refactor-and-fix-e2e-and-unit-tests/21-E2E-VERIFICATION.md</automated>
  </verify>
  <done>
    - All failures categorized by root cause
    - Action plan created for remaining issues
    - Success documented if 100% pass rate achieved
  </done>
</task>

<task type="auto">
  <name>Task 4: Finalize verification report and document summary</name>
  <files>.planning/phases/21-refactor-and-fix-e2e-and-unit-tests/21-E2E-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/21-refactor-and-fix-e2e-and-unit-tests/21-E2E-VERIFICATION.md
  </read_first>
  <action>
    Finalize the E2E verification report:

    1. Create executive summary:
       ```markdown
       # E2E Test Suite Verification Report

       ## Executive Summary
       - **Phase**: 21 - Refactor and Fix E2E and Unit Tests
       - **Date**: {date}
       - **Verification Runs**: 3
       - **Status**: {PASS/FAIL/CONDITIONAL}

       ## Success Criteria Status
       | Criteria | Status | Details |
       |----------|--------|---------|
       | 100% pass rate | {✓/✗} | {details} |
       | No intermittent failures | {✓/✗} | {details} |
       | All new tests pass | {✓/✗} | {details} |
       | Acceptable execution time | {✓/✗} | {details} |
       ```

    2. Document test suite metrics:
       ```markdown
       ## Test Suite Metrics

       Total E2E Test Files: {count}
       Total Test Cases: {count}
       New Tests Added (Phase 21): {count}

       Test Categories:
       - Unit Tests (Plans 01, 01A, 01B): {count}
       - E2E Refactor (Plan 02): {count}
       - Data-testid (Plan 03): {count}
       - Mobile E2E (Plan 04): {count}
       - Mobile Tests (Plan 05): {count}
       - Integration (Plan 06): {count}
       - Multi-round (Plan 07): {count}
       - Game Mode (Plan 08): {count}
       ```

    3. Create recommendations:
       ```markdown
       ## Recommendations

       ### For CI/CD
       - Run E2E tests with `--retries=0` to catch intermittent failures
       - Consider adding retry mechanism at job level (not test level)
       - Parallelize test runs across multiple workers (after 100% pass rate achieved)

       ### For Development
       - All tests must pass before PR merge
       - Run `pnpm run test:e2e` locally before pushing

       ### For Future Phases
       - Maintain 100% pass rate as requirement for all new features
       - Add E2E tests for all new user flows
       ```

    4. Sign and date report:
       ```markdown
       ---

       **Verified By**: Claude Code (Phase 21 Planner)
       **Date**: {ISO date}
       **Review Status**: {Ready for Review / Requires Fixes}

       ROADMAP Success Criteria #3 Status:
       - [ ] "E2E test suite passes with 100% success rate (no intermittent failures)"
       - If checked, phase 21 is complete.
       ```

  </action>
  <verify>
    <automated>grep -c "Verified By\|Review Status" .planning/phases/21-refactor-and-fix-e2e-and-unit-tests/21-E2E-VERIFICATION.md</automated>
  </verify>
  <done>
    - E2E verification report completed
    - Executive summary created
    - Test suite metrics documented
    - Recommendations provided
    - Report signed and dated
  </done>
</task>

</tasks>

<verification>
1. Verify report exists: `test -f .planning/phases/21-refactor-and-fix-e2e-and-unit-tests/21-E2E-VERIFICATION.md`
2. Verify report contains 3 runs: `grep -c "Run [1-3]" .planning/phases/21-refactor-and-fix-e2e-and-unit-tests/21-E2E-VERIFICATION.md`
3. Verify final status documented: `grep "Review Status" .planning/phases/21-refactor-and-fix-e2e-and-unit-tests/21-E2E-VERIFICATION.md`
4. If all tests pass: Verify "100% Pass Rate" or "SUCCESS" in report
</verification>

<success_criteria>

- 21-E2E-VERIFICATION.md created with complete verification report
- E2E test suite run 3 times to verify consistency
- All test results captured and analyzed
- Intermittent failures identified (if any)
- Final status documented (PASS/FAIL/CONDITIONAL)
- If PASS: ROADMAP Success Criteria #3 achieved with 100% pass rate
- If FAIL: Action plan provided for remaining issues
  </success_criteria>

<output>
After completion, create `.planning/phases/21-refactor-and-fix-e2e-and-unit-tests/21-09-SUMMARY.md`
</output>
