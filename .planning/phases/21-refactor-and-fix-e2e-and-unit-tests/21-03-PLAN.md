---
phase: 21-refactor-and-fix-e2e-and-unit-tests
plan: 03
type: execute
wave: 3
depends_on: []
files_modified:
  - apps/game/pages/credits.vue
  - apps/game/pages/language.vue
  - apps/game/pages/results/[[gameId]].vue
  - apps/game/app.vue
  - apps/game/pages/index.vue
autonomous: true
requirements:
  - TEST-02
user_setup: []

must_haves:
  truths:
    - Credits page has data-testid attributes for all interactive elements
    - Language page has data-testid attributes for options and buttons
    - Results page has data-testid attributes for all player entries and controls
    - Offline indicator has data-testid attribute
    - Main menu has data-testid for navigation buttons
  artifacts:
    - path: 'apps/game/pages/credits.vue'
      contains: 'data-testid="credits-'
    - path: 'apps/game/pages/language.vue'
      contains: 'data-testid="language-'
    - path: 'apps/game/pages/results/[[gameId]].vue'
      contains: 'data-testid="results-'
    - path: 'apps/game/app.vue'
      contains: 'data-testid="offline-indicator"'
    - path: 'apps/game/pages/index.vue'
      contains: 'data-testid="main-menu-'
  key_links:
    - from: 'credits.vue'
      to: 'tests/e2e/credits.spec.ts'
      via: 'data-testid selector matching'
      pattern: 'data-testid="credits-'
    - from: 'language.vue'
      to: 'tests/e2e/language.spec.ts'
      via: 'data-testid selector matching'
      pattern: 'data-testid="language-'
    - from: 'results.vue'
      to: 'tests/e2e/results.spec.ts'
      via: 'data-testid selector matching'
      pattern: 'data-testid="results-'
---

<objective>
Add missing data-testid attributes to page components that E2E tests need. Currently, E2E tests rely on CSS selectors because some interactive elements lack data-testid attributes. This task adds the missing attributes to enable selector refactoring in plan 21-02.

Purpose: Enable E2E test refactoring by adding data-testid attributes to pages missing them (TEST-02)
Output: 5 page files with comprehensive data-testid attributes for all interactive elements
</objective>

<execution_context>
@/Users/markuswagner/projects/riddle-rush-mono-repo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markuswagner/projects/riddle-rush-mono-repo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-refactor-and-fix-e2e-and-unit-tests/21-CONTEXT.md
@.planning/phases/21-refactor-and-fix-e2e-and-unit-tests/21-RESEARCH.md

# Page component patterns

@apps/game/pages/credits.vue
@apps/game/pages/language.vue
@apps/game/pages/results/[[gameId]].vue
@apps/game/app.vue
@apps/game/pages/index.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data-testid attributes to credits.vue</name>
  <files>apps/game/pages/credits.vue</files>
  <read_first>
    - apps/game/pages/credits.vue
  </read_first>
  <action>
    Add data-testid attributes to apps/game/pages/credits.vue:

    1. Add to main container:
       ```vue
       <div class="credits-page" data-testid="credits-page">
       ```

    2. Add to title image:
       ```vue
       <img class="title-image" data-testid="credits-title-image" ... />
       ```

    3. Add to credits panel:
       ```vue
       <div class="credits-panel" data-testid="credits-panel">
       ```

    4. Add to each credit section (3 sections: Game Design, Programming, Art):
       ```vue
       <div class="credit-section" :data-testid="'credits-section-' + index">
       <div class="section-heading" :data-testid="'credits-section-heading-' + index">
       <div class="credit-name" :data-testid="'credits-name-' + nameIndex">
       ```
       - Sections: Game Design (Tobi, Sophia), Programming (Markus), Art (Sarmad Ali)
       - Use index-based data-testid: credits-section-0, credits-section-1, credits-section-2

    5. Add to back button:
       ```vue
       <button class="back-btn" data-testid="credits-back-button" ... />
       ```

    6. Add to OK button:
       ```vue
       <button class="ok-btn" data-testid="credits-ok-button" ... />
       ```

    7. Add to container:
       ```vue
       <div class="container" data-testid="credits-container">
       ```

    8. Add to background:
       ```vue
       <div class="page-bg" data-testid="page-background">
       ```

    Naming convention: {page}-{element}-{type}
    - credits-page for main container
    - credits-title-image for title
    - credits-section-{index} for credit sections
    - credits-name-{index} for credit names
    - credits-back-button for back button
    - credits-ok-button for OK button

  </action>
  <verify>
    <automated>cd apps/game && grep -c 'data-testid=' pages/credits.vue</automated>
  </verify>
  <done>
    - credits.vue has 8+ data-testid attributes
    - All interactive elements have stable selectors
    - Naming convention follows {page}-{element}-{type} pattern
  </done>
</task>

<task type="auto">
  <name>Task 2: Add data-testid attributes to language.vue</name>
  <files>apps/game/pages/language.vue</files>
  <read_first>
    - apps/game/pages/language.vue
  </read_first>
  <action>
    Add data-testid attributes to apps/game/pages/language.vue:

    1. Add to main language card:
       ```vue
       <div class="language-card" data-testid="language-card">
       ```

    2. Add to language options (English and German):
       ```vue
       <div
         class="language-option"
         :data-testid="'language-option-' + (locale === 'en' ? 'english' : 'german')"
         ...
       >
       ```

    3. Add to flag containers (2 flags):
       ```vue
       <div class="flag-container" :data-testid="'language-flag-' + locale">
       ```

    4. Add to checkmark elements:
       ```vue
       <div class="checkmark" :data-testid="'language-checkmark-' + locale">
       ```

    5. Add to OK button:
       ```vue
       <button class="ok-btn" data-testid="language-ok-button" ... />
       ```

    6. Add to background pattern (if exists):
       ```vue
       <div class="bg-pattern" data-testid="page-background-pattern">
       ```

    Naming convention: {page}-{element}-{type}
    - language-card for main card
    - language-option-{english, german} for language rows
    - language-flag-{en, de} for flag containers
    - language-checkmark-{en, de} for checkmarks
    - language-ok-button for confirm button

  </action>
  <verify>
    <automated>cd apps/game && grep -c 'data-testid=' pages/language.vue</automated>
  </verify>
  <done>
    - language.vue has 6+ data-testid attributes
    - English and German options have unique data-testid values
    - OK button has stable selector
  </done>
</task>

<task type="auto">
  <name>Task 3: Add data-testid attributes to results.vue</name>
  <files>apps/game/pages/results/[[gameId]].vue</files>
  <read_first>
    - apps/game/pages/results/[[gameId]].vue
  </read_first>
  <action>
    Add data-testid attributes to apps/game/pages/results/[[gameId]].vue:

    1. Add to background:
       ```vue
       <div class="game-background" data-testid="page-background">
       ```

    2. Add to header:
       ```vue
       <div class="game-header" data-testid="results-header">
       ```

    3. Add to scoring page list (player entries container):
       ```vue
       <div class="scoring-page__list" data-testid="results-scores-container">
       ```

    4. Add to each player entry (v-for loop):
       ```vue
       <div
         class="scoring-page__player-entry"
         :data-testid="'results-player-entry-' + index"
         ...
       >
       ```

    5. Add to score controls container:
       ```vue
       <div class="scoring-page__score-controls" :data-testid="'results-score-controls-' + index">
       ```

    6. Add to score value display:
       ```vue
       <span class="scoring-page__score-value" :data-testid="'results-score-display-' + index">
       ```

    7. Ensure existing data-testid attributes are preserved:
       - [data-testid="results-player-entry-{index}"] for player entries
       - [data-testid="score-increment"] for increment buttons
       - [data-testid="score-decrement"] for decrement buttons
       - [data-testid="confirm-scores"] for confirm button

    8. Add to player leaderboard overlay:
       ```vue
       <div class="player-leaderboard" data-testid="player-leaderboard">
       ```

    Naming convention: {page}-{element}-{type}
    - results-header for page header
    - results-scores-container for player list
    - results-player-entry-{index} for individual player cards
    - results-score-controls-{index} for score button groups
    - results-score-display-{index} for score values

  </action>
  <verify>
    <automated>cd apps/game && grep -c 'data-testid=' pages/results/[[gameId]].vue</automated>
  </verify>
  <done>
    - results.vue has 6+ data-testid attributes added
    - All player entries have index-based data-testid values
    - Score controls and displays have index-based selectors
  </done>
</task>

<task type="auto">
  <name>Task 4: Add data-testid for offline indicator in app.vue</name>
  <files>apps/game/app.vue</files>
  <read_first>
    - apps/game/app.vue
  </read_first>
  <action>
    Add data-testid attribute to offline indicator in apps/game/app.vue:

    1. Find the offline indicator/connection status component
    2. Add data-testid attribute:
       ```vue
       <ConnectionStatus data-testid="offline-indicator" ... />
       ```
       OR if inline element:
       ```vue
       <div class="offline" data-testid="offline-indicator" ... >
       ```

    3. Ensure ConnectionStatus component passes through data-testid if used
    4. If ConnectionStatus.vue is used, ensure it exposes data-testid to rendered element

    Note: This enables E2E tests to reliably find the offline indicator

  </action>
  <verify>
    <automated>cd apps/game && grep -c 'data-testid="offline-indicator"' app.vue</automated>
  </verify>
  <done>
    - app.vue has offline-indicator data-testid attribute
    - ConnectionStatus component supports data-testid prop or passes it through
  </done>
</task>

<task type="auto">
  <name>Task 5: Add data-testid attributes to main menu in index.vue</name>
  <files>apps/game/pages/index.vue</files>
  <read_first>
    - apps/game/pages/index.vue
  </read_first>
  <action>
    Add data-testid attributes to apps/game/pages/index.vue main menu:

    1. Add to options/settings button:
       ```vue
       <button class="options-btn" data-testid="main-menu-options" ... />
       ```

    2. Add to menu items (if menu component used):
       ```vue
       <div
         class="menu-item"
         :data-testid="'menu-item-' + (item.type)"
         ...
       >
       ```
       - Examples: menu-item-language, menu-item-settings, menu-item-credits

    3. Add to Play button:
       ```vue
       <button class="play-btn" data-testid="main-menu-play" ... />
       ```

    4. Add to any other main menu buttons:
       - Menu/Credits button: data-testid="main-menu-credits"
       - Quit button: data-testid="main-menu-quit"
       - Settings button: data-testid="main-menu-settings"

    5. Ensure existing data-testid attributes are preserved if any

    Naming convention: {page}-{element}-{type}
    - main-menu-* for main menu elements
    - menu-item-{type} for dropdown menu items

  </action>
  <verify>
    <automated>cd apps/game && grep -c 'data-testid=' pages/index.vue</automated>
  </verify>
  <done>
    - index.vue has 4+ data-testid attributes
    - All main menu buttons have stable selectors
    - Menu items have type-based data-testid values
  </done>
</task>

</tasks>

<verification>
1. Verify credits.vue has data-testid: `grep "data-testid=" apps/game/pages/credits.vue | wc -l >= 8`
2. Verify language.vue has data-testid: `grep "data-testid=" apps/game/pages/language.vue | wc -l >= 6`
3. Verify results.vue has data-testid: `grep "data-testid=" apps/game/pages/results/[[gameId]].vue | wc -l >= 6`
4. Verify app.vue has offline-indicator: `grep 'data-testid="offline-indicator"' apps/game/app.vue`
5. Verify index.vue has data-testid: `grep "data-testid=" apps/game/pages/index.vue | wc -l >= 4`
</verification>

<success_criteria>

- 5 page files have data-testid attributes added
- All interactive elements have stable data-testid selectors
- Naming convention {page}-{element}-{type} followed consistently
- E2E tests can reliably find all elements via data-testid
  </success_criteria>

<output>
After completion, create `.planning/phases/21-refactor-and-fix-e2e-and-unit-tests/21-03-SUMMARY.md`
</output>
