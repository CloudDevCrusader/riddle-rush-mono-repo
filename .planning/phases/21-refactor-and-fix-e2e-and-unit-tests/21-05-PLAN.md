---
phase: 21-refactor-and-fix-e2e-and-unit-tests
plan: 05
type: execute
wave: 5
depends_on: []
files_modified:
  - apps/mobile/tests/unit/use-utilities.spec.ts
  - apps/mobile/tests/integration/game-flow.spec.ts
autonomous: true
requirements:
  - TEST-05
user_setup: []

must_haves:
  truths:
    - NativeScript mobile app has unit tests for core utilities
    - NativeScript mobile app has integration tests for game flows
    - Tests cover critical mobile app components
    - All mobile tests pass with no failures
  artifacts:
    - path: 'apps/mobile/tests/unit/'
      provides: 'Unit test directory for mobile app'
      contains: 'use-utilities.spec.ts'
    - path: 'apps/mobile/tests/integration/'
      provides: 'Integration test directory for mobile app'
      contains: 'game-flow.spec.ts'
    - path: 'apps/mobile/tests/'
      contains: 'Unit and integration tests for mobile app'
  key_links:
    - from: 'use-utilities.spec.ts'
      to: 'app/utils/ or app/components/'
      via: 'test utility functions'
      pattern: 'from [''"]~/utils/[''"]|from [''"]~/components/[''"]'
    - from: 'game-flow.spec.ts'
      to: 'app/screens/'
      via: 'test screen navigation and game flow'
      pattern: 'navigateTo|game.*flow'
---

<objective>
Add unit and integration tests for the NativeScript Vue mobile app (apps/mobile/). Currently, the mobile app has zero test coverage. This plan creates test infrastructure and adds tests for core utilities and key game flows.

Purpose: Achieve TEST-05 (NativeScript mobile app has unit/integration tests)
Output: Unit and integration test files for mobile app with test infrastructure
</objective>

<execution_context>
@/Users/markuswagner/projects/riddle-rush-mono-repo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markuswagner/projects/riddle-rush-mono-repo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-refactor-and-fix-e2e-and-unit-tests/21-CONTEXT.md
@.planning/phases/21-refactor-and-fix-e2e-and-unit-tests/21-RESEARCH.md

# NativeScript app structure

@apps/mobile/app/
@apps/mobile/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test directory structure for mobile app</name>
  <files>apps/mobile/tests/unit/, apps/mobile/tests/integration/</files>
  <read_first>
    - apps/mobile/package.json
  </read_first>
  <action>
    Create test directory structure for NativeScript mobile app:

    1. Create directories:
       ```bash
       mkdir -p apps/mobile/tests/unit
       mkdir -p apps/mobile/tests/integration
       mkdir -p apps/mobile/tests/fixtures
       ```

    2. Create test config file (apps/mobile/vitest.config.ts or jest.config.js):
       - Research NativeScript test framework options:
         a) @nativescript/unit-testing-framework (official)
         b) jest with @nativescript/unit-test-runner preset
       - Create minimal config based on chosen framework
       - Ensure TypeScript compilation for test files
       - Set up test file patterns: *.spec.ts, *.test.ts

    3. Create test setup file (apps/mobile/tests/setup.ts):
       - Import NativeScript globals if needed
       - Set up test fixtures
       - Configure test environment

    4. Update package.json test scripts:
       ```json
       {
         "scripts": {
           "test:unit": "vitest run tests/unit",
           "test:integration": "vitest run tests/integration",
           "test": "npm run test:unit && npm run test:integration",
           "test:watch": "vitest watch"
         }
       }
       ```
       - Or: `test:unit: "jest tests/unit"` if using Jest

    5. Add test dependencies to package.json if not present:
       - For Vitest: @vitest/ui, @vitest/coverage-v8
       - For Jest: @nativescript/unit-test-runner, @types/jest
       - Or: @nativescript/unit-testing-framework (if using official framework)

  </action>
  <verify>
    <automated>test -d apps/mobile/tests/unit && test -d apps/mobile/tests/integration</automated>
  </verify>
  <done>
    - apps/mobile/tests/unit/ directory created
    - apps/mobile/tests/integration/ directory created
    - Test config file created (vitest.config.ts or jest.config.js)
    - package.json has test scripts added
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create unit tests for mobile app utilities</name>
  <files>apps/mobile/tests/unit/use-utilities.spec.ts</files>
  <behavior>
    - Test 1: UUID generation creates unique identifiers
    - Test 2: UUID format is valid (36 chars, hyphens at positions)
    - Test 3: Format utility handles number/string conversion
    - Test 4: Storage wrapper handles get/set operations
    - Test 5: Storage wrapper handles remove operations
  </behavior>
  <read_first>
    - apps/mobile/app/utils/ (if exists)
    - apps/mobile/app/components/ (for utility functions)
  </read_first>
  <action>
    Create apps/mobile/tests/unit/use-utilities.spec.ts for mobile app utilities:

    1. Import test framework:
       ```typescript
       import { describe, it, expect, beforeEach, afterEach } from 'vitest'
       // OR for Jest:
       // import { describe, it, expect, beforeEach, afterEach } from '@jest/globals'
       ```

    2. Test any utility functions found in app/utils/:
       a) If UUID utility exists:
          - Test generateUUID returns 36-character string
          - Test generateUUID includes hyphens at correct positions (8-4-4-4-12)
          - Test generateUUID produces unique values

       b) If storage/utility functions exist:
          - Test setItem stores value correctly
          - Test getItem retrieves stored value
          - Test removeItem deletes stored value
          - Test clear removes all values

       c) If format functions exist:
          - Test formatDate returns correct string format
          - Test formatNumber returns formatted string
          - Test capitalize returns capitalized string

       d) If validation functions exist:
          - Test validateEmail returns true for valid emails
          - Test validateEmail returns false for invalid emails
          - Test validateName returns true for valid names

    3. Handle NativeScript-specific APIs:
       - Mock NativeScript application context if needed
       - Use @nativescript/core modules appropriately in tests
       - Test NativeScript platform-specific code

    Note: If app/utils/ directory is empty or doesn't exist, create placeholder tests for expected utilities

  </action>
  <verify>
    <automated>cd apps/mobile && npm run test:unit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - use-utilities.spec.ts created with 5+ test cases
    - Mobile app utility functions tested
    - Tests cover common utility patterns (UUID, storage, format, validation)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create integration tests for mobile game flow</name>
  <files>apps/mobile/tests/integration/game-flow.spec.ts</files>
  <behavior>
    - Test 1: Main menu navigation works
    - Test 2: Players page allows setting player names
    - Test 3: Starting game navigates to game screen
    - Test 4: Answer submission updates UI
    - Test 5: Results display scores correctly
    - Test 6: Leaderboard shows rankings
    - Test 7: Back navigation returns to previous screen
  </behavior>
  <read_first>
    - apps/mobile/app/screens/ (for screen structure)
    - apps/mobile/app/App.vue or app.component.ts (for navigation)
  </read_first>
  <action>
    Create apps/mobile/tests/integration/game-flow.spec.ts for mobile app game flow:

    1. Import test framework and NativeScript components:
       ```typescript
       import { describe, it, expect, beforeEach, afterEach } from 'vitest'
       import { mount } from '@nativescript/unit-test-runner'
       import HomeScreen from '@/screens/HomeScreen.vue'
       import PlayersScreen from '@/screens/PlayersScreen.vue'
       import GameScreen from '@/screens/GameScreen.vue'
       import ResultsScreen from '@/screens/ResultsScreen.vue'
       ```

    2. Test main screen (home) behavior:
       ```typescript
       describe('Home Screen', () => {
         it('renders title and play button', () => {
           const wrapper = mount(HomeScreen)
           expect(wrapper.text()).toContain('Riddle Rush')
           expect(wrapper.findComponent('Button')).toBeTruthy()
         })

         it('navigates to players when play button tapped', async () => {
           const wrapper = mount(HomeScreen)
           const button = wrapper.findComponent('Button')
           await button.trigger('tap')
           // Verify navigation occurred (mock router)
         })
       })
       ```

    3. Test players screen behavior:
       ```typescript
       describe('Players Screen', () => {
         it('renders player inputs', () => {
           const wrapper = mount(PlayersScreen)
           expect(wrapper.findComponent('TextField')).toBeTruthy()
         })

         it('allows setting player names', async () => {
           const wrapper = mount(PlayersScreen)
           const input = wrapper.findComponent('TextField')
           await input.setValue('Player 1')
           expect(input.value).toBe('Player 1')
         })

         it('navigates to game when start button tapped', async () => {
           const wrapper = mount(PlayersScreen)
           const button = wrapper.findComponent('Button')
           await button.trigger('tap')
           // Verify navigation occurred
         })
       })
       ```

    4. Test game screen behavior:
       ```typescript
       describe('Game Screen', () => {
         it('displays category and letter', () => {
           const wrapper = mount(GameScreen)
           // Verify category and letter are displayed
         })

         it('shows answer input', () => {
           const wrapper = mount(GameScreen)
           expect(wrapper.findComponent('TextField')).toBeTruthy()
         })

         it('allows submitting answer', async () => {
           const wrapper = mount(GameScreen)
           const input = wrapper.findComponent('TextField')
           await input.setValue('Apple')
           const button = wrapper.findComponent('Button')
           await button.trigger('tap')
           // Verify submission occurred
         })
       })
       ```

    5. Test results/leaderboard screen behavior:
       ```typescript
       describe('Results/Leaderboard Screen', () => {
         it('displays player scores', () => {
           const wrapper = mount(ResultsScreen)
           expect(wrapper.text()).toContain('Ranking')
         })

         it('shows ranked players', () => {
           const wrapper = mount(ResultsScreen)
           // Verify players are displayed in ranked order
         })

         it('allows navigating back to main menu', async () => {
           const wrapper = mount(ResultsScreen)
           const backButton = wrapper.findComponent('Button')
           await backButton.trigger('tap')
           // Verify navigation to home
         })
       })
       ```

    6. Test navigation flow across screens:
       ```typescript
       describe('Complete Game Flow', () => {
         it('navigates from home to players to game', async () => {
           // Mount home, tap play
           const home = mount(HomeScreen)
           await home.findComponent('Button').trigger('tap')

           // Mount players, set name, tap start
           const players = mount(PlayersScreen)
           await players.findComponent('TextField').setValue('Player 1')
           await players.findComponent('Button').trigger('tap')

           // Mount game, verify loaded
           const game = mount(GameScreen)
           expect(game).toBeTruthy()
         })
       })
       ```

  </action>
  <verify>
    <automated>cd apps/mobile && npm run test:integration 2>&1 | tail -5</automated>
  </verify>
  <done>
    - game-flow.spec.ts created with 7+ test cases
    - All main screens (home, players, game, results) tested
    - Navigation flow between screens verified
    - Touch interactions (tap) tested
  </done>
</task>

<task type="auto">
  <name>Task 4: Create test fixtures for mobile app</name>
  <files>apps/mobile/tests/fixtures/players.ts, apps/mobile/tests/fixtures/game-data.ts</files>
  <read_first>
    - apps/mobile/app/screens/ (for data structures used)
  </read_first>
  <action>
    Create test fixtures for mobile app tests:

    1. Create apps/mobile/tests/fixtures/players.ts:
       ```typescript
       export const mockPlayers = [
         { id: '1', name: 'Player 1', score: 10 },
         { id: '2', name: 'Player 2', score: 5 },
         { id: '3', name: 'Player 3', score: 15 },
       ]

       export const mockCategories = [
         { id: 1, name: 'Animals', letter: 'A' },
         { id: 2, name: 'Cities', letter: 'C' },
         { id: 3, name: 'Food', letter: 'F' },
       ]
       ```

    2. Create apps/mobile/tests/fixtures/game-data.ts:
       ```typescript
       export const mockGameSession = {
         id: 'test-session-1',
         players: mockPlayers,
         currentRound: 1,
         category: mockCategories[0],
         letter: 'A',
         status: 'active' as const,
         startTime: Date.now(),
       }

       export const mockGameHistory = [
         mockGameSession,
         { ...mockGameSession, id: 'test-session-2' },
       ]
       ```

    3. Create apps/mobile/tests/fixtures/index.ts:
       ```typescript
       export * from './players'
       export * from './game-data'
       ```

  </action>
  <verify>
    <automated>test -f apps/mobile/tests/fixtures/players.ts && test -f apps/mobile/tests/fixtures/game-data.ts</automated>
  </verify>
  <done>
    - players.ts fixture file created with mock data
    - game-data.ts fixture file created with mock session
    - Fixtures can be imported by tests
  </done>
</task>

</tasks>

<verification>
1. Verify test directories created: `test -d apps/mobile/tests/unit && test -d apps/mobile/tests/integration && test -d apps/mobile/tests/fixtures`
2. Verify test config exists: `test -f apps/mobile/vitest.config.ts || test -f apps/mobile/jest.config.js`
3. Verify package.json has test scripts: `grep "test:" apps/mobile/package.json`
4. Run unit tests: `cd apps/mobile && npm run test:unit`
5. Run integration tests: `cd apps/mobile && npm run test:integration`
6. Verify tests pass: Check output for "passed" and no "failed"
</verification>

<success_criteria>

- Test directory structure created (unit/, integration/, fixtures/)
- Test configuration file created (vitest.config.ts or jest.config.js)
- package.json has test scripts (test, test:unit, test:integration, test:watch)
- use-utilities.spec.ts created with 5+ unit tests
- game-flow.spec.ts created with 7+ integration tests
- Test fixtures created (players.ts, game-data.ts)
- All mobile tests pass with 100% success rate
  </success_criteria>

<output>
After completion, create `.planning/phases/21-refactor-and-fix-e2e-and-unit-tests/21-05-SUMMARY.md`
</output>
