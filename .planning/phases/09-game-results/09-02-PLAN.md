---
phase: 09-game-results
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/game/pages/leaderboard.vue
autonomous: false

must_haves:
  truths:
    - "Leaderboard displays 'Ranking' header in blue panel"
    - 'Crown icons appear for positions 1-3'
    - 'Numbered badges appear for positions 4-6'
    - 'Player names and scores align consistently in rows'
    - 'Scrollable list handles 1-6 players without layout breaking'
    - 'OK button navigates back to home'
  artifacts:
    - path: 'apps/game/pages/leaderboard.vue'
      provides: 'Leaderboard page using GameScrollList component'
      min_lines: 70
  key_links:
    - from: 'apps/game/pages/leaderboard.vue'
      to: 'GameScrollList'
      via: 'component usage with show-ranks prop'
      pattern: '<GameScrollList.*:show-ranks.*true'
    - from: 'apps/game/pages/leaderboard.vue'
      to: 'gameStore.leaderboard'
      via: 'reactive computed leaderboard data'
      pattern: "gameStore\\.leaderboard"
    - from: 'apps/game/pages/leaderboard.vue'
      to: 'GameDisplay'
      via: 'score display in slot content'
      pattern: '<GameDisplay.*totalScore'
---

<objective>
Refactor leaderboard page to use GameScrollList component with rank badges

Purpose: Display final game rankings matching leaderboard.png mockup using existing structural components
Output: Updated leaderboard page with crown/badge rankings and consistent styling
</objective>

<execution_context>
@/home/cloudcrusader/.claude/get-shit-done/workflows/execute-plan.md
@/home/cloudcrusader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-game-results/09-RESEARCH.md

# Existing page to refactor

@apps/game/pages/leaderboard.vue

# Components to use

@apps/game/components/game/GameScrollList.vue
@apps/game/components/game/GameHeader.vue
@apps/game/components/game/GameButton.vue
@apps/game/components/game/GameDisplay.vue
@apps/game/components/game/GameBackground.vue
@apps/game/components/game/GamePanel.vue

# Types

@packages/types/src/game.ts

# Design system

@apps/game/assets/scss/design-system.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor leaderboard page to use GameScrollList</name>
  <files>apps/game/pages/leaderboard.vue</files>
  <action>
Refactor existing leaderboard.vue page to use GameScrollList component with scoped slot pattern.

**Page structure:**

- GameBackground wrapper (existing)
- Main container: .leaderboard-page with flex-col, center alignment
- GameHeader with "Leaderboard" title and gold variant
- GamePanel with blue variant and "Ranking" subtitle
- GameScrollList with :show-ranks="true" prop and max-height="500px"
- GameButton(s) for navigation at bottom

**GameScrollList slot content:**

- Use default slot to render custom player rows
- v-for over leaderboard computed array (from gameStore.leaderboard)
- Each row: .leaderboard-row with flex, justify-between
- Row content: player name (left), GameDisplay with totalScore (right)
- GameScrollList automatically adds rank indicators (crowns 1-3, badges 4-6)

**Ranking subtitle panel:**

- GamePanel component with variant="blue" and padding="sm"
- h2.leaderboard-page\_\_subtitle with "Ranking" text
- Font: var(--font-display), var(--font-size-xl), var(--font-weight-bold)
- Color: white
- Text-transform: uppercase, letter-spacing: 1px

**Data binding:**

- Keep existing: `const leaderboard = computed(() => gameStore.leaderboard)`
- Keep existing: `const isGameCompleted = computed(() => gameStore.currentSession?.status === 'completed')`
- Use leaderboard array (already sorted by totalScore descending)

**Navigation buttons:**

- Container: .leaderboard-page\_\_actions with flex, gap, center alignment
- Conditional Next Round button: v-if="!isGameCompleted"
- OK/Finish button: variant="secondary", navigates to home

**Styling updates:**

- .leaderboard-page: flex-col, align-center, gap var(--spacing-2xl)
- Padding: var(--spacing-2xl) var(--spacing-md)
- Min-height: 100vh and 100dvh
- .leaderboard-page\_\_actions: flex, gap var(--spacing-lg), center, flex-wrap

**Leaderboard row styles:**

- .leaderboard-row: flex, align-center, justify-between, gap var(--spacing-md), width 100%
- .leaderboard-row\_\_name: flex: 1, font-display, font-size-lg, font-weight-semibold
- Name text: color var(--color-text-dark), overflow hidden, text-overflow ellipsis, white-space nowrap

**GameDisplay usage:**

- Size: "md"
- Glow: false (no glow effect for leaderboard scores)
- Display player's totalScore

**Remove old custom ranking logic:**

- Delete any custom crown/badge rendering code (GameScrollList handles this)
- Delete any manual scrollbar styling (GameScrollList has built-in styling)
- Keep store integration and navigation handlers

**i18n keys:**

- t('leaderboard.title', 'Leaderboard')
- t('leaderboard.ranking', 'Ranking')
- t('leaderboard.next_round', 'Next Round')
- t('leaderboard.finish', 'OK')

Follow pattern from RESEARCH.md code example "Leaderboard Page Refactor" section.
Do NOT include coin displays anywhere (explicitly excluded per project constraints).
</action>
<verify>
Run typecheck: `pnpm --filter @riddle-rush/game run typecheck`
Run dev server: `pnpm --filter @riddle-rush/game run dev`
Visit http://localhost:3000/leaderboard in browser
Page displays GameHeader, Ranking panel, GameScrollList with ranks, and buttons
GameScrollList shows crown for 1st place, crown for 2nd, crown for 3rd
GameScrollList shows numbered badge (4) for 4th place if >=4 players
</verify>
<done>
Leaderboard page refactored to use GameScrollList component with show-ranks prop, displaying crown icons for top 3 and numbered badges for 4-6
</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Scoring page (results/[[gameId]].vue) with GamePlayerCard components showing +pts/-pts indicators, and refactored leaderboard page using GameScrollList with crown/badge rankings
  </what-built>
  <how-to-verify>
**Visual verification against mockups:**

1. Start dev server: `pnpm --filter @riddle-rush/game run dev`

2. **Verify Scoring Page** (results/[[gameId]].vue):
   - Navigate to scoring page (may need to complete a game round first, or manually visit /results)
   - Compare against `docs/mockups/scoring.png`:
     - Header displays "Scoring" with gold 3D text effect
     - Player cards display in vertical list with cream panel background
     - Each card shows player label, name, and answer
     - Green +pts indicators appear for positive scores (light-to-dark green gradient)
     - Red -pts indicators appear for negative scores (light-to-dark red gradient)
     - No indicator shows when score is 0
     - Cards have gold border and shadow
     - "Next Round" button at bottom (green gradient)
     - No coins visible anywhere
   - Test responsiveness: Resize from 360px to 1024px width
   - Check hover states: Cards lift slightly on hover

3. **Verify Leaderboard Page** (leaderboard.vue):
   - Navigate to /leaderboard
   - Compare against `docs/mockups/leaderboard.png`:
     - Header displays "Leaderboard" with gold 3D text effect
     - Blue panel displays "Ranking" subtitle in white text
     - Player rows display in scrollable list with cream background
     - Gold crown for 1st place (left side)
     - Silver crown for 2nd place
     - Bronze crown for 3rd place
     - Numbered badge (4) for 4th place
     - Numbered badge (5) for 5th place
     - Numbered badge (6) for 6th place
     - Player names and scores align consistently
     - GameDisplay shows totalScore on right side
     - Navigation buttons at bottom ("Next Round" if not completed, "OK")
     - No coins visible anywhere
   - Test with different player counts (1-6 players)
   - Test scrolling if >4-5 players exceed max-height
   - Check custom scrollbar styling (webkit and Firefox)

4. **Verify Component Reusability:**
   - GamePlayerCard used in scoring page
   - GameScrollList used in leaderboard page with show-ranks prop
   - Consistent design system token usage across both pages

**Expected behavior:**

- Both pages match mockup layouts and color schemes
- Score indicators use correct gradient backgrounds (green/red)
- Rank indicators (crowns/badges) render correctly in order
- Pages scale responsively without breaking
- No TypeScript errors in console
- No coins visible on either page

**Report any issues:**

- Visual mismatches with mockups
- Missing/incorrect colors or spacing
- Broken responsive behavior
- TypeScript/runtime errors
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe any issues found</resume-signal>
  </task>

</tasks>

<verification>
1. TypeScript compilation passes with no errors
2. Leaderboard page uses GameScrollList component with show-ranks prop
3. Crown icons (gold/silver/bronze) appear for ranks 1-3
4. Numbered badges appear for ranks 4-6
5. "Ranking" subtitle displays in blue panel with white text
6. Player names and scores align in rows using flex layout
7. GameDisplay shows totalScore for each player
8. Navigation buttons (Next Round, OK) function correctly
9. Page matches leaderboard.png mockup structure
10. No coins visible anywhere on the page
11. Scoring page displays player cards with conditional indicators
12. Both pages scale responsively from 360px to 1024px
</verification>

<success_criteria>

- Leaderboard page refactored to use GameScrollList component
- GameScrollList receives show-ranks prop set to true
- Ranking subtitle displays in GamePanel with blue variant
- Crown icons render for top 3 positions
- Numbered badges render for positions 4-6
- Player rows use scoped slot pattern with custom content
- GameDisplay components show totalScore values
- Page layout matches leaderboard.png mockup structure
- Human verification confirms visual match with both mockups
- No coins visible on either page
  </success_criteria>

<output>
After completion, create `.planning/phases/09-game-results/09-02-SUMMARY.md`
</output>
