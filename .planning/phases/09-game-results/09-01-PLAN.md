---
phase: 09-game-results
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/game/components/game/GamePlayerCard.vue
  - apps/game/pages/results/[[gameId]].vue
autonomous: true

must_haves:
  truths:
    - 'Scoring page displays player cards with names and round scores'
    - 'Green +pts indicators appear for positive scores'
    - 'Red -pts indicators appear for negative scores'
    - 'No indicators show when score is 0'
    - 'NEXT ROUND button navigates to round-start page'
  artifacts:
    - path: 'apps/game/components/game/GamePlayerCard.vue'
      provides: 'Player score card with conditional score indicators'
      min_lines: 80
    - path: 'apps/game/pages/results/[[gameId]].vue'
      provides: 'Scoring page displaying round results'
      min_lines: 60
  key_links:
    - from: 'apps/game/pages/results/[[gameId]].vue'
      to: 'GamePlayerCard'
      via: 'component usage in v-for'
      pattern: '<GamePlayerCard.*:player='
    - from: 'apps/game/components/game/GamePlayerCard.vue'
      to: 'player.currentRoundScore'
      via: 'conditional indicator rendering'
      pattern: 'currentRoundScore.*!==.*0'
    - from: 'apps/game/pages/results/[[gameId]].vue'
      to: 'gameStore.players'
      via: 'reactive data binding'
      pattern: "gameStore\\.players"
---

<objective>
Create scoring page displaying player cards with animated +pts/-pts indicators

Purpose: Display round results with visual feedback for correct/incorrect answers matching scoring.png mockup
Output: GamePlayerCard component and results page showing score changes with green/red indicators
</objective>

<execution_context>
@/home/cloudcrusader/.claude/get-shit-done/workflows/execute-plan.md
@/home/cloudcrusader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-game-results/09-RESEARCH.md

# Existing components to reference

@apps/game/components/game/GameScrollList.vue
@apps/game/components/game/GameHeader.vue
@apps/game/components/game/GameButton.vue
@apps/game/components/game/GameDisplay.vue
@apps/game/components/game/GameBackground.vue

# Types

@packages/types/src/game.ts

# Design system

@apps/game/assets/scss/design-system.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GamePlayerCard component with score indicators</name>
  <files>apps/game/components/game/GamePlayerCard.vue</files>
  <action>
Create GamePlayerCard.vue component displaying player info with conditional score indicators.

**Component structure:**

- Props: player (Player type), label (string, optional), showIndicator (boolean, default true)
- Layout: Flex row with player info (left) and score indicator (right)
- Player info section: label (e.g., "Player 1"), player name, round answer (if available)
- Score indicator: Conditional rendering when showIndicator && currentRoundScore !== 0
- Variants: Positive (+pts green) vs negative (-pts red) based on score sign

**Styling:**

- Card background: var(--color-panel) gradient (cream panel from design system)
- Border: 3px solid var(--color-border-gold)
- Border radius: var(--radius-lg)
- Padding: var(--spacing-md) var(--spacing-lg)
- Shadow: 0 4px 8px rgba(0, 0, 0, 0.12)
- Hover: translateY(-2px) with enhanced shadow

**Score indicator styling:**

- Positive: background linear-gradient(180deg, var(--color-btn-green-light), var(--color-btn-green-dark))
- Positive text color: #2d5016 (dark green for contrast on light green)
- Negative: background linear-gradient(180deg, var(--color-btn-red-light), var(--color-btn-red-dark))
- Negative text color: white
- Both: border 2px solid matching shadow color, border-radius var(--radius-md)
- Padding: var(--spacing-xs) var(--spacing-md)
- Font: var(--font-display), var(--font-size-base), var(--font-weight-bold)
- Min-width: 70px for consistent sizing
- Box shadow: 0 2px 6px rgba(0, 0, 0, 0.15)

**Animation (optional):**

- Use v-motion directive with :initial="{ opacity: 0, scale: 0.9, x: -10 }"
- Enter: { opacity: 1, scale: 1, x: 0, transition: { duration: 350 } }

**Format score text:**

- Positive: "+{score}pts" (e.g., "+10pts")
- Negative: "{score}pts" (e.g., "-5pts")

Follow existing component patterns from GameButton and GameDisplay for structure and TypeScript typing.
</action>
<verify>
Run typecheck: `pnpm --filter @riddle-rush/game run typecheck`
Component file exists at apps/game/components/game/GamePlayerCard.vue
Component exports Props interface with player, label, showIndicator
Component has scoped styles with .game-player-card class
</verify>
<done>
GamePlayerCard component created with conditional green/red indicators based on currentRoundScore sign, styled using design system tokens
</done>
</task>

<task type="auto">
  <name>Task 2: Create scoring page with player cards list</name>
  <files>apps/game/pages/results/[[gameId]].vue</files>
  <action>
Create results/[[gameId]].vue page displaying scoring results with player cards.

**Page structure:**

- GameBackground wrapper (blue gradient backdrop)
- Main container: .scoring-page with flex-col, center alignment
- GameHeader with "Scoring" title (or i18n key) and gold variant
- Player cards list: .scoring-page\_\_list with gap var(--spacing-md)
- GameButton "Next Round" (primary variant, lg size) at bottom

**Data binding:**

- Import useGameStore from stores/game
- Get players array: `const players = computed(() => gameStore.players)`
- Use v-for to iterate over players array
- Pass player object to GamePlayerCard component
- Label each card: "Player 1", "Player 2", etc. using index + 1

**List animation (optional):**

- Add v-motion to each GamePlayerCard in v-for
- Stagger animation: :initial="{ opacity: 0, y: 20 }"
- Enter: { opacity: 1, y: 0, transition: { duration: 300, delay: index \* 50 } }

**Navigation:**

- Next Round button onClick: `await router.push('/round-start')`
- Import useRouter from vue-router (Nuxt auto-import)

**Styling:**

- .scoring-page: flex-col, align-center, gap var(--spacing-2xl)
- Padding: var(--spacing-2xl) var(--spacing-md)
- Min-height: 100vh and 100dvh (mobile viewport support)
- .scoring-page\_\_list: flex-col, gap var(--spacing-md), width 100%, max-width 600px

**i18n:**

- Use `const { t } = useI18n()` for translation keys
- Title: t('scoring.title', 'Scoring')
- Button: t('scoring.next_round', 'Next Round')
- Player label: t('scoring.player', 'Player')

**Meta:**

- Add useHead with title 'Scoring' and description

Follow existing page patterns from pages/players.vue and pages/game/[[gameId]].vue for structure.
Do NOT include coin displays anywhere (explicitly excluded per project constraints).
</action>
<verify>
Run typecheck: `pnpm --filter @riddle-rush/game run typecheck`
Run dev server: `pnpm --filter @riddle-rush/game run dev`
Visit http://localhost:3000/results in browser (may need test game session)
Page displays GameHeader, player cards, and Next Round button
</verify>
<done>
Scoring page created at pages/results/[[gameId]].vue displaying player cards with score indicators, GameHeader, and navigation button
</done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes with no errors
2. GamePlayerCard component renders with green +pts indicator when currentRoundScore > 0
3. GamePlayerCard component renders with red -pts indicator when currentRoundScore < 0
4. GamePlayerCard component shows no indicator when currentRoundScore === 0
5. Scoring page displays list of player cards with consistent styling
6. Next Round button navigates to /round-start when clicked
7. Page scales responsively from 360px to 1024px width
</verification>

<success_criteria>

- GamePlayerCard component exists and compiles without TypeScript errors
- Scoring page exists and compiles without TypeScript errors
- Component uses design system tokens (--color-btn-green-_, --color-btn-red-_)
- Indicators conditionally render based on score value
- Page layout matches scoring.png mockup structure (header, cards, button)
- No coins visible anywhere on the page
  </success_criteria>

<output>
After completion, create `.planning/phases/09-game-results/09-01-SUMMARY.md`
</output>
