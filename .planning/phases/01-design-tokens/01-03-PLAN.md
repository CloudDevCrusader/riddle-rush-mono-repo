---
phase: 01-design-tokens
plan: 03
type: execute
wave: 2
depends_on: ['01-01', '01-02']
files_modified:
  - apps/game/uno.config.ts
  - apps/game/app.vue
autonomous: false

must_haves:
  truths:
    - 'UnoCSS utility classes consume design-system.scss CSS variables correctly'
    - "class='gap-md' produces gap using var(--spacing-md)"
    - 'Embossed text effect visually matches mockup style'
    - 'Typography maintains readability across viewport sizes'
  artifacts:
    - path: 'apps/game/uno.config.ts'
      provides: 'Color shortcuts referencing SCSS tokens'
      contains: 'btn-green'
    - path: 'apps/game/app.vue'
      provides: 'Test page demonstrating token usage'
      min_lines: 20
  key_links:
    - from: 'apps/game/uno.config.ts'
      to: 'apps/game/assets/scss/design-system.scss'
      via: 'CSS variable references'
      pattern: "var\\(--color-"
---

<objective>
Wire UnoCSS theme to SCSS design tokens and verify the complete token system works correctly through visual testing.

Purpose: Ensure UnoCSS utilities and SCSS tokens work together seamlessly. The hybrid system should allow using `class="gap-md text-btn-green"` alongside custom CSS using `var(--color-btn-green-light)`.

Output: Fully integrated token system verified through a test component, ready for component development.
</objective>

<execution_context>
@/home/cloudcrusader/.claude/get-shit-done/workflows/execute-plan.md
@/home/cloudcrusader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-design-tokens/01-CONTEXT.md
@.planning/phases/01-design-tokens/01-RESEARCH.md
@.planning/phases/01-design-tokens/01-01-SUMMARY.md
@.planning/phases/01-design-tokens/01-02-SUMMARY.md
@apps/game/uno.config.ts
@apps/game/assets/scss/design-system.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend UnoCSS Theme with Color Shortcuts</name>
  <files>apps/game/uno.config.ts</files>
  <action>
Update uno.config.ts to add color shortcuts that reference the SCSS CSS variables. This allows using `text-btn-green` or `bg-btn-green` utility classes.

ADD the following to the theme object in uno.config.ts:

```typescript
theme: {
  // ... existing spacing and borderRadius ...

  // Color shortcuts referencing SCSS CSS variables
  colors: {
    // Button colors
    'btn-green': {
      light: 'var(--color-btn-green-light)',
      dark: 'var(--color-btn-green-dark)',
      shadow: 'var(--color-btn-green-shadow)',
    },
    'btn-blue': {
      light: 'var(--color-btn-blue-light)',
      dark: 'var(--color-btn-blue-dark)',
      shadow: 'var(--color-btn-blue-shadow)',
    },
    'btn-orange': {
      light: 'var(--color-btn-orange-light)',
      dark: 'var(--color-btn-orange-dark)',
      shadow: 'var(--color-btn-orange-shadow)',
    },
    'btn-red': {
      light: 'var(--color-btn-red-light)',
      dark: 'var(--color-btn-red-dark)',
      shadow: 'var(--color-btn-red-shadow)',
    },

    // Border colors
    'border-gold': 'var(--color-border-gold)',
    'border-orange': 'var(--color-border-orange)',

    // Text colors
    'game-yellow': 'var(--color-text-yellow)',
    'game-dark': 'var(--color-text-dark)',

    // Background gradient stops
    'bg-blue': {
      light: 'var(--color-bg-blue-light)',
      mid: 'var(--color-bg-blue-mid)',
      dark: 'var(--color-bg-blue-dark)',
    },
  },

  // Font family shortcuts
  fontFamily: {
    display: 'var(--font-display)',
    sans: 'var(--font-primary)',
  },
},
```

Also update the safelist to include commonly used color utilities:

```typescript
safelist: [
  // ... existing safelist ...
  'text-game-yellow',
  'text-game-dark',
  'text-btn-green-light',
  'bg-btn-green-light',
  'border-border-gold',
],
```

The full uno.config.ts should look like this after updates:

```typescript
import { defineConfig, presetWind, transformerDirectives, transformerVariantGroup } from 'unocss'

export default defineConfig({
  presets: [presetWind()],

  transformers: [transformerDirectives(), transformerVariantGroup()],

  theme: {
    spacing: {
      xs: 'var(--spacing-xs)',
      sm: 'var(--spacing-sm)',
      md: 'var(--spacing-md)',
      lg: 'var(--spacing-lg)',
      xl: 'var(--spacing-xl)',
      '2xl': 'var(--spacing-2xl)',
      '3xl': 'var(--spacing-3xl)',
    },

    borderRadius: {
      sm: 'var(--radius-sm)',
      md: 'var(--radius-md)',
      lg: 'var(--radius-lg)',
      xl: 'var(--radius-xl)',
      '2xl': 'var(--radius-2xl)',
      full: 'var(--radius-full)',
    },

    colors: {
      'btn-green': {
        light: 'var(--color-btn-green-light)',
        dark: 'var(--color-btn-green-dark)',
        shadow: 'var(--color-btn-green-shadow)',
      },
      'btn-blue': {
        light: 'var(--color-btn-blue-light)',
        dark: 'var(--color-btn-blue-dark)',
        shadow: 'var(--color-btn-blue-shadow)',
      },
      'btn-orange': {
        light: 'var(--color-btn-orange-light)',
        dark: 'var(--color-btn-orange-dark)',
        shadow: 'var(--color-btn-orange-shadow)',
      },
      'btn-red': {
        light: 'var(--color-btn-red-light)',
        dark: 'var(--color-btn-red-dark)',
        shadow: 'var(--color-btn-red-shadow)',
      },
      'border-gold': 'var(--color-border-gold)',
      'border-orange': 'var(--color-border-orange)',
      'game-yellow': 'var(--color-text-yellow)',
      'game-dark': 'var(--color-text-dark)',
      'bg-blue': {
        light: 'var(--color-bg-blue-light)',
        mid: 'var(--color-bg-blue-mid)',
        dark: 'var(--color-bg-blue-dark)',
      },
    },

    fontFamily: {
      display: 'var(--font-display)',
      sans: 'var(--font-primary)',
    },

    container: {
      center: true,
      padding: {
        DEFAULT: '1rem',
        sm: '1.5rem',
        lg: '2rem',
      },
    },
  },

  safelist: [
    'flex',
    'flex-col',
    'items-center',
    'justify-center',
    'gap-md',
    'gap-lg',
    'p-md',
    'p-lg',
    'px-md',
    'py-md',
    'text-game-yellow',
    'text-game-dark',
    'text-btn-green-light',
    'bg-btn-green-light',
    'border-border-gold',
  ],

  content: {
    pipeline: {
      include: [/\.vue$/, /\.ts$/, /\.tsx$/],
    },
  },
})
```

  </action>
  <verify>
Run `cd apps/game && grep "btn-green" uno.config.ts` - should find color definitions.
Run `cd apps/game && grep "font-display" uno.config.ts` - should find font family.
  </verify>
  <done>
UnoCSS theme.colors includes btn-green, btn-blue, btn-orange, btn-red with light/dark/shadow variants.
UnoCSS theme.fontFamily includes display and sans referencing CSS variables.
Safelist includes color utility classes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Token Test Component</name>
  <files>apps/game/app.vue</files>
  <action>
Temporarily add a token test section to app.vue to verify all tokens work. This will be removed after visual verification.

Read the current app.vue first to understand its structure, then ADD a test section that can be toggled on/off.

Add a computed ref and test section. The test section should demonstrate:

1. Color tokens (button colors, border colors)
2. Typography tokens (font sizes, display font)
3. Spacing tokens (gaps, padding)
4. Text effects (embossed styles)
5. Gradient backgrounds

Add this inside the main template, conditionally rendered:

```vue
<!-- Token Test Section - Remove after Phase 1 verification -->
<div v-if="showTokenTest" class="token-test">
  <div class="token-test__header">
    <h2 class="title-lg text-embossed-gold">Design Token Test</h2>
    <button @click="showTokenTest = false" class="close-btn">Close</button>
  </div>

  <!-- Color Tokens -->
  <section class="token-test__section">
    <h3 class="title-md text-embossed-white">Button Colors</h3>
    <div class="flex flex-wrap gap-md">
      <div class="color-swatch" style="background: var(--bg-gradient-btn-green)">Green</div>
      <div class="color-swatch" style="background: var(--bg-gradient-btn-blue)">Blue</div>
      <div class="color-swatch" style="background: var(--bg-gradient-btn-orange)">Orange</div>
      <div class="color-swatch" style="background: var(--bg-gradient-btn-red)">Red</div>
    </div>
  </section>

  <!-- Typography Scale -->
  <section class="token-test__section">
    <h3 class="title-md text-embossed-white">Typography Scale</h3>
    <p style="font-size: var(--font-size-xs)">XS - Extra Small Text</p>
    <p style="font-size: var(--font-size-sm)">SM - Small Text</p>
    <p style="font-size: var(--font-size-base)">Base - Body Text</p>
    <p style="font-size: var(--font-size-lg)">LG - Large Text</p>
    <p style="font-size: var(--font-size-xl)">XL - Extra Large</p>
    <p style="font-size: var(--font-size-2xl)">2XL - Heading</p>
    <p style="font-size: var(--font-size-3xl)">3XL - Title</p>
    <p class="font-display" style="font-size: var(--font-size-display)">Display</p>
  </section>

  <!-- Embossed Text Effects -->
  <section class="token-test__section">
    <h3 class="title-md text-embossed-white">Text Effects</h3>
    <p class="title-display text-embossed-gold">RIDDLE RUSH</p>
    <p class="title-lg text-embossed-white">White Embossed</p>
    <p class="title-md text-glow-gold">Golden Glow</p>
  </section>

  <!-- Spacing Scale -->
  <section class="token-test__section">
    <h3 class="title-md text-embossed-white">Spacing Scale</h3>
    <div class="flex flex-col gap-sm">
      <div class="spacing-demo" style="width: var(--spacing-xs)">xs</div>
      <div class="spacing-demo" style="width: var(--spacing-sm)">sm</div>
      <div class="spacing-demo" style="width: var(--spacing-md)">md</div>
      <div class="spacing-demo" style="width: var(--spacing-lg)">lg</div>
      <div class="spacing-demo" style="width: var(--spacing-xl)">xl</div>
      <div class="spacing-demo" style="width: var(--spacing-2xl)">2xl</div>
    </div>
  </section>

  <!-- UnoCSS Utilities Test -->
  <section class="token-test__section">
    <h3 class="title-md text-embossed-white">UnoCSS Utilities</h3>
    <div class="flex items-center gap-md p-md rounded-lg" style="background: rgba(0,0,0,0.2)">
      <span class="text-game-yellow">Yellow Text</span>
      <span class="text-btn-green-light">Green Text</span>
      <span class="text-white">White Text</span>
    </div>
  </section>
</div>
```

Add the script setup ref:

```typescript
const showTokenTest = ref(true) // Set to false by default after testing
```

Add scoped styles for the test component:

```scss
.token-test {
  position: fixed;
  inset: 0;
  z-index: 9999;
  background: var(--bg-gradient-main);
  overflow-y: auto;
  padding: var(--spacing-lg);
}

.token-test__header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--spacing-xl);
}

.close-btn {
  padding: var(--spacing-sm) var(--spacing-md);
  background: var(--bg-gradient-btn-red);
  border: none;
  border-radius: var(--radius-md);
  color: white;
  cursor: pointer;
}

.token-test__section {
  margin-bottom: var(--spacing-xl);
  padding: var(--spacing-md);
  background: rgba(0, 0, 0, 0.15);
  border-radius: var(--radius-lg);
}

.color-swatch {
  width: 100px;
  height: 60px;
  border-radius: var(--radius-md);
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
  font-weight: bold;
  text-shadow: 0 1px 2px rgba(0, 0, 0, 0.3);
}

.spacing-demo {
  height: 30px;
  background: var(--color-btn-blue-light);
  border-radius: var(--radius-sm);
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
  font-size: var(--font-size-xs);
}
```

  </action>
  <verify>
Run `cd apps/game && pnpm run dev` - server should start.
Open http://localhost:3000 - should see token test overlay.
  </verify>
  <done>
Token test section added to app.vue.
Test shows color swatches, typography scale, text effects, spacing demos.
Page loads without errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete design token system with:
1. UnoCSS installed and configured with preset-wind
2. SCSS design tokens enhanced with mockup-specific colors, gradients, typography, and spacing
3. UnoCSS theme wired to consume SCSS CSS variables
4. Token test component demonstrating all tokens
  </what-built>
  <how-to-verify>
1. Open http://localhost:3000 in browser
2. Token test overlay should be visible
3. Verify each section:

**Button Colors:**

- Should see 4 gradient button swatches (Green, Blue, Orange, Red)
- Green should be bright lime-to-green gradient
- Colors should match mockup buttons (playful, vibrant)

**Typography Scale:**

- Font sizes should progress from XS (small) to Display (large)
- Display text should use Baloo 2 font (rounded, playful)
- Resize browser 360px to 1080px - sizes should scale smoothly (no jumps)

**Text Effects:**

- "RIDDLE RUSH" should have gold/yellow color with 3D embossed shadow effect
- "White Embossed" should have white text with subtle depth
- "Golden Glow" should have soft outer glow

**Spacing Scale:**

- Bars should progressively increase in width
- Resize browser - spacing should scale proportionally

**UnoCSS Utilities:**

- "Yellow Text" should be gold/yellow
- "Green Text" should be bright green
- Items should be centered with gap between them

4. Check devtools:

- Inspect any element with `gap-md` class
- Should see computed value using `var(--spacing-md)`
- Check that clamp() values resolve correctly

5. Test responsiveness:

- Set viewport to 360px width (mobile)
- Set viewport to 1080px width (mockup design width)
- Typography and spacing should scale, not jump
  </how-to-verify>
  <resume-signal>
  Type "approved" if tokens look correct and match mockup style.
  Or describe any issues:
- Colors don't match mockup
- Text effects don't look embossed
- Scaling doesn't work
- Specific token missing or wrong
  </resume-signal>
  </task>

</tasks>

<verification>
Final verification after human approval:

1. UnoCSS integration complete:
   - Module registered in nuxt.config.ts
   - uno.config.ts has theme with colors, spacing, fonts

2. SCSS tokens enhanced:
   - All mockup colors as CSS variables
   - Button gradients (4 colors)
   - Text shadow presets (embossed effects)
   - Fluid typography scale (xs to display)
   - Fluid spacing scale (xs to 3xl)

3. Systems work together:
   - UnoCSS utilities reference CSS variables
   - class="gap-md" uses var(--spacing-md)
   - class="text-game-yellow" uses var(--color-text-yellow)

4. Visual quality:
   - Colors match mockup (vibrant, playful)
   - Embossed text has 3D depth
   - Scales smoothly across viewports

After approval, remove the token test section from app.vue (or set showTokenTest to false).
</verification>

<success_criteria>

1. UnoCSS theme extended with mockup-specific color shortcuts
2. Token test component demonstrates all token categories
3. Human verification confirms visual quality matches mockup
4. Typography readable at 360px and 1024px widths
5. Embossed text effects create 3D appearance
6. No console errors or SCSS compilation issues
   </success_criteria>

<output>
After completion, create `.planning/phases/01-design-tokens/01-03-SUMMARY.md`
</output>
