---
phase: 23-improve-performance-and-accessibility-find-out-why-there-are
plan: 04
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - apps/game/nuxt.config.ts
  - apps/game/app.vue
  - apps/game/package.json
  - apps/game/tests/e2e/smoke/performance-smoke.spec.ts
autonomous: true
requirements:
  - PERF-04
must_haves:
  truths:
    - 'App no longer depends on Google Fonts CDN at runtime.'
    - 'Non-critical/dev overlays are lazy-loaded to reduce initial bundle cost.'
    - 'Post-change Lighthouse and bundle metrics are captured against baseline.'
  artifacts:
    - path: apps/game/nuxt.config.ts
      provides: self-hosted font config and updated fontaine settings
    - path: apps/game/app.vue
      provides: removal of google fonts links + lazy-loaded debug overlays
    - path: .planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-PERF-RESULTS.md
      provides: baseline-vs-final metrics and 90+ target status
  key_links:
    - from: apps/game/nuxt.config.ts
      to: apps/game/app.vue
      via: head/font config + CSS imports replacing runtime CDN links
      pattern: '@fontsource-variable'
    - from: .planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-PERF-RESULTS.md
      to: .planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-PERF-BASELINE.md
      via: before/after metric comparison
      pattern: 'Delta'
---

<objective>
Complete the performance quick wins: self-host fonts, reduce initial JS through lazy-loading, and verify final Lighthouse/bundle targets.

Purpose: Deliver D-09, D-11, D-12, D-13 with measurable evidence tied to baseline.
Output: Font migration, lazy-load updates, and final performance result artifact.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-CONTEXT.md
@.planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-RESEARCH.md
@.planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-PERF-BASELINE.md
@apps/game/nuxt.config.ts
@apps/game/app.vue
@apps/game/package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate fonts to self-hosted packages and remove CDN runtime dependency (D-11)</name>
  <files>apps/game/package.json, apps/game/nuxt.config.ts, apps/game/app.vue</files>
  <read_first>
    - apps/game/package.json
    - apps/game/nuxt.config.ts
    - apps/game/app.vue
    - .planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: app head no longer injects fonts.googleapis.com / fonts.gstatic.com links.
    - Test 2: nuxt config includes self-hosted font CSS imports and matching fontaine metrics config.
    - Test 3: build output contains no runtime Google Fonts dependency warnings.
  </behavior>
  <action>Add `@fontsource-variable/baloo-2` and `@fontsource-variable/nunito` (devDependency per research), import them via `nuxt.config.ts` CSS pipeline, and remove Google Fonts `<link>` entries from `app.vue` `useHead()`. Update `fontMetrics.fonts` to match actual self-hosted font family names used by the app so fontaine works for real fonts, not Inter defaults. Remove stale PWA runtime caching rules for Google Fonts URLs after migration.</action>
  <acceptance_criteria>
    - `app.vue` contains no `fonts.googleapis.com` or `fonts.gstatic.com` references.
    - `nuxt.config.ts` includes `@fontsource-variable/baloo-2` and `@fontsource-variable/nunito` in CSS list.
    - `nuxt.config.ts` `fontMetrics.fonts` no longer uses only `Inter/system-ui` defaults.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @riddle-rush/game build</automated>
  </verify>
  <done>Fonts are self-hosted and no external Google Fonts requests are required at runtime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lazy-load non-critical overlays and collect final Lighthouse + bundle evidence (D-09, D-12, D-13)</name>
  <files>apps/game/app.vue, apps/game/tests/e2e/smoke/performance-smoke.spec.ts, .planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-PERF-RESULTS.md</files>
  <read_first>
    - apps/game/app.vue
    - .planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-PERF-BASELINE.md
    - .planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: app root uses LazyDebugPanel and LazyStoryboardDevOverlay instead of eager mounts.
    - Test 2: performance smoke check confirms app renders and key route is interactive after lazy-loading changes.
    - Test 3: final report records before/after Lighthouse and JS bundle numbers, plus 90+ target status.
  </behavior>
  <action>Replace eager overlay usage in `app.vue` with Nuxt lazy component prefix for non-critical panels per D-13. Run bundle analysis using existing visualizer output and capture initial JS size versus D-12 target (<500KB initial JS). Run lighthouse via scripts from Plan 23-01 and create `.planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-PERF-RESULTS.md` containing baseline vs final tables, per-category score deltas, pass/fail against D-09 90+ goal, and a short remediation note for any category below target.</action>
  <acceptance_criteria>
    - `app.vue` uses `LazyDebugPanel` and `LazyStoryboardDevOverlay`.
    - `23-PERF-RESULTS.md` includes sections: `## Baseline`, `## Final`, `## Delta`, `## Target Check`.
    - `23-PERF-RESULTS.md` includes explicit values for JS bundle size and all Lighthouse categories.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @riddle-rush/game run lighthouse:ci</automated>
  </verify>
  <done>Performance quick wins are implemented and final metrics are documented against baseline and targets.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                     | Description                                                         |
| -------------------------------------------- | ------------------------------------------------------------------- |
| dependency packages -> runtime asset loading | Third-party font packages become runtime-rendered typography assets |

## STRIDE Threat Register

| Threat ID | Category | Component            | Disposition | Mitigation Plan                                                                                                          |
| --------- | -------- | -------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------ |
| T-23-07   | T        | font loading path    | mitigate    | Use pinned package versions and remove remote CDN font fetches to reduce tampering/supply-chain-at-runtime exposure.     |
| T-23-08   | D        | lazy-loaded overlays | mitigate    | Keep lazy-loading scoped to non-critical overlays only, preserving core route render path and adding smoke verification. |

</threat_model>

<verification>
- `pnpm --filter @riddle-rush/game build`
- `pnpm --filter @riddle-rush/game run lighthouse:ci`
</verification>

<success_criteria>

- Google Fonts CDN links are removed from runtime.
- Non-critical overlays are lazy-loaded.
- Final Lighthouse/bundle report exists with baseline deltas and target checks.
  </success_criteria>

<output>
After completion, create `.planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-04-SUMMARY.md`
</output>
