---
phase: 23-improve-performance-and-accessibility-find-out-why-there-are
plan: 03
type: execute
wave: 2
depends_on:
  - 23-01
  - 23-02
files_modified:
  - apps/game/public/assets/**
  - apps/game/scripts/optimize-assets.mjs
  - apps/game/package.json
  - apps/game/components/SplashScreen.vue
  - apps/game/components/GlobalLoading.vue
autonomous: true
requirements:
  - PERF-03
must_haves:
  truths:
    - 'Production ships only referenced, normalized image assets.'
    - 'Referenced assets are available in optimized WebP form with fallback strategy.'
  artifacts:
    - path: apps/game/public/assets/
      provides: reduced and normalized asset set (no unreferenced legacy files)
    - path: apps/game/scripts/optimize-assets.mjs
      provides: reproducible WebP generation/optimization pipeline
    - path: apps/game/components/SplashScreen.vue
      provides: critical image rendering with eager loading and fallback-safe paths
  key_links:
    - from: apps/game/scripts/optimize-assets.mjs
      to: apps/game/public/assets/
      via: deterministic conversion pipeline
      pattern: '.webp'
    - from: apps/game/components/GlobalLoading.vue
      to: apps/game/public/assets/splash/
      via: helper-based references to normalized filenames
      pattern: "getAssetPath('assets/splash/"
---

<objective>
Apply file-level asset cleanup and optimization to cut payload size and eliminate fragile legacy filenames.

Purpose: Deliver D-01, D-03, D-05, D-07 with reproducible asset optimization and valid runtime references.
Output: Pruned asset tree, optimization script, and critical component path updates.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-CONTEXT.md
@.planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-RESEARCH.md
@apps/game/public/assets/
@apps/game/components/SplashScreen.vue
@apps/game/components/GlobalLoading.vue
@apps/game/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unreferenced assets and normalize referenced filenames (D-01, D-03)</name>
  <files>apps/game/public/assets/**, apps/game/components/SplashScreen.vue, apps/game/components/GlobalLoading.vue</files>
  <read_first>
    - .planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-CONTEXT.md
    - .planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-RESEARCH.md
    - apps/game/public/assets/
    - apps/game/components/SplashScreen.vue
    - apps/game/components/GlobalLoading.vue
  </read_first>
  <action>Delete unreferenced files under `apps/game/public/assets/` per D-01 (legacy coins/profiles/designer exports not referenced by any component). Rename all remaining referenced files to lowercase-kebab-case per D-03 (e.g., `BACKGROUND.png` -> `background.png`, `LOGO.png` -> `logo.png`, `LOADING_.png` -> `loading.png`) and update all affected splash/loading references accordingly via `getAssetPath` paths.</action>
  <acceptance_criteria>
    - No uppercase or spaced filenames remain in referenced asset set.
    - `apps/game/public/assets/` no longer contains unreferenced legacy directories identified in research.
    - `SplashScreen.vue` and `GlobalLoading.vue` reference normalized filenames only.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @riddle-rush/game exec node -e "const fs=require('fs');const path='public/assets';const walk=(d)=>fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(d+'/'+e.name):[d+'/'+e.name]);const bad=walk(path).filter(f=>/[A-Z ]/.test(f));if(bad.length){console.error(bad.join('\n'));process.exit(1)}"</automated>
  </verify>
  <done>Asset tree is pruned and remaining filenames are normalized to stable URL-safe naming.</done>
</task>

<task type="auto">
  <name>Task 2: Add reproducible WebP optimization pipeline and apply lazy-loading policy (D-05, D-07)</name>
  <files>apps/game/scripts/optimize-assets.mjs, apps/game/package.json, apps/game/components/SplashScreen.vue, apps/game/components/GlobalLoading.vue</files>
  <read_first>
    - apps/game/package.json
    - apps/game/components/SplashScreen.vue
    - apps/game/components/GlobalLoading.vue
    - .planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-RESEARCH.md
  </read_first>
  <action>Create `apps/game/scripts/optimize-assets.mjs` using installed `sharp` to generate `.webp` versions for all referenced PNGs (quality chosen by discretion, documented in script). Add script commands in `apps/game/package.json` (`assets:optimize`, optional `assets:verify`). Apply lazy-loading attributes per D-07: keep splash and above-the-fold loading visuals as eager/high-priority; ensure non-critical images in touched components are `loading=\"lazy\"`.
</action>
  <acceptance_criteria>
    - `apps/game/scripts/optimize-assets.mjs` exists and writes `.webp` outputs.
    - `apps/game/package.json` contains `assets:optimize` script.
    - Critical splash/loading images remain eager/high priority, non-critical touched images are lazy.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @riddle-rush/game run assets:optimize</automated>
  </verify>
  <done>Asset optimization is scriptable/repeatable and loading attributes align with critical vs non-critical policy.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                           |
| --------------------------------- | ----------------------------------------------------- |
| source assets -> optimized assets | Build script rewrites/creates production-served files |

## STRIDE Threat Register

| Threat ID | Category | Component              | Disposition | Mitigation Plan                                                                                                                     |
| --------- | -------- | ---------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| T-23-05   | T        | optimize-assets script | mitigate    | Limit script scope to `public/assets` and deterministic extension transforms (`.png` -> `.webp`) to avoid unintended file mutation. |
| T-23-06   | D        | critical splash assets | mitigate    | Keep splash/loading assets eager and verify paths post-rename to prevent blank initial render.                                      |

</threat_model>

<verification>
- `pnpm --filter @riddle-rush/game run assets:optimize`
- `pnpm --filter @riddle-rush/game exec node -e "const fs=require('fs');const path='public/assets';const walk=(d)=>fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(d+'/'+e.name):[d+'/'+e.name]);const bad=walk(path).filter(f=>/[A-Z ]/.test(f));if(bad.length){console.error(bad.join('\n'));process.exit(1)}"`
</verification>

<success_criteria>

- Unreferenced assets removed and referenced names normalized.
- WebP generation pipeline exists and runs successfully.
- Lazy-loading policy applied with critical-image exceptions preserved.
  </success_criteria>

<output>
After completion, create `.planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-03-SUMMARY.md`
</output>
