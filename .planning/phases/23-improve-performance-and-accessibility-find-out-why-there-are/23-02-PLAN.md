---
phase: 23-improve-performance-and-accessibility-find-out-why-there-are
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/game/composables/useAssets.ts
  - apps/game/composables/usePageSetup.ts
  - apps/game/components/SettingsModal.vue
  - apps/game/pages/game/[[gameId]].vue
  - apps/game/pages/round-start.vue
  - apps/game/layouts/menu.vue
  - apps/game/layouts/game.vue
  - apps/game/components/Base/ImageButton.vue
  - apps/game/tests/unit/use-assets.spec.ts
autonomous: true
requirements:
  - PERF-02
must_haves:
  truths:
    - 'All game image paths resolve through one stable helper instead of ad-hoc template literals.'
    - 'No production-critical image depends on broken ssr:false ipx runtime URLs.'
  artifacts:
    - path: apps/game/composables/useAssets.ts
      provides: single source of truth for asset path resolution
    - path: apps/game/composables/usePageSetup.ts
      provides: no duplicate getAssetPath implementation
    - path: apps/game/components/Base/ImageButton.vue
      provides: static-safe image rendering (no NuxtImg ipx runtime dependency)
  key_links:
    - from: apps/game/pages/game/[[gameId]].vue
      to: apps/game/composables/useAssets.ts
      via: getAssetPath usage for all in-page images
      pattern: 'getAssetPath'
    - from: apps/game/pages/round-start.vue
      to: apps/game/composables/useAssets.ts
      via: background image path via helper
      pattern: 'assets/alphabets/background'
---

<objective>
Fix the broken image loading path and consolidate asset resolution contracts across layouts/pages/components.

Purpose: Implement D-02 and D-04 while resolving the confirmed `NuxtImg` + `ssr:false` production breakage path.
Output: Unified path helper usage and static-safe image rendering across all listed files.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-CONTEXT.md
@.planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-RESEARCH.md
@apps/game/composables/useAssets.ts
@apps/game/composables/usePageSetup.ts
@apps/game/pages/round-start.vue
@apps/game/components/Base/ImageButton.vue
@apps/game/components/SettingsModal.vue
@apps/game/pages/game/[[gameId]].vue
@apps/game/layouts/menu.vue
@apps/game/layouts/game.vue
</context>

<interfaces>
From apps/game/composables/useAssets.ts:
```ts
const getAssetPath = (path: string): string
const getMenuAsset = (filename: string): string
const getSettingsAsset = (filename: string): string
```

Required contract change:

```ts
// keep getAssetPath as canonical API
// remove or replace helpers pointing to non-existent directories:
// getIconAsset() and getGameAsset()
```

</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize asset helper contracts and remove dead directory helpers (D-04)</name>
  <files>apps/game/composables/useAssets.ts, apps/game/composables/usePageSetup.ts, apps/game/tests/unit/use-assets.spec.ts</files>
  <read_first>
    - apps/game/composables/useAssets.ts
    - apps/game/composables/usePageSetup.ts
    - apps/game/tests/unit/use-assets.spec.ts
    - .planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: getAssetPath returns baseUrl-prefixed paths for relative and slash-prefixed inputs.
    - Test 2: no helper in useAssets references `assets/icons/` or `assets/game/`.
    - Test 3: usePageSetup no longer defines a separate getAssetPath implementation.
  </behavior>
  <action>Refactor `useAssets.ts` into canonical helper module: keep `getAssetPath` and only valid category helpers (`main-menu`, `settings`, `splash`, `alphabets`, `players` as needed). Remove `getIconAsset` and `getGameAsset` helpers (or replace with valid directory helpers) per D-04. In `usePageSetup.ts`, delete its duplicate path-builder and consume `useAssets().getAssetPath` directly so one implementation owns baseUrl behavior.</action>
  <acceptance_criteria>
    - `useAssets.ts` has no `assets/icons/` or `assets/game/` strings.
    - `usePageSetup.ts` has no local `const getAssetPath =` function body.
    - `use-assets.spec.ts` includes expectations for canonical helper behavior.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @riddle-rush/game test:unit -- tests/unit/use-assets.spec.ts</automated>
  </verify>
  <done>Asset path behavior is centralized and dead helpers are removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate all raw/template image references to canonical helper and static-safe tags (D-02)</name>
  <files>apps/game/components/SettingsModal.vue, apps/game/pages/game/[[gameId]].vue, apps/game/pages/round-start.vue, apps/game/layouts/menu.vue, apps/game/layouts/game.vue, apps/game/components/Base/ImageButton.vue</files>
  <read_first>
    - apps/game/components/SettingsModal.vue
    - apps/game/pages/game/[[gameId]].vue
    - apps/game/pages/round-start.vue
    - apps/game/layouts/menu.vue
    - apps/game/layouts/game.vue
    - apps/game/components/Base/ImageButton.vue
    - apps/game/composables/useAssets.ts
    - .planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: listed files no longer contain `` `${baseUrl}assets/` `` template string image paths.
    - Test 2: round-start and ImageButton no longer render NuxtImg with ipx `format/quality` runtime dependency.
    - Test 3: all migrated references call `getAssetPath()` from useAssets.
  </behavior>
  <action>Update every image reference in the six target files to call `getAssetPath('assets/...')` and remove raw template-literal path concatenation per D-02. Replace `NuxtImg` usage in `round-start.vue` and `Base/ImageButton.vue` with static-safe rendering (`img` or `picture`) so no `/_ipx` runtime URLs are required in production (`ssr:false` constraint from research). Preserve existing visual layout, dimensions, and test IDs; this task is path/rendering migration only.</action>
  <acceptance_criteria>
    - `SettingsModal.vue`, `game/[[gameId]].vue`, `round-start.vue`, `layouts/menu.vue`, `layouts/game.vue` contain no `` `${baseUrl}assets/` `` pattern.
    - `round-start.vue` contains no `<NuxtImg`.
    - `Base/ImageButton.vue` contains no `<NuxtImg`.
    - All listed files import/use `useAssets` and call `getAssetPath`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @riddle-rush/game typecheck</automated>
  </verify>
  <done>All targeted image references are canonicalized and production-safe for static deployment.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                   | Description                                                 |
| ------------------------------------------ | ----------------------------------------------------------- |
| runtime config baseUrl -> client image URL | Configured baseUrl directly influences asset URL generation |

## STRIDE Threat Register

| Threat ID | Category | Component                   | Disposition | Mitigation Plan                                                                                              |
| --------- | -------- | --------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------ |
| T-23-03   | T        | useAssets path construction | mitigate    | Normalize inputs in getAssetPath and keep single implementation to prevent divergent URL handling.           |
| T-23-04   | D        | production image delivery   | mitigate    | Remove NuxtImg ipx runtime dependency in ssr:false pages/components to avoid 404-driven denial of UI assets. |

</threat_model>

<verification>
- `pnpm --filter @riddle-rush/game test:unit -- tests/unit/use-assets.spec.ts`
- `pnpm --filter @riddle-rush/game typecheck`
</verification>

<success_criteria>

- Canonical getAssetPath usage replaces all targeted raw template image paths.
- Dead directory helpers are removed and covered by tests.
- No NuxtImg ipx runtime usage remains in the known broken files.
  </success_criteria>

<output>
After completion, create `.planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-02-SUMMARY.md`
</output>
