---
phase: 23-improve-performance-and-accessibility-find-out-why-there-are
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/game/package.json
  - turbo.json
  - .planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-PERF-BASELINE.md
autonomous: true
requirements:
  - PERF-01
must_haves:
  truths:
    - 'A single command runs Lighthouse auditing for this phase.'
    - 'A baseline performance report exists before optimization work begins.'
  artifacts:
    - path: apps/game/package.json
      provides: lighthouse script wiring (unlighthouse)
    - path: turbo.json
      provides: turborepo task registration for lighthouse command
    - path: .planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-PERF-BASELINE.md
      provides: pre-change Lighthouse baseline with route-level scores
  key_links:
    - from: apps/game/package.json
      to: turbo.json
      via: lighthouse task command mapping
      pattern: 'lighthouse'
    - from: .planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-PERF-BASELINE.md
      to: apps/game/package.json
      via: generated using package script
      pattern: 'pnpm run lighthouse'
---

<objective>
Establish measurement tooling and a hard baseline so optimization impact in this phase is quantifiable.

Purpose: Prevent "improvements" without evidence and satisfy D-08 + D-10 before implementation changes.
Output: Lighthouse command wiring and baseline report artifact.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-CONTEXT.md
@.planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-RESEARCH.md
@apps/game/package.json
@turbo.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Unlighthouse command and Turborepo task (D-08)</name>
  <files>apps/game/package.json, turbo.json</files>
  <read_first>
    - apps/game/package.json
    - turbo.json
    - .planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-CONTEXT.md
  </read_first>
  <action>Add `unlighthouse` as a devDependency in `apps/game/package.json` and add scripts: `lighthouse` (audits local preview), `lighthouse:ci` (non-interactive output for verification). Register matching `lighthouse` task in root `turbo.json` with `dependsOn: ["build"]` and `cache: false` so runs are always fresh per D-08.</action>
  <acceptance_criteria>
    - `apps/game/package.json` contains `"unlighthouse"` in `devDependencies`.
    - `apps/game/package.json` contains scripts `lighthouse` and `lighthouse:ci`.
    - `turbo.json` contains a `lighthouse` task configured with `dependsOn` and `cache: false`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @riddle-rush/game run lighthouse --help</automated>
  </verify>
  <done>Lighthouse tooling is installable and invokable from workspace scripts.</done>
</task>

<task type="auto">
  <name>Task 2: Capture and store pre-change Lighthouse baseline (D-10)</name>
  <files>.planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-PERF-BASELINE.md</files>
  <read_first>
    - .planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-RESEARCH.md
    - .planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-VALIDATION.md
  </read_first>
  <action>Run the new Lighthouse command against the generated game app before applying optimization plans. Create `23-PERF-BASELINE.md` capturing command, audited URLs/routes, and explicit numeric scores for Performance, Accessibility, Best Practices, SEO per D-10 and D-09 baseline expectation.</action>
  <acceptance_criteria>
    - `23-PERF-BASELINE.md` exists.
    - File contains sections: `## Command`, `## Audited Routes`, `## Scores`.
    - File includes numeric values for all four Lighthouse categories.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @riddle-rush/game run lighthouse:ci</automated>
  </verify>
  <done>Baseline is documented and can be compared against post-change results.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                             |
| ---------------------------- | ------------------------------------------------------- |
| local CLI -> report artifact | Command output becomes source of optimization decisions |

## STRIDE Threat Register

| Threat ID | Category | Component            | Disposition | Mitigation Plan                                                                           |
| --------- | -------- | -------------------- | ----------- | ----------------------------------------------------------------------------------------- |
| T-23-01   | T        | baseline report      | mitigate    | Record exact executed command and timestamp to prevent fabricated/manual score edits.     |
| T-23-02   | R        | lighthouse execution | mitigate    | Persist command + route list in baseline artifact for reproducibility during verify-work. |

</threat_model>

<verification>
- `pnpm --filter @riddle-rush/game run lighthouse --help`
- `pnpm --filter @riddle-rush/game run lighthouse:ci`
</verification>

<success_criteria>

- Lighthouse tooling is executable via pnpm and turbo.
- Baseline report exists with four-category numeric scores.
  </success_criteria>

<output>
After completion, create `.planning/phases/23-improve-performance-and-accessibility-find-out-why-there-are/23-01-SUMMARY.md`
</output>
