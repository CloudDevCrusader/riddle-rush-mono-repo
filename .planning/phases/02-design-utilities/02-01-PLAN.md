---
phase: 02-design-utilities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/game/assets/scss/effects/_scaling.scss
  - apps/game/assets/scss/effects/_shadows.scss
  - apps/game/assets/scss/design-system.scss
autonomous: true

must_haves:
  truths:
    - "Spacing, type, radius, and shadow values scale smoothly across common viewport widths without runaway sizes."
    - "Text glow and panel shadow effects can be applied to elements and visibly render layered depth."
  artifacts:
    - path: "apps/game/assets/scss/effects/_scaling.scss"
      provides: "Responsive scaling helper functions based on 1080x1920 mockup values"
    - path: "apps/game/assets/scss/effects/_shadows.scss"
      provides: "Text glow and panel drop shadow mixins with 3-5 layers"
    - path: "apps/game/assets/scss/design-system.scss"
      provides: "Forwards effect utilities and exposes quick-use classes"
  key_links:
    - from: "apps/game/assets/scss/design-system.scss"
      to: "apps/game/assets/scss/effects/_scaling.scss"
      via: "@forward effects/scaling"
      pattern: "@forward\s+['\"]\.?/?effects/scaling['\"]"
    - from: "apps/game/assets/scss/design-system.scss"
      to: "apps/game/assets/scss/effects/_shadows.scss"
      via: "@forward effects/shadows"
      pattern: "@forward\s+['\"]\.?/?effects/shadows['\"]"
    - from: "apps/game/assets/scss/design-system.scss"
      to: "apps/game/assets/scss/effects/_shadows.scss"
      via: "utility classes using shadow mixins"
      pattern: "@include\s+text-glow|@include\s+panel-shadow"
    - from: "apps/game/assets/scss/design-system.scss"
      to: "apps/game/assets/scss/effects/_scaling.scss"
      via: "token maps using mockup-clamp()"
      pattern: "mockup-clamp\("
---

<objective>
Add responsive scaling helpers and shared shadow/glow mixins so effects can be applied consistently without duplicating CSS.

Purpose: Establish the reusable foundation for mockup-accurate sizing and lighting effects before component work begins.
Output: Scaling utility functions, shadow/glow mixins, and quick-use utility classes exposed via the design system.
</objective>

<execution_context>
@/home/cloudcrusader/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/cloudcrusader/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-design-utilities/02-CONTEXT.md
@.planning/phases/02-design-utilities/02-RESEARCH.md
@apps/game/assets/scss/design-system.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mockup clamp() scaling utilities</name>
  <files>
    apps/game/assets/scss/effects/_scaling.scss
    apps/game/assets/scss/design-system.scss
  </files>
  <action>
    Create `apps/game/assets/scss/effects/_scaling.scss` with a small set of Sass helpers that convert mockup pixel values into bounded `clamp()` output.
    - Provide a function (for example `mockup-clamp($px, $min-scale: 0.86, $max-scale: 1.04, $base-width: 1080px)`) that returns `clamp(min, preferred, max)`.
    - Preferred value should be `calc(#{$px} * (100vw / #{$base-width}))` so the value scales with viewport width.
    - Min/max should be derived from the passed scales, keeping bounds moderate as per Phase 2 context.
    - Keep the API simple enough to reuse for font-size, spacing, radius, and shadow offsets.
    Update `apps/game/assets/scss/design-system.scss` to `@forward` the scaling utilities so they are available to component styles that `@use` the design system.
    Avoid JS, avoid new dependencies, and keep all sizing logic in CSS/Sass math.
  </action>
  <verify>rg "mockup-clamp|@function" apps/game/assets/scss/effects/_scaling.scss</verify>
  <done>Scaling helper function exists and the design system forwards it for use in component styles.</done>
</task>

<task type="auto">
  <name>Task 2: Add multi-layer shadow/glow mixins and utility classes</name>
  <files>
    apps/game/assets/scss/effects/_shadows.scss
    apps/game/assets/scss/design-system.scss
  </files>
  <action>
    Create `apps/game/assets/scss/effects/_shadows.scss` with mixins for multi-layer text glow and panel drop shadows.
    - Include a text glow mixin that applies 3-5 layered `text-shadow` entries, defaulting to warm gold glow using existing CSS variables.
    - Include a panel shadow mixin that combines a medium lift with a subtle inner shadow for depth, keeping blur radii modest for performance.
    - Use CSS variables for colors (`--color-text-yellow`, `--color-border-gold`, etc.) so the mixins stay token-driven.
    Update `apps/game/assets/scss/design-system.scss` to `@forward` the shadow mixins and add lightweight utility classes (e.g. `.fx-text-glow`, `.fx-panel-shadow`) that apply these mixins for quick visual verification.
    Avoid `filter`/`backdrop-filter` and keep to 3-5 layers maximum.
  </action>
  <verify>rg "@mixin\s+text-glow|@mixin\s+panel-shadow|fx-text-glow|fx-panel-shadow" apps/game/assets/scss</verify>
  <done>Shadow/glow mixins are defined, forwarded, and exposed via utility classes for quick usage.</done>
</task>

<task type="auto">
  <name>Task 3: Wire mockup-clamp into design tokens</name>
  <files>
    apps/game/assets/scss/design-system.scss
  </files>
  <action>
    Update the token maps in `apps/game/assets/scss/design-system.scss` to apply `mockup-clamp()` to mockup-based values so scaling is actually used.
    - Apply `mockup-clamp()` to spacing tokens (padding/margin steps), typography sizes (display/body scale), radius values, and shadow offsets/blur values.
    - Keep the existing token names and structure intact; only wrap numeric values where scaling is intended.
    - Avoid changing colors or introducing new tokens; this is wiring only.
  </action>
  <verify>rg "mockup-clamp\(" apps/game/assets/scss/design-system.scss</verify>
  <done>Design-system token maps use mockup-clamp for spacing, type sizes, radius, and shadow offsets.</done>
</task>

</tasks>

<verification>
pnpm run workspace:check
</verification>

<success_criteria>

- Responsive clamp utilities exist and can be used from component styles via the design system.
- Text glow and panel shadow mixins provide multi-layer effects without heavy filters.
- Utility classes allow immediate visual smoke testing of the new effects.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-design-utilities/02-01-SUMMARY.md`
</output>
