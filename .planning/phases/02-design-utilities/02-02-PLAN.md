---
phase: 02-design-utilities
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/game/assets/scss/effects/_glossy.scss
  - apps/game/assets/scss/effects/_embossed.scss
  - apps/game/assets/scss/design-system.scss
autonomous: false

must_haves:
  truths:
    - "Glossy button mixin renders a top-center highlight with a broad fade and soft bevel."
    - "Embossed panel mixin renders a gold/orange border with inner glow and pronounced depth."
  artifacts:
    - path: "apps/game/assets/scss/effects/_glossy.scss"
      provides: "Glossy button mixins with top-center highlight"
    - path: "apps/game/assets/scss/effects/_embossed.scss"
      provides: "Embossed panel border mixins with inner glow and depth"
    - path: "apps/game/assets/scss/design-system.scss"
      provides: "Forwards new mixins and exposes quick-use classes"
  key_links:
    - from: "apps/game/assets/scss/design-system.scss"
      to: "apps/game/assets/scss/effects/_glossy.scss"
      via: "@forward effects/glossy"
      pattern: "@forward\s+['\"]\.?/?effects/glossy['\"]"
    - from: "apps/game/assets/scss/design-system.scss"
      to: "apps/game/assets/scss/effects/_embossed.scss"
      via: "@forward effects/embossed"
      pattern: "@forward\s+['\"]\.?/?effects/embossed['\"]"
    - from: "apps/game/assets/scss/design-system.scss"
      to: "apps/game/assets/scss/effects/_glossy.scss"
      via: "utility class uses glossy mixin"
      pattern: "@include\s+glossy"
    - from: "apps/game/assets/scss/design-system.scss"
      to: "apps/game/assets/scss/effects/_embossed.scss"
      via: "utility class uses embossed mixin"
      pattern: "@include\s+embossed"
---

<objective>
Create glossy and embossed effect mixins that match mockup lighting and expose them for easy reuse across components.

Purpose: Provide the core visual treatments used by buttons and panels in the mockups.
Output: Glossy button mixin, embossed panel mixin, and utility classes wired through the design system.
</objective>

<execution_context>
@/home/cloudcrusader/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/cloudcrusader/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-design-utilities/02-CONTEXT.md
@.planning/phases/02-design-utilities/02-RESEARCH.md
@apps/game/assets/scss/design-system.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add glossy button mixin with top-center highlight</name>
  <files>
    apps/game/assets/scss/effects/_glossy.scss
    apps/game/assets/scss/design-system.scss
  </files>
  <action>
    Create `apps/game/assets/scss/effects/_glossy.scss` with a mixin (e.g. `@mixin glossy-button(...)`) that applies:
    - A vertical base gradient using existing button CSS variables (`--color-btn-green-light`, `--color-btn-green-dark`, etc.).
    - A broad top-center highlight using `&::before` with a radial gradient and low-opacity white.
    - Soft bevel via subtle outer shadow and a lighter top edge highlight.
    The mixin should set `position: relative`, `overflow: hidden`, and `border-radius: inherit` to support the highlight layer.
    Update `apps/game/assets/scss/design-system.scss` to `@forward` the glossy mixin and add a utility class (e.g. `.fx-glossy-button`) that applies it for quick visual testing.
    Keep the highlight broad (no thin specular line) and avoid heavy filters.
  </action>
  <verify>rg "@mixin\s+glossy|fx-glossy-button" apps/game/assets/scss</verify>
  <done>Glossy button mixin exists, is forwarded, and can be applied via a utility class.</done>
</task>

<task type="auto">
  <name>Task 2: Add embossed panel border mixin with inner glow</name>
  <files>
    apps/game/assets/scss/effects/_embossed.scss
    apps/game/assets/scss/design-system.scss
  </files>
  <action>
    Create `apps/game/assets/scss/effects/_embossed.scss` with a mixin (e.g. `@mixin embossed-panel(...)`) that applies:
    - A medium border thickness using warm gold/orange token variables.
    - An inner glow (inset shadow) with neutral white tint.
    - A pronounced bevel using a darker outer edge and subtle inner shadow.
    Update `apps/game/assets/scss/design-system.scss` to `@forward` the embossed mixin and add a utility class (e.g. `.fx-embossed-panel`) that applies it for quick visual testing.
    Keep the shadow stack at 3-5 layers max to avoid performance issues.
  </action>
  <verify>rg "@mixin\s+embossed|fx-embossed-panel" apps/game/assets/scss</verify>
  <done>Embossed panel mixin exists, is forwarded, and can be applied via a utility class.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Glossy and embossed effects with utility classes.</what-built>
  <how-to-verify>
    1. Run the app (or open any existing page that loads `apps/game/assets/scss/design-system.scss`).
    2. Apply `.fx-glossy-button`, `.fx-embossed-panel`, `.fx-text-glow`, and `.fx-panel-shadow` to a few elements via markup or devtools.
    3. Scroll the page for 10-15 seconds on desktop and confirm no visible jank or stutter.
  </how-to-verify>
  <resume-signal>Type "approved" if scrolling stays smooth, or describe the jank observed.</resume-signal>
</task>

</tasks>

<verification>
pnpm run workspace:check
</verification>

<success_criteria>

- Glossy button mixin produces a broad top-center highlight with soft beveling.
- Embossed panel mixin creates a pronounced 3D border with inner glow.
- Both effects are exposed via utility classes for quick visual checks.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-design-utilities/02-02-SUMMARY.md`
</output>
