---
phase: 19-move-from-pinia-to-zustand
plan: 02
type: execute
wave: 2
depends_on: ['19-01']
files_modified:
  - apps/game/composables/useGameState.ts
  - apps/game/composables/useGameActions.ts
  - apps/game/composables/useFeatureFlags.ts
  - apps/game/components/SettingsModal.vue
  - apps/game/components/DebugPanel.vue
  - apps/game/pages/settings.vue
  - apps/game/plugins/i18n.client.ts
  - apps/game/plugins/zustand.ts
  - apps/game/plugins/error-sync.client.ts
  - apps/game/app.vue
  - apps/game/nuxt.config.ts
  - apps/game/tsconfig.json
  - apps/game/package.json
  - apps/game/vitest.config.ts
  - apps/game/components/GlobalLoading.vue
autonomous: true
requirements:
  - MIGRATE-03
  - MIGRATE-04

must_haves:
  truths:
    - 'All 6+ consumer files import from focused Zustand hooks (not Pinia stores)'
    - 'Pinia is fully removed -- no @pinia/nuxt in modules, no pinia in package.json'
    - 'Old Pinia store files and stores/zustand/ directory are deleted'
    - 'App starts without Pinia runtime errors'
  artifacts:
    - path: 'apps/game/nuxt.config.ts'
      provides: 'Config without @pinia/nuxt module'
      contains: 'no @pinia/nuxt'
    - path: 'apps/game/composables/useGameState.ts'
      provides: 'Consumer using focused Zustand hooks'
      pattern: 'useGameSession|useCategories|useSettings'
    - path: 'apps/game/composables/useGameActions.ts'
      provides: 'Consumer using focused Zustand hooks'
      pattern: 'useGameSession|usePlayerActions|useCategories'
  key_links:
    - from: 'apps/game/composables/useGameState.ts'
      to: 'apps/game/stores/hooks/useGameSession.ts'
      via: 'import { useGameSession }'
      pattern: 'useGameSession'
    - from: 'apps/game/plugins/zustand.ts'
      to: 'apps/game/stores/migrate.ts'
      via: 'migrateFromPinia() call'
      pattern: 'migrateFromPinia'
---

<objective>
Big-bang consumer cutover: replace all Pinia store imports with focused Zustand hooks across all consumer files, remove Pinia entirely (package, config, plugins), delete old store files, and flatten the directory structure.

Purpose: Complete the actual migration -- swap every consumer to Zustand hooks and remove all traces of Pinia.
Output: Working app with zero Pinia dependencies, all state flowing through Zustand hooks.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-move-from-pinia-to-zustand/19-CONTEXT.md
@.planning/phases/19-move-from-pinia-to-zustand/19-RESEARCH.md
@.planning/phases/19-move-from-pinia-to-zustand/19-01-SUMMARY.md

@apps/game/composables/useGameState.ts
@apps/game/composables/useGameActions.ts
@apps/game/composables/useFeatureFlags.ts
@apps/game/components/SettingsModal.vue
@apps/game/components/DebugPanel.vue
@apps/game/pages/settings.vue
@apps/game/plugins/i18n.client.ts
@apps/game/plugins/zustand.ts
@apps/game/plugins/error-sync.client.ts
@apps/game/app.vue
@apps/game/nuxt.config.ts
@apps/game/package.json

<interfaces>
<!-- From Plan 01 -- these are the hooks consumers will import -->

From stores/hooks/useGameSession.ts:

- currentSession, history, isOnline, pendingPlayerNames, selectedLetter (computed refs)
- hasActiveSession, isGameCompleted, gameStatus, currentRound, currentCategory, currentLetter, players, currentPlayerTurn, allPlayersSubmitted, leaderboard (computed getters)
- resumeOrStartNewGame, startNewGame, endGame, completeGame, abandonGame, clearSession, loadFromDB, saveSessionToDB, saveHistoryToDB, loadSessionById, setupPlayers, generateLetter (actions)

From stores/hooks/useCategories.ts:

- categories, categoriesLoaded, categoriesLoading, displayedCategoryCount, categoryLoadError (computed refs)
- displayedCategories, hasMoreCategories (computed getters)
- fetchCategories, loadMoreCategories, resetDisplayedCategories, getCategoryById, getRandomCategory, categoryEmoji (actions)

From stores/hooks/usePlayerActions.ts:

- submitPlayerAnswer, assignPlayerScore, updatePlayerAvatar, completeRound, startNextRound, resetPlayerSubmissions, getPlayerById (actions)

From stores/hooks/useInstallPrompt.ts:

- installPromptEvent (computed ref), canInstall (computed getter)
- setInstallPrompt, showInstallPrompt (actions)

From stores/hooks/useSettings.ts:

- All GameSettings fields as computed refs
- isDebugMode, isLeaderboardEnabled, shouldShowLeaderboard, isFortuneWheelEnabled, isWebSocketEnabled, isAnswerInputEnabled (computed getters)
- updateSetting, toggleDebugMode, toggleLeaderboard, toggleSound, toggleFortuneWheel, toggleWebSocket, toggleAnswerInput, setOfflineMode, resetToDefaults, setLanguage, getLanguage (actions)

From stores/hooks/useLoading.ts:

- isLoading, progress, showProgress (computed refs)
- showLoading, hideLoading, setProgress (actions)
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Update all consumer files to use focused Zustand hooks</name>
  <files>
    apps/game/composables/useGameState.ts,
    apps/game/composables/useGameActions.ts,
    apps/game/composables/useFeatureFlags.ts,
    apps/game/components/SettingsModal.vue,
    apps/game/components/DebugPanel.vue,
    apps/game/pages/settings.vue,
    apps/game/plugins/i18n.client.ts,
    apps/game/app.vue,
    apps/game/components/GlobalLoading.vue
  </files>
  <action>
Update each consumer file to import from the new focused hooks. Per user decision: big-bang swap, no backward-compatible useGameStore().

**useGameState.ts**: Replace `const gameStore = useGameStore()` and `const settingsStore = useSettingsStore()` with destructured imports from `useGameSession()`, `useCategories()`, `useSettings()`. The returned computed refs can be spread directly since they're already computed. Read the current file to understand exactly what state/getters it exposes and map each to the correct hook.

**useGameActions.ts**: Replace `const gameStore = useGameStore()` with destructured imports from `useGameSession()`, `useCategories()`, `usePlayerActions()`. Map each action call (e.g., `gameStore.fetchCategories`) to the corresponding hook.

**useFeatureFlags.ts**: Replace all `useSettingsStore()` calls with `useSettings()`. Note: this file has multiple places where useSettingsStore() is called -- inside functions like `isEnabled()`, `loadFeatureFlagsFromGitLab()`, etc. Each call site needs updating.

**SettingsModal.vue**: Replace `const settingsStore = useSettingsStore()` with `const settings = useSettings()`. Update all template/script references from `settingsStore.x` to `settings.x`.

**DebugPanel.vue**: Replace `const gameStore = useGameStore()` with `const gameSession = useGameSession()` and `const settingsStore = useSettingsStore()` with `const settings = useSettings()`. Update all references.

**pages/settings.vue**: Replace `const settingsStore = useSettingsStore()` with `const settings = useSettings()`. Update all references.

**plugins/i18n.client.ts**: Replace `useSettingsStore()` calls with importing settingsStore directly from `~/stores/settingsStore` and using `settingsStore.getState()` -- plugins run outside Vue component scope so hooks with onScopeDispose won't work. Use `settingsStore.getState().language` for reading settings in plugin context.

**app.vue**: Replace `const gameStore = useGameStore()` with `const gameSession = useGameSession()` and `const settingsStore = useSettingsStore()` with `const settings = useSettings()`. Also add `const installPrompt = useInstallPrompt()` if install prompt is used. Update all template/script references.

**GlobalLoading.vue**: Verify it uses `useLoading()` -- if it auto-imports from the old `composables/useLoading.ts`, it will break once we delete that file. Ensure it resolves to the new `stores/hooks/useLoading` hook. If it uses auto-import, Nuxt should find it in stores/hooks/ since hooks are exported from stores/index.ts.

IMPORTANT: For Nuxt auto-imports to find the hooks in `stores/hooks/`, add `stores/hooks` to the `imports.dirs` array in nuxt.config.ts if not already scanned. Nuxt auto-imports from `stores/` but may not scan subdirectories. Add: `imports: { dirs: ['stores/hooks'] }` to nuxt.config.ts.
</action>
<verify>
<automated>cd apps/game && npx tsc --noEmit --pretty 2>&1 | head -50</automated>
</verify>
<done> - All 6+ consumer files import from focused Zustand hooks - No consumer file references useGameStore() or useSettingsStore() from Pinia - nuxt.config.ts imports.dirs includes 'stores/hooks' for auto-import
</done>
</task>

<task type="auto">
  <name>Task 2: Remove Pinia entirely, delete old files, update config and plugins</name>
  <files>
    apps/game/nuxt.config.ts,
    apps/game/tsconfig.json,
    apps/game/package.json,
    apps/game/vitest.config.ts,
    apps/game/plugins/zustand.ts,
    apps/game/plugins/error-sync.client.ts
  </files>
  <action>
1. **nuxt.config.ts**: Remove `'@pinia/nuxt'` from the `modules` array. Remove `'@pinia/nuxt'` from `typescript.tsConfig.compilerOptions.types`. Remove `'pinia'` from `vite.optimizeDeps.include` array. Add `'stores/hooks'` to `imports.dirs` if not done in Task 1.

2. **tsconfig.json**: Remove `"@pinia/nuxt"` from the `types` array.

3. **package.json**: Run `cd apps/game && pnpm remove @pinia/nuxt` to uninstall. This removes the direct dependency; pinia (transitive) goes with it.

4. **vitest.config.ts**: Remove `'pinia'` from the `globals.imports` array (line with `imports: ['vue', 'vue-router', 'pinia']` -- change to `imports: ['vue', 'vue-router']`).

5. **plugins/zustand.ts**: Update to:
   - Import from new store paths (`~/stores/gameStore`, `~/stores/settingsStore`, `~/stores/loadingStore`)
   - Call `migrateFromPinia()` from `~/stores/migrate` BEFORE any store access (per user decision: auto-migrate old Pinia localStorage data on first load)
   - Remove the loadSettings() call (persist middleware auto-rehydrates)
   - Keep the provide for backward compat or simplify to just run migration

6. **plugins/error-sync.client.ts**: Remove the entire Pinia error handler block (lines that reference `nuxtApp.$pinia`, `import type { Pinia } from 'pinia'`, and the `pinia.use(...)` block). Keep the rest of the plugin intact.

7. **Delete old files** (use git rm):
   - `stores/game.ts` (old Pinia game store)
   - `stores/settings.ts` (old Pinia settings store)
   - `composables/useLoading.ts` (old Pinia loading store)
   - `stores/zustand/` entire directory (files already copied to stores/ root in Plan 01)

8. After all changes, run `pnpm install` from repo root to update lockfile.

9. Run `pnpm run workspace:check` to verify everything compiles.
   </action>
   <verify>
   <automated>cd /Users/markuswagner/projects/riddle-rush-mono-repo && pnpm run workspace:check 2>&1 | tail -20</automated>
   </verify>
   <done> - @pinia/nuxt removed from package.json, nuxt.config.ts modules, tsconfig types - pinia removed from vitest globals - Old Pinia stores deleted (stores/game.ts, stores/settings.ts, composables/useLoading.ts) - stores/zustand/ directory fully removed - error-sync.client.ts has no Pinia references - zustand.ts plugin calls migrateFromPinia() and imports from new paths - workspace:check passes
   </done>
   </task>

</tasks>

<verification>
- `pnpm run workspace:check` passes (TypeScript + ESLint + Syncpack)
- `grep -r "pinia" apps/game/stores/ apps/game/composables/ apps/game/plugins/ apps/game/nuxt.config.ts apps/game/app.vue` returns no matches (except possibly comments)
- `grep -r "@pinia/nuxt" apps/game/` returns no matches
- No `stores/zustand/` directory exists
- No `stores/game.ts` or `stores/settings.ts` Pinia files exist
</verification>

<success_criteria>

- Zero Pinia imports anywhere in production code (stores, composables, plugins, components, pages)
- All consumers use focused Zustand hooks
- workspace:check passes clean
- Old files deleted, directory structure flattened
  </success_criteria>

<output>
After completion, create `.planning/phases/19-move-from-pinia-to-zustand/19-02-SUMMARY.md`
</output>
