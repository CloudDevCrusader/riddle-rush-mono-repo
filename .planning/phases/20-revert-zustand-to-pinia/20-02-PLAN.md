---
phase: 20-revert-zustand-to-pinia
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified: 
  - apps/game/stores/settingsStore.ts
  - apps/game/stores/loadingStore.ts
  - apps/game/pages/**/*.vue
  - apps/game/components/**/*.vue
  - apps/game/tests/**/*.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - 'All Vue components consume Pinia store instead of Zustand'
    - 'No reactivity issues between store and templates'
    - 'All existing page and component functionality preserved'
    - 'data-testid attributes working correctly for E2E tests'
    - 'Component reactivity updates when state changes'
  artifacts:
    - path: 'apps/game/stores/settingsStore.ts'
      provides: 'Pinia-based settings store'
      contains: 'defineStore', 'settings'
    - path: 'apps/game/stores/loadingStore.ts'
      provides: 'Pinia-based loading store'
      contains: 'defineStore', 'loading'
  key_links:
    - from: 'apps/game/pages/**/*.vue'
      to: 'stores/gameStore.ts'
      via: 'useGameStore() import'
      pattern: 'useGameStore\(\)'
    - from: 'apps/game/components/**/*.vue'
      to: 'stores/gameStore.ts'
      via: 'store methods consumption'
      pattern: 'gameStore\.'

<objective>
Complete the Pinia migration by converting all remaining stores and updating all Vue components to use Pinia instead of Zustand. This ensures full compatibility and eliminates any remaining Zustand dependencies.

Purpose: Complete state management reversion across entire codebase
Output: Fully Pinia-based codebase with no Zustand dependencies
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-revert-zustand-to-pinia/20-01-PLAN.md
@apps/game/stores/gameStore.ts
</context>

<tasks>

<task type="auto">
  <name>Convert settingsStore and loadingStore to Pinia</name>
  <files>apps/game/stores/settingsStore.ts, apps/game/stores/loadingStore.ts</files>
  <action>
    - Convert settingsStore from Zustand to Pinia defineStore
    - Convert loadingStore from Zustand to Pinia defineStore
    - Preserve all store logic and state structure
    - Maintain same API surface for consumption
    - Ensure all settings and loading state management works correctly
  </action>
  <verify>Both stores compile and work correctly</verify>
  <done>All remaining stores converted to Pinia</done>
</task>

<task type="auto">
  <name>Update all Vue components for Pinia consumption</name>
  <files>apps/game/pages/**/*.vue, apps/game/components/**/*.vue</files>
  <action>
    - Update all page components to use Pinia stores instead of Zustand
    - Update all components to use Pinia store patterns
    - Ensure reactivity works correctly between stores and templates
    - Preserve all existing component functionality
    - Fix any remaining template-state reactivity issues
    
    Key patterns to update:
    - Remove Zustand store imports
    - Add `useGameStore()`, `useSettingsStore()` imports
    - Ensure store.state and store getters work in templates
    - Fix reactivity issues for players.length, flowState etc.
  </action>
  <verify>All components compile and render correctly</verify>
  <done>All Vue components updated for Pinia</done>
</task>

<task type="auto">
  <name>Update all tests to use Pinia</name>
  <files>apps/game/tests/**/*.ts</files>
  <action>
    - Update unit tests to use Pinia stores instead of Zustand
    - Update E2E test helpers to access Pinia state correctly
    - Update wait conditions and state checks
    - Remove Zustand-specific test patterns
    - Ensure all test state access works with Pinia
    
    Key changes:
    - Update helpers/waits.ts to use Pinia getState patterns
    - Update E2E test helpers to access store state correctly
    - Update component tests for Pinia reactivity
    - Remove __zustand__ references, use proper Pinia access
  </action>
  <verify>All tests compile and run correctly</verify>
  <done>All tests updated for Pinia consumption</done>
</task>

</tasks>

<verification>
1. All Vue components use Pinia stores correctly
2. No reactivity issues in templates
3. All tests pass with Pinia
4. No Zustand imports remain in codebase
5. Component reactivity works properly
6. data-testid attributes function correctly
</verification>

<success_criteria>
- Entire codebase uses Pinia state management
- All components and pages work without reactivity issues
- All tests pass with Pinia
- No Zustand dependencies or imports remain
- Application is stable and production-ready
</success_criteria>

<output>
After completion, create `.planning/phases/20-revert-zustand-to-pinia/{phase}-{plan}-SUMMARY.md`
</output>
---
