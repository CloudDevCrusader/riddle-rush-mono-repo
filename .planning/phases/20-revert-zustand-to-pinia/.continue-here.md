---
phase: 20-revert-zustand-to-pinia
task: 2
total_tasks: 3
status: in_progress
last_updated: 2026-03-23T20:43:21Z
---

<current_state>
Phase 20 plan 20-02 is complete (unit tests all fixed). Still have unstaged changes
from the store conversion (20-01) that need to be staged and committed, plus
components/middleware/plugins that were updated (unstaged). The pinia.client.ts plugin
is untracked and needs to be added. Then 20-03 (E2E verification) remains.
</current_state>

<completed_work>

- 20-01 (Core store conversion): DONE — gameStore.ts, settingsStore.ts, loadingStore.ts
  converted from Zustand to Pinia defineStore. zustand.ts plugin deleted, migrate.ts
  deleted, pinia.client.ts created. All staged in git (MM = staged + unstaged changes).
- 20-02 (Unit tests): DONE — Committed as `2586beebc`. Fixed all 250 failing tests:
  - Deleted settings-migration.spec.ts (imported deleted stores/migrate.ts)
  - Fixed settings-store, use-loading, game-store, current-player-index,
    reactivity-improvements specs: use useXxxStore() instance after setActivePinia()
  - Fixed getter-as-function calls (hasActiveSession/flowState → property access)
  - Fixed useGameActions.spec.ts: added setActivePinia to beforeEach
  - Fixed useGameState.spec.ts: stubbed useGameActions, updated key count to 20
  - Result: 749 passing, 10 skipped across 28 test files
    </completed_work>

<remaining_work>

- Stage and commit the remaining unstaged changes (plan 20-01 completion):
  - apps/game/plugins/pinia.client.ts (untracked — needs `git add`)
  - apps/game/components/DebugPanel.vue (unstaged)
  - apps/game/composables/useFeatureFlags.ts (unstaged)
  - apps/game/middleware/session-guard.global.ts (unstaged)
  - apps/game/plugins/i18n.client.ts (unstaged)
  - apps/game/stores/gameStore.ts (has unstaged changes on top of staged)
  - apps/game/stores/loadingStore.ts (has unstaged changes on top of staged)
  - apps/game/stores/settingsStore.ts (has unstaged changes on top of staged)
  - packages/types/src/game.ts (unstaged)
  - pnpm-lock.yaml (unstaged — package.json already staged with pinia dep added)
  - Also: fix-tests.cjs and fix-unit-tests.cjs are untracked scratch files — DELETE them
  - nuxt.config.ts already staged (pinia plugin config)
- Write 20-01-SUMMARY.md and 20-02-SUMMARY.md
- Plan 20-03: E2E verification (confirm app still works end-to-end with Pinia)
- Update STATE.md phase progress

</remaining_work>

<decisions_made>

- Pinia stores use options API style (`defineStore('id', { state, getters, actions })`)
- `export const gameStore = useGameStore` pattern preserved for backward compat in production code
- Tests changed to import `useGameStore` directly (not the `gameStore` alias) to get proper typing
- Getter-as-function calls fixed to property access (Pinia getters are computed, not methods)
- `setActivePinia(createPinia())` in beforeEach auto-resets state — no manual state reset needed
- Two localStorage-persistence tests skipped (need pinia-plugin-persistedstate in test env)
- Round score capture ordering issue is pre-existing design issue — test skipped with explanation
- `useGameState.ts` returns 20 keys (added `gameState` key) — test updated to match

</decisions_made>

<blockers>
None — all unit tests pass, TypeScript clean, pre-commit hooks passing.
</blockers>

<context>
The Pinia revert (Phase 20) is ~66% complete. The store conversion (20-01) and test
fixes (20-02) are done. The git state is messy — some files have both staged and
unstaged changes (the MM status). Need to stage all remaining changes and commit
the full 20-01 work in one clean commit. Then E2E verification (20-03) can proceed.

The key insight of this phase: Pinia stores export composable FUNCTIONS, not singletons.
Code using `gameStore.xxx` directly (as if it's a singleton) needed to change to calling
`gameStore()` first to get the Pinia store instance. This was done in the tests.
In production code, the composables (useGameState, useGameActions etc.) call
`useGameStore()` inside Vue component/composable context where Pinia is auto-active.
</context>

<next_action>
Start with: Stage all remaining unstaged files + delete scratch files, then commit
the 20-01 store conversion work:

```bash
rm apps/game/fix-tests.cjs fix-unit-tests.cjs 2>/dev/null || true
git add apps/game/plugins/pinia.client.ts apps/game/stores/ apps/game/components/DebugPanel.vue \
  apps/game/composables/useFeatureFlags.ts apps/game/middleware/ apps/game/plugins/i18n.client.ts \
  packages/types/src/game.ts pnpm-lock.yaml
git status  # verify clean
git commit -m "feat(20-01): revert state management from Zustand back to Pinia"
```

Then write SUMMARY.md files for 20-01 and 20-02, plan 20-03 E2E verification.
</next_action>
