---
phase: 14-maintenance-quality-of-life
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/game/composables/usePlayerManager.ts
  - apps/game/pages/game/[[gameId]].vue
  - apps/game/nuxt.config.ts
  - apps/game/app.d.ts
autonomous: true
requirements:
  - MAINT-MULTI-01
  - MAINT-TS-01
must_haves:
  truths:
    - 'pnpm run typecheck passes with no errors (not intermittently — clean every run)'
    - 'The multiplayer round-skip root cause is identified: either confirmed fixed by quick task #007 or a targeted fix is applied'
    - 'Game store structure is documented (brief note in SUMMARY confirming Phase 12 refactor is complete, deferring deep rewrite to Phase 19)'
    - 'pnpm run workspace:check passes'
  artifacts:
    - path: 'apps/game/composables/usePlayerManager.ts'
      provides: 'Turn progression logic — reviewed and patched if round-skip bug is still present'
    - path: 'apps/game/nuxt.config.ts'
      provides: 'PWA type conflict suppressed if that is the source of the intermittent typecheck error'
  key_links:
    - from: 'apps/game/pages/game/[[gameId]].vue'
      to: 'apps/game/composables/usePlayerManager.ts'
      via: 'advancePlayerIndex — currentPlayerIndex must reset to 0 before new round'
      pattern: 'currentPlayerIndex'
---

<objective>
Investigate and fix two independent bugs: (1) the intermittent `nuxi typecheck` error related to `@vite-pwa/nuxt`, and (2) the multiplayer round-skip bug where some players are skipped in round 1. Also produce a brief assessment confirming the game store's current structure is sound.

Purpose: Eliminate flaky TypeScript errors that erode developer confidence in the typecheck gate, and fix (or confirm fixed) the multiplayer round progression bug that corrupts game sessions for 2-3 player games.
Output: Clean typecheck run, confirmed or patched multiplayer turn flow, game store assessment note.
</objective>

<execution_context>
@/Users/markuswagner/projects/riddle-rush-mono-repo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markuswagner/projects/riddle-rush-mono-repo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-maintenance-quality-of-life/14-RESEARCH.md

<interfaces>
<!-- advancePlayerIndex in usePlayerManager.ts (verified from research) -->
<!-- Returns currentIndex + 1 — intentionally non-wrapping -->
<!-- When index reaches players.length, currentPlayerTurn getter returns null -->
<!-- Bug: if currentPlayerIndex is NOT reset to 0 before a new round, -->
<!-- the first submit goes to players[players.length] = null (player skipped) -->

<!-- startNextRound in stores/game.ts resets currentPlayerIndex = 0 -->
<!-- but ONLY after round advances — timing with IndexedDB restore is the concern -->

<!-- Quick task #007 (f205f0de3) "Fix page reload on game start causing wrong player" -->
<!-- may have already addressed the root cause -->
<!-- MUST check if the bug is reproducible BEFORE writing a fix -->

<!-- TypeScript error context from STATE.md: -->
<!-- "@vite-pwa/nuxt augments NuxtConfig type, sometimes generates incompatible VitePlugin types" -->
<!-- Established fix pattern: @ts-expect-error with comment (per STATE.md decisions) -->
<!-- nuxt.config.ts already imports: type { Manifest as ViteBundleManifest } from 'vue-bundle-renderer' -->
<!-- This import may itself be the source or a red herring -->

<!-- Game store: 344 lines, already delegated to 5 composables in Phase 12 -->
<!-- Phase 19 (Pinia → Zustand) will rewrite it anyway — defer deep refactor -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate and fix intermittent nuxi typecheck error</name>
  <files>apps/game/nuxt.config.ts, apps/game/app.d.ts</files>
  <action>
**Step 1 — Capture the exact error:**
Run `pnpm run typecheck` from the project root (or `cd apps/game && npx nuxi typecheck`). Capture the full output. If it passes cleanly, run it 2-3 more times to check for intermittency.

**Step 2 — Identify the error source:**

- If error is in `nuxt.config.ts` itself: look for the `@vite-pwa/nuxt` type that conflicts
- If error is in `.nuxt/types/` generated files: the fix goes in a type augmentation, not in `nuxt.config.ts`
- If error involves the `ViteBundleManifest` import on line 7 of nuxt.config.ts: that unused import may be causing the issue — remove it if unused

**Step 3 — Apply the fix (choose the appropriate approach):**

Option A — If error is a specific line in nuxt.config.ts conflicting with @vite-pwa/nuxt types:
Add `// @ts-expect-error — @vite-pwa/nuxt type conflict with vite-plugin-pwa VitePlugin` on the line above the error. Per STATE.md established pattern.

Option B — If the `ViteBundleManifest` import is unused and causing a type error:
Remove the import: `import type { Manifest as ViteBundleManifest } from 'vue-bundle-renderer'`

Option C — If error is in generated `.nuxt/` files (not project source):
Add a `skipLibCheck: true` directive or update the tsconfig.json `include` to exclude `.nuxt/` from strict checking. Check `apps/game/tsconfig.json` first — `skipLibCheck` may already be set.

Option D — If typecheck passes cleanly on 3 consecutive runs:
Document as likely fixed by a prior dependency update. Note in SUMMARY that it could not be reproduced.

**Step 4 — Verify the fix is stable:**
Run `pnpm run typecheck` twice more to confirm no intermittency.
</action>
<verify>
<automated>cd /Users/markuswagner/projects/riddle-rush-mono-repo && pnpm run typecheck 2>&1 | tail -10</automated>
</verify>
<done>pnpm run typecheck exits 0 with no errors. When run 3 times in sequence, it passes each time.</done>
</task>

<task type="auto">
  <name>Task 2: Investigate and fix multiplayer round-skip bug, assess game store</name>
  <files>apps/game/composables/usePlayerManager.ts, apps/game/pages/game/[[gameId]].vue</files>
  <action>
**Part A — Multiplayer round-skip investigation:**

Read `apps/game/composables/usePlayerManager.ts` fully, then read the game page `apps/game/pages/game/[[gameId]].vue` script section.

Trace the full flow:

1. `advancePlayerIndex(currentIndex, _playerCount)` — confirm it returns `currentIndex + 1` (no wrap)
2. `currentPlayerTurn` getter — confirm it returns `players[currentPlayerIndex] ?? null`
3. `startNextRound` action in stores/game.ts — confirm it sets `currentPlayerIndex = 0` BEFORE saving to IndexedDB
4. Check `resetPlayerRoundState` call — does it reset `hasSubmitted` for all players?
5. In the game page template: check `v-if="allPlayersSubmitted || players.length === 0"` on NEXT button

**Specific check for the IndexedDB restore path:**
When the game page mounts with an existing session (restored from IndexedDB), is `currentPlayerIndex` already 0?

- If session was saved mid-round (currentPlayerIndex = 1 for a 2-player game), after a page reload the first player would be skipped.
- Quick task #007 (commit f205f0de3) reportedly fixed "page reload on game start causing wrong player" — check if that fix covers this scenario.

**If bug is still reproducible (index not reset on restore):**
Add a guard in the game page's `onMounted` or session loading logic: if `currentPlayerTurn` is null and NOT all players have submitted, reset `currentPlayerIndex` to 0 via a store action.

OR: In `startNextRound` store action, ensure `resetPlayerRoundState` is called AND `currentPlayerIndex = 0` is set BEFORE any `saveSessionToDB()` call so the IndexedDB-persisted state is always correct.

**If bug cannot be reproduced after quick task #007:**
Document in SUMMARY that the bug appears to have been fixed by #007 and include the reasoning. No code change needed.

**Part B — Game store assessment (read-only):**
Read `apps/game/stores/game.ts`. Count lines. List the 5 composables it delegates to. Confirm no obvious structural issues. Note: Phase 19 (Pinia→Zustand) will rewrite this store — defer any deep refactor. Document the assessment in the SUMMARY.
</action>
<verify>
<automated>cd /Users/markuswagner/projects/riddle-rush-mono-repo && pnpm run workspace:check 2>&1 | tail -5</automated>
</verify>
<done>workspace:check passes. Either: (a) multiplayer turn progression has a confirmed fix applied with explanation of what was changed, or (b) bug is confirmed fixed by quick task #007 with reasoning documented in SUMMARY. Game store assessment is documented in SUMMARY (line count, delegate composables, recommendation to defer to Phase 19).</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `pnpm run typecheck` exits 0 (run twice to confirm stability)
2. `pnpm run workspace:check` passes
3. If a code fix was applied to usePlayerManager or game page: `pnpm run test:unit` passes
4. SUMMARY documents the game store assessment (344 lines, 5 composables, defer to Phase 19)
</verification>

<success_criteria>

- pnpm run typecheck passes cleanly (no intermittent failures)
- Multiplayer round-skip bug: either confirmed fixed (with evidence from #007 commit analysis) or a targeted fix applied
- Game store: documented as lean/well-structured, no changes made (defer to Phase 19 Zustand migration)
- workspace:check passes
  </success_criteria>

<output>
After completion, create `.planning/phases/14-maintenance-quality-of-life/14-03-SUMMARY.md`
</output>
