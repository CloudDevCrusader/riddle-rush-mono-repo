---
phase: 14-maintenance-quality-of-life
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/game/translations/locales/de.json
  - apps/game/translations/locales/en.json
  - apps/game/pages/credits.vue
  - apps/game/pages/splash.vue
  - apps/game/pages/game/[[gameId]].vue
  - apps/game/pages/round-start.vue
  - apps/game/pages/language.vue
autonomous: true
requirements:
  - MAINT-I18N-01
must_haves:
  truths:
    - 'All visible user-facing text in credits, splash, game, round-start, and language pages uses t() calls — no hardcoded English or German strings'
    - 'de.json has no duplicate key sections — language and settings blocks each appear exactly once'
    - 'en.json contains every key in de.json with English values'
    - 'pnpm run workspace:check passes with no TypeScript or lint errors'
  artifacts:
    - path: 'apps/game/translations/locales/de.json'
      provides: 'German translations including new game.pause, game.round_start_title, game.round_start_description, home.page_title keys; merged duplicate language and settings sections'
    - path: 'apps/game/translations/locales/en.json'
      provides: 'English translations with same keys as de.json, all values in English'
  key_links:
    - from: 'apps/game/pages/credits.vue'
      to: 'apps/game/translations/locales/de.json'
      via: "t('credits.game_design'), t('credits.programming'), t('credits.art'), t('common.back')"
      pattern: "t\\('credits\\."
    - from: 'apps/game/pages/game/[[gameId]].vue'
      to: 'apps/game/translations/locales/de.json'
      via: "t('game.page_title'), t('game.pause'), t('common.loading'), t('common.back')"
      pattern: "t\\('game\\."
---

<objective>
Replace all remaining hardcoded user-facing strings with i18n translation keys, and fix the duplicate key sections in de.json that silently shadow earlier-defined translations.

Purpose: Complete the i18n implementation so all text is translatable and the locale JSON files are structurally sound — no silent key shadowing, no English fallbacks leaking into the German UI.
Output: Updated locale JSON files (de.json, en.json) with new keys and merged duplicate sections; updated Vue pages using t() for all visible text and aria-labels.
</objective>

<execution_context>
@/Users/markuswagner/projects/riddle-rush-mono-repo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markuswagner/projects/riddle-rush-mono-repo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-maintenance-quality-of-life/14-RESEARCH.md

<interfaces>
<!-- i18n pattern used throughout this project -->
<!-- Translation files are at apps/game/translations/locales/{de,en}.json -->
<!-- i18n config: apps/game/translations/i18n.config.ts -->
<!-- nuxt.config.ts sets restructureDir: '.' — paths resolve from project root -->

Usage pattern in Vue SFC:

```vue
<script setup lang="ts">
const { t } = useI18n()
useHead({ title: t('game.page_title') })
</script>
<template>
  <span>{{ t('common.loading') }}</span>
  <button :aria-label="t('game.pause')">
  <img :alt="t('common.back')" />
</template>
```

Known existing keys (de.json verified):

- common.back = "Zurück"
- common.loading = "Laden..."
- game.page_title = "Riddle Rush - Spiel"
- credits.game_design = "Spieldesign"
- credits.programming = "Programmierung"
- credits.art = "Kunst"
- menu.title = "Menü"

Keys to ADD (missing from both locale files):

- game.pause = "Spiel pausieren" / "Pause game"
- game.round_start_title = "Rundenstart" / "Round Start"
- game.round_start_description = "Dreht für Kategorie und Buchstabe" / "Spinning for category and letter"
- home.page_title = "Hauptmenü" / "Main Menu"

CRITICAL — de.json has duplicate sections that must be MERGED (not appended):

- "language" key appears twice — merge to single block keeping all unique keys
- "settings" key appears twice — merge to single block keeping all unique keys
  JSON parsers use the LAST definition; merging prevents silent key shadowing.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Fix de.json duplicate sections and add missing translation keys</name>
  <files>apps/game/translations/locales/de.json</files>
  <action>
Read the full de.json file first. Perform two operations:

**Operation A — Merge duplicate sections:**
The file has "language" defined twice (around lines 160-163 and 221-225) and "settings" defined twice (around lines 211-219 and 288-300). For each duplicate:

1. Identify both occurrences
2. Merge their keys into a single object (union of all keys from both blocks)
3. Remove the first occurrence, keep one clean block
4. Verify the merged block contains all keys from both original blocks

**Operation B — Add missing keys to the correct namespaces:**
Add these keys inside the existing namespace blocks (do NOT create new top-level blocks):

- Inside "game" block: `"pause": "Spiel pausieren"`, `"round_start_title": "Rundenstart"`, `"round_start_description": "Dreht für Kategorie und Buchstabe"`
- Inside "home" block (create if missing): `"page_title": "Hauptmenü"`
- Inside "menu" block (if home block doesn't exist, add page_title there): verify menu.title = "Menü" already exists

Write the corrected de.json. Validate it is valid JSON by checking structure.
</action>
<verify>
<automated>cd /Users/markuswagner/projects/riddle-rush-mono-repo && node -e "const j = require('./apps/game/translations/locales/de.json'); console.log('game.pause:', j.game?.pause); console.log('game.round_start_title:', j.game?.round_start_title); console.log('home or menu page_title:', j.home?.page_title || j.menu?.page_title); const keys = JSON.stringify(j); const langCount = (keys.match(/"language":/g) || []).length; const settCount = (keys.match(/"settings":/g) || []).length; console.log('language sections:', langCount, '(expected 1)'); console.log('settings sections:', settCount, '(expected 1)');"</automated>
</verify>
<done>de.json is valid JSON, contains exactly one "language" block and one "settings" block, and the game.pause / game.round_start_title / game.round_start_description / home.page_title keys are present with German values.</done>
</task>

<task type="auto">
  <name>Task 2: Update en.json with matching keys and wire hardcoded strings in Vue pages</name>
  <files>
    apps/game/translations/locales/en.json,
    apps/game/pages/credits.vue,
    apps/game/pages/splash.vue,
    apps/game/pages/game/[[gameId]].vue,
    apps/game/pages/round-start.vue,
    apps/game/pages/language.vue
  </files>
  <action>
**Step 1 — Update en.json:**
Add the same keys added to de.json in Task 1, with English values:
- `game.pause = "Pause game"`
- `game.round_start_title = "Round Start"`
- `game.round_start_description = "Spinning for category and letter"`
- `home.page_title = "Main Menu"` (or inside menu block to match de.json structure)

**Step 2 — Wire Vue pages (read each file before editing):**

`pages/credits.vue`:

- Replace `Back` button text with `{{ t('common.back') }}`
- Replace `Game Design` heading with `{{ t('credits.game_design') }}`
- Replace `Programming` heading with `{{ t('credits.programming') }}`
- Replace `Art` heading with `{{ t('credits.art') }}`
- Ensure `const { t } = useI18n()` is in script setup

`pages/splash.vue` (line 65):

- Replace hardcoded `LOADING....` text with `{{ t('common.loading') }}`
- Note: `RIDDLE RUSH` in GameHeader is the app name — intentionally NOT translated, leave as-is

`pages/game/[[gameId]].vue`:

- Line 30: Replace `aria-label="Pause game"` with `:aria-label="t('game.pause')"`
- Line 16: Replace `alt="Back"` with `:alt="t('common.back')"`
- Line 65: Replace `: 'LOADING...'` fallback with `: t('common.loading')`
- Line 314: Replace `title: 'Riddle Rush - Game'` in useHead with `title: t('game.page_title')`

`pages/round-start.vue` (lines 305-309):

- Replace `title: 'Round Start'` with `title: t('game.round_start_title')`
- Replace description string with `t('game.round_start_description')`

`pages/language.vue` (line 5):

- Replace `aria-label="Go back"` with `:aria-label="t('common.back')"`

`pages/index.vue` (line 118):

- Replace `title: 'Main Menu'` with `title: t('home.page_title')` or `t('menu.page_title')` — use whichever key path matches de.json

For each file: read the file first, make the minimal targeted changes only for the listed strings, preserve all surrounding code.
</action>
<verify>
<automated>cd /Users/markuswagner/projects/riddle-rush-mono-repo && pnpm run workspace:check 2>&1 | tail -20</automated>
</verify>
<done>workspace:check passes. No hardcoded English strings remain in the listed Vue pages for the identified locations. en.json contains all keys present in de.json. Both locale files are valid JSON.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `pnpm run workspace:check` passes (TypeScript + ESLint + syncpack)
2. `node -e "require('./apps/game/translations/locales/de.json')"` exits 0 (valid JSON)
3. `node -e "require('./apps/game/translations/locales/en.json')"` exits 0 (valid JSON)
4. `grep -r "aria-label=\"Go back\"\|aria-label=\"Pause game\"\|alt=\"Back\"" apps/game/pages/` returns no matches
5. `grep -rn "Game Design\|Programming\|Art\b" apps/game/pages/credits.vue` returns no matches for hardcoded headings
</verification>

<success_criteria>

- All 13 identified hardcoded strings in the 5 pages replaced with t() calls
- de.json has exactly one "language" block and one "settings" block (no duplicate sections)
- en.json mirrors de.json key structure with English values
- workspace:check passes clean
  </success_criteria>

<output>
After completion, create `.planning/phases/14-maintenance-quality-of-life/14-01-SUMMARY.md`
</output>
