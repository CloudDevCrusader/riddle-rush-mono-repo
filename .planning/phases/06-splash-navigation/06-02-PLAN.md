---
phase: 06-splash-navigation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/game/pages/index.vue
autonomous: true

must_haves:
  truths:
    - 'Main menu displays PLAY/MENU/OPTIONS/CREDITS buttons stacked vertically'
    - 'Buttons use GameButton component with proper variants and visual feedback'
    - 'No coins or profile avatar visible on page'
    - 'Blue gradient background matches other pages'
    - 'Buttons respond to touch/click with 3D press effect'
  artifacts:
    - path: 'apps/game/pages/index.vue'
      provides: 'Refactored main menu with GameButton components'
      min_lines: 150
      exports: []
  key_links:
    - from: 'apps/game/pages/index.vue'
      to: 'components/game/GameButton.vue'
      via: 'component usage for menu buttons'
      pattern: '<GameButton variant='
    - from: 'apps/game/pages/index.vue'
      to: 'components/game/GameBackground.vue'
      via: 'component usage for background'
      pattern: '<GameBackground'
---

<objective>
Refactor main menu (index.vue) to match `start.png` mockup by replacing image buttons with GameButton components and ensuring no coins/avatar appear.

Purpose: Align main menu visual design with mockup using design system components
Output: Refactored index.vue with GameButton components and GameBackground
</objective>

<execution_context>
@/home/cloudcrusader/.claude/get-shit-done/workflows/execute-plan.md
@/home/cloudcrusader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-splash-navigation/06-RESEARCH.md

# File to refactor

@apps/game/pages/index.vue

# Components to use

@apps/game/components/game/GameButton.vue
@apps/game/components/game/GameBackground.vue
@apps/game/components/game/GameHeader.vue

# Mockup reference

Mockup: docs/mockups/start.png

- PLAY button (primary/green)
- MENU button (secondary/blue)
- OPTIONS button (warning/orange)
- CREDITS button (secondary/blue)
- Buttons stacked vertically with consistent spacing
- No coins, no profile avatar
- Logo/title at top
  </context>

<tasks>

<task type="auto">
  <name>Refactor index.vue to use GameButton components and remove image assets</name>
  <files>apps/game/pages/index.vue</files>
  <action>
Refactor existing index.vue page by replacing PNG image buttons with GameButton components:

**Template changes:**

1. Replace `<img src="BACKGROUND.png">` with `<GameBackground />` component
2. Logo section: Keep existing LOGO.png image OR optionally replace with GameHeader component (decision: keep image if it exists and looks good, otherwise use GameHeader with color="gold")
3. Replace image-based menu buttons with GameButton components:
   - PLAY button: `<GameButton variant="primary" size="lg" full-width @click="handlePlay">{{ $t('menu.play', 'PLAY') }}</GameButton>`
   - MENU button: `<GameButton variant="secondary" size="lg" full-width @click="toggleMenu">{{ $t('menu.menu', 'MENU') }}</GameButton>`
   - OPTIONS button: `<GameButton variant="warning" size="lg" full-width @click="wrappedGoToSettings">{{ $t('menu.options', 'OPTIONS') }}</GameButton>`
   - CREDITS button: `<GameButton variant="secondary" size="lg" full-width @click="wrappedGoToCredits">{{ $t('menu.credits', 'CREDITS') }}</GameButton>`
4. Keep menu panel structure (language/settings) with GameButton components for consistency
5. Remove all image button imports and references to PLAY.png, OPTIONS.png, CREDITS.png

**Script changes:**

1. Existing logic remains: showMenu ref, toggleMenu, handlePlay, wrapped navigation methods
2. Keep onMounted logic for needsGame query handling
3. All composables (usePageSetup, useNavigation) remain unchanged
4. Keep existing methods and their logic intact

**Style changes:**

1. Remove image-specific styles: .menu-btn, .btn-image classes
2. Update .menu-buttons: Keep flex column layout, gap, alignment, max-width 400px
3. Update .logo-container and .logo-image: Keep existing responsive sizing
4. Remove .page-bg styles (replaced by GameBackground component)
5. Update .container: Remove explicit background, GameBackground handles it
6. Keep .menu-panel styles for toggled menu
7. Keep responsive breakpoints but simplify since buttons now use GameButton's built-in responsive behavior
8. Remove custom animation keyframes if GameButton provides sufficient feedback
9. Use design system spacing variables (--spacing-\*) consistently

**Why these specific choices:**

- GameButton instead of images: Provides consistent 3D press effect, better accessibility, smaller bundle size
- Keep existing PNG logo: Asset already exists and matches mockup (if quality is good)
- variant="primary" for PLAY: Green gradient emphasizes main action
- variant="warning" for OPTIONS: Orange gradient matches mockup color
- variant="secondary" for MENU/CREDITS: Blue gradient for secondary actions
- size="lg": Larger touch targets matching mockup button size
- full-width prop: Buttons fill container width for better mobile UX
- GameBackground component: Consistent blue gradient across all pages
- Keep i18n translation keys: Supports German/English language switching
- Preserve menu panel toggle: Existing UX pattern users may expect
  </action>
  <verify>

1. Run `pnpm run typecheck` - TypeScript compilation passes
2. Run `pnpm run lint` - No linting errors
3. Run `pnpm run dev`
4. Navigate to http://localhost:3000/ (main menu)
5. Verify PLAY/MENU/OPTIONS/CREDITS buttons display stacked vertically
6. Verify buttons use gradient styling (green/blue/orange) matching mockup
7. Click each button and verify navigation works (PLAY -> players, OPTIONS -> settings, CREDITS -> credits)
8. Click MENU button, verify language/settings panel appears
9. Verify no coins visible anywhere on page
10. Verify no profile avatar visible
11. Verify logo displays at top
12. Test button press feedback (should show inset effect)
13. Test responsive behavior at 360px, 768px, 1024px widths
14. Compare visual against docs/mockups/start.png mockup
    </verify>
    <done>

- index.vue uses GameBackground component instead of PNG background
- All menu buttons (PLAY/MENU/OPTIONS/CREDITS) use GameButton component
- Button variants match mockup: primary (green), secondary (blue), warning (orange)
- No image button assets (PLAY.png, OPTIONS.png, CREDITS.png) referenced in code
- No coins visible on page
- No profile avatar visible on page
- Logo displays at top (either PNG image or GameHeader)
- Buttons stacked vertically with consistent spacing
- Button press shows 3D inset effect
- Menu panel toggle works with language/settings buttons
- All navigation actions work correctly
- TypeScript compilation passes
- ESLint passes with no errors
- Visual comparison with mockup confirms alignment
  </done>
  </task>

</tasks>

<verification>
**Visual verification:**
1. Compare main menu against `docs/mockups/start.png`
2. Verify button stack matches mockup (PLAY, MENU, OPTIONS, CREDITS order)
3. Verify button colors match mockup (green, blue, orange, blue)
4. Verify no coins or avatar visible
5. Verify logo positioning and size match mockup

**Functional verification:**

1. PLAY button navigates to /players
2. MENU button toggles language/settings panel
3. OPTIONS button navigates to /settings
4. CREDITS button navigates to /credits
5. Language/Settings buttons in menu panel work
6. Button press shows visual feedback (3D inset)
7. No console errors

**Code quality:**

1. TypeScript compilation passes
2. ESLint passes
3. No unused imports or variables
4. Responsive styles work across viewport sizes
   </verification>

<success_criteria>
**Measurable completion:**

1. `apps/game/pages/index.vue` imports and uses GameButton component
2. All four main menu buttons use GameButton with appropriate variants
3. GameBackground component replaces PNG background image
4. No references to PLAY.png, OPTIONS.png, CREDITS.png in template
5. No coins or avatar elements in template
6. Navigation to /players, /settings, /credits works
7. Button press effect visible on click
8. Visual match with mockup confirmed
9. `pnpm run typecheck` passes
10. `pnpm run lint` passes
    </success_criteria>

<output>
After completion, create `.planning/phases/06-splash-navigation/06-02-SUMMARY.md`
</output>
