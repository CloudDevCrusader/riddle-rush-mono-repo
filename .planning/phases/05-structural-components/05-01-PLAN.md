---
phase: 05-structural-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/game/components/game/GameHeader.vue
  - apps/game/components/game/GameScrollList.vue
autonomous: true

must_haves:
  truths:
    - 'Page titles display with bold 3D text effects matching mockup style'
    - 'Back button slot renders when provided in GameHeader'
    - 'Player/leaderboard lists show rank badges (crowns 1-3, numbered 4-6)'
    - 'ScrollList handles 1-6 items without layout breaking'
  artifacts:
    - path: 'apps/game/components/game/GameHeader.vue'
      provides: 'Page title bar with 3D text effects and optional slots'
      exports: ['default']
    - path: 'apps/game/components/game/GameScrollList.vue'
      provides: 'Scrollable list with rank display and slot-based row content'
      exports: ['default']
  key_links:
    - from: 'apps/game/components/game/GameHeader.vue'
      to: 'assets/scss/effects/_shadows.scss'
      via: 'text-glow mixin for 3D text effects'
      pattern: '@use.*_shadows.*text-glow'
    - from: 'apps/game/components/game/GameScrollList.vue'
      to: 'inline SVG icons'
      via: 'crown icons in template'
      pattern: '<svg.*crown'
---

<objective>
Create GameHeader and GameScrollList components for page structure and list displays.

Purpose: Provide reusable header bar with 3D title effects and scrollable player/leaderboard lists with rank badges. These are the final structural components before page migration begins in Phase 6.

Output: Two Vue SFC components following established patterns from GameButton/GameDisplay, with full SCSS styling and responsive behavior.
</objective>

<execution_context>
@/home/cloudcrusader/.claude/get-shit-done/workflows/execute-plan.md
@/home/cloudcrusader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-structural-components/05-CONTEXT.md

# Existing component patterns

@apps/game/components/game/GameButton.vue
@apps/game/components/game/GameDisplay.vue

# Available SCSS mixins

@apps/game/assets/scss/effects/\_shadows.scss
</context>

<tasks>

<task type="auto">
  <name>Create GameHeader component with 3D text effects and slots</name>
  <files>apps/game/components/game/GameHeader.vue</files>
  <action>
Create GameHeader component at `apps/game/components/game/GameHeader.vue` following the GameDisplay.vue pattern:

**Component Props:**

- `variant?: 'white' | 'gold' | 'green' | 'blue' | 'orange'` (default: 'white') - Text color matching game button colors
- `tag?: 'h1' | 'h2' | 'h3' | 'div'` (default: 'h1') - Semantic HTML tag

**Template Structure:**

- Root div with class `game-header` (acts as full header bar, not just text)
- Flexbox layout: `left` slot | centered title | `right` slot
- Left slot for back button (optional, used on Players page)
- Right slot for future flexibility (currently unused but provide for consistency)
- Center area uses component :is tag for title with default slot content
- Transparent background (sits on GameBackground)

**Styling (scoped SCSS):**

- Import text-glow mixin from `assets/scss/effects/shadows`
- Use font-display, font-weight-bold from design tokens
- Apply multi-layer text-shadow for 3D embossed effect (3-5 shadow layers for prominence)
- Font size: `var(--font-size-2xl)` for consistency across pages
- Padding: `var(--spacing-lg)` top/bottom, `var(--spacing-md)` left/right
- Min-height: 80px for consistent header height
- Flexbox: align-items center, justify-content space-between
- Center title area should flex-grow to push side content to edges
- Text-align center on title element

**Color Variants:**
Each variant applies a different color with matching glow:

- `white`: color white, text-glow with white/light-blue
- `gold`: color var(--color-border-gold), text-glow with gold
- `green`: color var(--color-btn-green-light), text-glow
- `blue`: color var(--color-btn-blue-light), text-glow
- `orange`: color var(--color-btn-orange-light), text-glow

Use BEM naming: `.game-header`, `.game-header__slot`, `.game-header--{variant}`

**TypeScript:**

- Interface Props with optional variant and tag
- withDefaults for default values
- Computed class binding for variant classes
  </action>
  <verify>

1. File exists: `apps/game/components/game/GameHeader.vue`
2. Component compiles without TypeScript errors: `pnpm run typecheck`
3. Template includes three slots (left, default for title, right)
4. SCSS includes variant classes for all 5 colors
5. Multi-layer text-shadow applied (check 3+ layers in CSS)
   </verify>
   <done>
   GameHeader component created with:

- ✓ Props: variant (5 colors), tag (semantic HTML)
- ✓ Slots: left (back button), center (title), right (future use)
- ✓ Multi-layer 3D text effect using text-glow mixin
- ✓ Transparent background for use on GameBackground
- ✓ Consistent sizing (--font-size-2xl, min-height 80px)
- ✓ TypeScript types and scoped SCSS
  </done>
  </task>

<task type="auto">
  <name>Create GameScrollList component with rank badges</name>
  <files>apps/game/components/game/GameScrollList.vue</files>
  <action>
Create GameScrollList component at `apps/game/components/game/GameScrollList.vue` as a generic list wrapper:

**Component Props:**

- `showRanks?: boolean` (default: false) - Display rank badges before each row
- `items: unknown[]` - Array of items to render (generic, component doesn't care about structure)

**Template Structure:**

- Root div with class `game-scroll-list`
- overflow-y: auto for scrolling
- v-for over items with scoped slot for row content
- Each row wrapped in `.game-scroll-list__row` div
- If showRanks is true, render rank badge before slot content:
  - Positions 1-3: SVG crown icons (inline SVG, gold/silver/bronze fill colors)
  - Positions 4-6: Numbered circle badges (CSS-styled div with number)
- Scoped slot passes `{ item, index }` to parent for custom row content

**SVG Crown Icons (inline in template):**
Create three simple crown SVG icons:

- Crown 1 (gold): `fill="var(--color-border-gold)"`, 24×24 viewBox
- Crown 2 (silver): `fill="#C0C0C0"`, 24×24 viewBox
- Crown 3 (bronze): `fill="#CD7F32"`, 24×24 viewBox

Use basic crown shape: triangle base with 3-5 pointed tips at top. Keep SVG simple for crisp scaling.

**Numbered Badges (CSS):**
For ranks 4-6, render a circular div with the rank number:

- Circle: 32px width/height, border-radius 50%
- Background: semi-transparent white (rgba(255, 255, 255, 0.15))
- Border: 2px solid rgba(255, 255, 255, 0.3)
- Number: center-aligned, font-display, font-weight-bold, color white
- Font size: --font-size-md

**Styling (scoped SCSS):**

- List container: max-height 60vh, overflow-y auto
- Custom scrollbar styling (webkit-scrollbar for Chrome, scrollbar-color for Firefox)
  - Thin scrollbar (8px width)
  - Track: semi-transparent dark
  - Thumb: semi-transparent light with rounded corners
- Row spacing: gap of --spacing-md between rows
- Row layout: flexbox with gap --spacing-sm between rank badge and content
- Row background: semi-transparent panel effect (optional subtle background)
- Row padding: --spacing-md
- Row border-radius: --radius-md

**TypeScript:**

- Interface Props with showRanks (boolean) and items (generic array)
- withDefaults for showRanks: false
- Computed function to determine rank badge type (crown vs number vs none)
  </action>
  <verify>

1. File exists: `apps/game/components/game/GameScrollList.vue`
2. Component compiles without TypeScript errors: `pnpm run typecheck`
3. Template includes three inline SVG crown icons with different fill colors
4. Scoped slot passes item and index to parent
5. SCSS includes custom scrollbar styles
6. Rank badge logic checks index (0-2 = crowns, 3-5 = numbers, 6+ = none if showRanks true)
   </verify>
   <done>
   GameScrollList component created with:

- ✓ Props: showRanks (boolean), items (generic array)
- ✓ Scoped slot for custom row content
- ✓ Rank badges: gold/silver/bronze SVG crowns for 1-3, numbered circles for 4-6
- ✓ Custom scrollbar styling (webkit + Firefox)
- ✓ Responsive row layout with consistent spacing
- ✓ TypeScript types and scoped SCSS
  </done>
  </task>

</tasks>

<verification>
After both components created:

1. **TypeScript Check:**

   ```bash
   cd /home/cloudcrusader/projects/riddle-rush-mono-repo/apps/game
   pnpm run typecheck
   ```

   Expect: No errors related to GameHeader or GameScrollList

2. **Component Structure:**
   - GameHeader: 3 slots (left, default, right), 5 color variants, transparent background
   - GameScrollList: scoped slot, rank badges (crowns + numbers), custom scrollbar

3. **Pattern Consistency:**
   - Both follow GameButton/GameDisplay naming (game/Game\*.vue)
   - Both use withDefaults and computed classes
   - Both use scoped SCSS with @use imports
   - Both use design token CSS variables
     </verification>

<success_criteria>
Phase 5 Plan 1 complete when:

- [x] GameHeader component exists with props, slots, and 3D text styling
- [x] GameHeader supports 5 color variants (white, gold, green, blue, orange)
- [x] GameScrollList component exists with rank badge logic
- [x] ScrollList includes inline SVG crowns (gold/silver/bronze) and numbered badges
- [x] Both components compile without TypeScript errors
- [x] Both use scoped SCSS following established patterns
- [x] Code committed with conventional commit messages
      </success_criteria>

<output>
After completion, create `.planning/phases/05-structural-components/05-01-SUMMARY.md` following the summary template with:
- Frontmatter: phase, plan, subsystem (ui), tags, dependency graph, tech-stack
- Performance metrics (duration, timestamps)
- Files created (both component paths)
- Key decisions (text shadow layers, crown SVG approach, rank badge thresholds)
- Patterns established (full header bar pattern, scoped slot pattern for lists)
</output>
