---
phase: 17-repo-cleanup-docs-optimization
plan: 03
type: execute
wave: 2
depends_on:
  - 17-01
  - 17-02
files_modified:
  - CLAUDE.md
  - README.md
autonomous: true
requirements:
  - CLEAN-05
  - CLEAN-06

must_haves:
  truths:
    - 'CLAUDE.md reflects current pnpm version, current phase count, and accurate composable/component tables'
    - 'README.md monorepo structure diagram matches the post-cleanup directory layout'
    - 'Stale docs files are archived or removed'
    - 'pnpm run build succeeds confirming no documentation changes broke anything'
  artifacts:
    - path: 'CLAUDE.md'
      provides: 'Updated project instructions matching current codebase state'
      contains: 'pnpm'
    - path: 'README.md'
      provides: 'Updated project overview matching current repository structure'
      contains: 'monorepo'
  key_links:
    - from: 'CLAUDE.md'
      to: 'package.json'
      via: 'pnpm version reference'
      pattern: 'pnpm@'
---

<objective>
Update CLAUDE.md and README.md to reflect current codebase state after cleanup, archive stale docs, verify build integrity.

Purpose: Documentation has drifted from reality — pnpm version mismatch, phase counts wrong, composable/component tables outdated, monorepo structure diagram stale. After Plans 01-02 removed files/dirs, docs must reflect the new state.
Output: Accurate CLAUDE.md and README.md, stale docs archived.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-repo-cleanup-docs-optimization/17-RESEARCH.md
@CLAUDE.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md to match current codebase</name>
  <files>CLAUDE.md</files>
  <action>
1. Read CLAUDE.md fully.

2. Fix known inaccuracies identified in research:
   - Update `packageManager` version reference: check `cat package.json | grep packageManager` for actual version, update CLAUDE.md to match
   - Update "Phase: 13 of 13" references to reflect current phase count (phases go up to 17 now)

3. Audit the Composables table:
   - Run `ls apps/game/composables/` to get actual composable files
   - Compare with the table in CLAUDE.md
   - Add any new composables, remove any deleted ones
   - Keep descriptions concise

4. Audit the Components tables:
   - Run `ls apps/game/components/game/` and `ls apps/game/components/` (recursively) to get actual components
   - Compare with the tables in CLAUDE.md
   - Add any new components, remove any deleted ones

5. Audit the Monorepo Structure diagram:
   - Run `ls -d */` at repo root to see current top-level directories
   - Update the tree diagram to match (after Plans 01-02 cleaned up dirs)
   - Remove any directories that no longer exist

6. Check if any Essential Commands have changed (unlikely but verify `package.json` scripts section).

7. Run `pnpm run workspace:check` to verify no issues.
   </action>
   <verify>
   <automated>cd /Users/markuswagner/projects/riddle-rush-mono-repo && pnpm run workspace:check</automated>
   </verify>
   <done>CLAUDE.md accurately reflects current pnpm version, phase count, composable list, component list, and monorepo structure. No stale references remain.</done>
   </task>

<task type="auto">
  <name>Task 2: Update README.md and archive stale docs</name>
  <files>README.md</files>
  <action>
1. Read README.md fully.

2. Update monorepo structure diagram to match post-cleanup state (same as CLAUDE.md updates).

3. Check technology versions mentioned (Node, pnpm, Nuxt) against actual:
   - `cat package.json | grep -E "packageManager|engines"`
   - `cat apps/game/package.json | grep nuxt`

4. Verify quick start instructions still work with current commands.

5. Archive stale docs:
   - Check `docs/nuxt.config.ts` — if it is a stale copy, remove it
   - Check `docs/pages/` — if it contains stale pages, remove the directory
   - Move any clearly outdated docs to `docs/archive/` (use `git mv`)
   - Do NOT bulk-archive docs that might still be useful — only clearly stale ones

6. Run `pnpm run workspace:check && pnpm run build` for final verification.
   </action>
   <verify>
   <automated>cd /Users/markuswagner/projects/riddle-rush-mono-repo && pnpm run workspace:check && pnpm run build</automated>
   </verify>
   <done>README.md reflects current project state. Stale docs archived. Final workspace:check and build both pass.</done>
   </task>

</tasks>

<verification>
- `pnpm run workspace:check` passes
- `pnpm run build` passes
- CLAUDE.md pnpm version matches package.json packageManager field
- CLAUDE.md composable table matches actual composables directory
- README.md structure diagram matches actual directory layout
</verification>

<success_criteria>

- CLAUDE.md is accurate for pnpm version, phase count, composables, components, and directory structure
- README.md is accurate for tech versions, directory structure, and quick start
- Stale docs are archived
- Both workspace:check and build pass
  </success_criteria>

<output>
After completion, create `.planning/phases/17-repo-cleanup-docs-optimization/17-03-SUMMARY.md`
</output>
