---
phase: 17-repo-cleanup-docs-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02

must_haves:
  truths:
    - 'Orphaned root files (config.json, junit.xml, package-lock.json, debug-500-main-menu.png, terraform.tfstate, uv.lock, pyproject-old.toml, seed.spec.ts, nuxt.config.terraform.ts) are removed from git tracking'
    - 'AI tool config directories (~20 dirs) are removed from git tracking and added to .gitignore'
    - 'Stale root-level files (.eslintignore, .gitignore.monorepo, stagewise.json, opencode.json, flags.json, mcp-config.yaml) are removed from git tracking'
    - 'AI tool dot-files (.cursorrules, .vibemodes, .roomodes, .goosehints, .aiignore, .brew-packages, .aider.*) are removed from git tracking and gitignored'
    - 'pnpm run workspace:check still passes after all removals'
  artifacts:
    - path: '.gitignore'
      provides: 'Updated gitignore with AI tool dirs, orphaned files, and stale configs'
      contains: '.ai/'
  key_links:
    - from: '.gitignore'
      to: 'git tracking'
      via: 'git rm --cached for all removed files'
      pattern: 'git rm --cached'
---

<objective>
Remove orphaned root files and AI tool config directories from git tracking, update .gitignore to prevent re-tracking.

Purpose: The repo has ~450+ tracked files across 20+ AI tool config directories plus ~20 orphaned root files that bloat the repository and confuse contributors.
Output: Clean git tracking with comprehensive .gitignore entries.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-repo-cleanup-docs-optimization/17-RESEARCH.md
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove orphaned root files and stale configs from git tracking</name>
  <files>.gitignore</files>
  <action>
1. Run `git rm --cached` on these orphaned root files (keep on disk if they exist):
   - config.json, junit.xml, package-lock.json, debug-500-main-menu.png
   - terraform.tfstate, uv.lock, pyproject-old.toml, seed.spec.ts
   - nuxt.config.terraform.ts, trigger.config.ts, opencode.json
   - mcp-config.yaml, flags.json, stagewise.json
   - .eslintignore, .gitignore.monorepo
   - .cursorrules, .vibemodes, .roomodes, .goosehints, .aiignore, .brew-packages
   - .aider.chat.history.md, .aider.input.history

2. Move these files to docs/archive/ (git mv if tracked):
   - CI_CD_IMPLEMENTATION_SUMMARY.md
   - COMPLETION-SUMMARY.md
   - DEPLOYMENT-CHECKLIST.md

3. Add all removed files to .gitignore under a new "# Orphaned/stale files" section.

4. IMPORTANT: Use `git rm --cached` (not `git rm`) so local copies are preserved. If a file is not tracked, skip it silently (use `2>/dev/null` or check first).

5. Run `pnpm run workspace:check` to verify nothing broke.
   </action>
   <verify>
   <automated>cd /Users/markuswagner/projects/riddle-rush-mono-repo && pnpm run workspace:check</automated>
   </verify>
   <done>All orphaned root files removed from git tracking, historical docs moved to docs/archive/, .gitignore updated. workspace:check passes.</done>
   </task>

<task type="auto">
  <name>Task 2: Remove AI tool config directories from git tracking and update .gitignore</name>
  <files>.gitignore</files>
  <action>
1. Run `git rm -r --cached` on these AI tool directories (KEEP .agent/ and .agents/ which are used by project):
   - .ai/, .augment/, .cline/, .codebuddy/, .codex/, .continue/
   - .cursor/, .grok/, .junie/, .kilocode/, .kiro/
   - .mcpjam/, .old.gemeni/, .opencode/, .openhands/
   - .pi/, .qoder/, .qwen/, .tdad/, .trae/
   - .vibe/, .windsurf/, .zencoder/

2. Also remove these directories from tracking if they exist:
   - .aider.tags.cache.v4/

3. Add .gitignore entries for all removed dirs plus untracked ones mentioned in research:
   - .crush/, .superdesign/ (already untracked, just gitignore)

4. Add a clearly labeled section in .gitignore: "# AI tool configs (keep .agent/ and .agents/ which are project-used)"

5. Use `git rm -r --cached` (not `git rm -r`) to preserve local copies.
   Skip any directory that is not currently tracked (check with `git ls-files <dir>` first).

6. Run `pnpm run workspace:check` to verify nothing broke.
   </action>
   <verify>
   <automated>cd /Users/markuswagner/projects/riddle-rush-mono-repo && pnpm run workspace:check</automated>
   </verify>
   <done>All non-essential AI tool config directories removed from git tracking. .gitignore has entries for all 20+ AI tool dirs. Only .agent/ and .agents/ remain tracked. workspace:check passes.</done>
   </task>

</tasks>

<verification>
- `git ls-files | wc -l` shows significant reduction from baseline (expect ~450+ fewer files)
- `pnpm run workspace:check` passes
- `git status` shows deleted files staged, .gitignore modified
- .agent/ and .agents/ directories are still tracked
</verification>

<success_criteria>

- Orphaned root files no longer appear in `git ls-files`
- AI tool config directories (except .agent/, .agents/) no longer in `git ls-files`
- .gitignore comprehensively covers all removed items
- workspace:check passes clean
  </success_criteria>

<output>
After completion, create `.planning/phases/17-repo-cleanup-docs-optimization/17-01-SUMMARY.md`
</output>
