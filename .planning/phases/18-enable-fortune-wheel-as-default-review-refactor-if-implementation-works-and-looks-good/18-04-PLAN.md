---
phase: 18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good
plan: 04
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - apps/game/composables/useFeatureFlags.ts
  - apps/game/nuxt.config.ts
  - apps/game/tests/unit/use-feature-flags.spec.ts
autonomous: true

must_haves:
  truths:
    - "Feature flag precedence is deterministic and documented in code"
    - "GitLab client remains authoritative when configured"
    - "Fortune wheel fallback default remains enabled when GitLab is unavailable"
    - "Feature flag behavior is covered by unit tests"
  artifacts:
    - path: "apps/game/composables/useFeatureFlags.ts"
      provides: "Single feature-flag resolution contract with explicit precedence"
    - path: "apps/game/nuxt.config.ts"
      provides: "Runtime config defaults aligned with the feature-flag contract"
    - path: "apps/game/tests/unit/use-feature-flags.spec.ts"
      provides: "Regression tests for precedence and fallback semantics"
  key_links:
    - from: "apps/game/composables/useFeatureFlags.ts"
      to: "apps/game/nuxt.config.ts"
      via: "runtimeConfig public feature flags"
      pattern: "featureAnswerInput"
    - from: "apps/game/composables/useFeatureFlags.ts"
      to: "apps/game/stores/settingsStore.ts"
      via: "local fallback state"
      pattern: "settingsStore\.getState"
---

<objective>
Finalize and codify the feature-flag solution used post-Zustand migration so precedence and defaults are explicit, test-backed, and stable.

Purpose: The user explicitly requested a feature-flag solution decision for `useFeatureFlags.ts` and `nuxt.config.ts`. This plan converts the existing implicit behavior into a hard contract with regression coverage while honoring locked decisions: GitLab-first precedence and fortune-wheel fallback default enabled.

Output: Updated feature-flag contract implementation + unit tests proving precedence/default behavior.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good/18-RESEARCH.md

@apps/game/composables/useFeatureFlags.ts
@apps/game/nuxt.config.ts
@apps/game/tests/unit/use-feature-flags.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement explicit feature-flag resolution contract</name>
  <files>apps/game/composables/useFeatureFlags.ts, apps/game/nuxt.config.ts</files>
  <action>
Refactor `useFeatureFlags.ts` to make resolution order explicit and consistent across all three flags (`fortune-wheel`, `answer-input`, `websocket`):

1. Preserve user-locked behavior exactly:
   - GitLab client authoritative when available.
   - Fortune wheel fallback default enabled (`true`) when GitLab is unavailable.
2. Extract a shared resolver helper inside the composable (or module) so each flag does not hand-roll precedence logic.
3. Keep runtime-config override semantics only where intentionally supported (currently `featureAnswerInput`), and document why in code comments.
4. Update `nuxt.config.ts` comments/defaults so they align with the implemented contract (avoid contradictory wording between config and composable).

Do NOT introduce a new external feature-flag library. Use the existing Unleash + settings fallback architecture.
</action>
<verify>pnpm --filter @riddle-rush/game run typecheck</verify>
<done>Feature-flag precedence is implemented in one explicit contract path, with GitLab-first and fortune-wheel fallback default true preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Add precedence regression tests for feature flags</name>
  <files>apps/game/tests/unit/use-feature-flags.spec.ts</files>
  <action>
Expand unit tests to cover concrete precedence outcomes:

- GitLab configured + returns false for `fortune-wheel` -> effective false.
- GitLab missing + local setting true/false -> local value used.
- GitLab missing + no local mutation -> fortune wheel defaults to true.
- `answer-input` runtime config forced false -> effective false even if store/GitLab true.
- `websocket` fallback behavior remains unchanged.

Use existing test conventions and mocks in the file. Keep tests deterministic and avoid timing-based assertions.
</action>
<verify>pnpm --filter @riddle-rush/game run test:unit -- tests/unit/use-feature-flags.spec.ts</verify>
<done>Unit tests cover precedence and fallback defaults for all managed flags, and pass reliably.</done>
</task>

<task type="auto">
  <name>Task 3: Validate contract consistency in workspace checks</name>
  <files>apps/game/composables/useFeatureFlags.ts, apps/game/nuxt.config.ts, apps/game/tests/unit/use-feature-flags.spec.ts</files>
  <action>
Run full quality checks and ensure there are no regressions from the feature-flag contract update.

If checks fail, fix issues within this plan scope (typing, linting, test mocks) and re-run.
</action>
<verify>pnpm run workspace:check && pnpm run test:unit</verify>
<done>workspace:check passes and unit tests pass with the new feature-flag contract in place.</done>
</task>

</tasks>

<verification>
1. `useFeatureFlags.ts` contains one explicit precedence contract path.
2. Fortune wheel fallback remains enabled by default when GitLab is unavailable.
3. `use-feature-flags.spec.ts` includes precedence regression cases and passes.
4. `pnpm run workspace:check` passes.
</verification>

<success_criteria>

- Feature-flag selection behavior is deterministic and test-backed.
- Locked decision (GitLab-first + fortune-wheel fallback default true) is preserved in code and tests.
- No new warnings/errors from typecheck or lint.
  </success_criteria>

<output>
After completion, create `.planning/phases/18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good/18-04-SUMMARY.md`
</output>
