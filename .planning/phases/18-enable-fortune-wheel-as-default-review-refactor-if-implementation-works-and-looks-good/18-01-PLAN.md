---
phase: 18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/game/composables/useFeatureFlags.ts
  - apps/game/stores/settings.ts
  - apps/game/tests/unit/use-feature-flags.spec.ts
  - apps/game/tests/unit/settings-store.spec.ts
autonomous: true
user_setup:
  - service: gitlab-feature-flags
    why: 'GitLab/Unleash is authoritative for fortune-wheel when configured'
    env_vars:
      - name: NUXT_PUBLIC_GITLAB_FEATURE_FLAGS_URL
        source: 'GitLab project/instance feature flag proxy endpoint config'
      - name: NUXT_PUBLIC_GITLAB_FEATURE_FLAGS_TOKEN
        source: 'GitLab feature flag client token'
    dashboard_config:
      - task: 'Set fortune-wheel flag enabled by default for active environments (development/staging/production as used)'
        location: 'GitLab Feature Flags UI (Unleash-backed project flags)'

must_haves:
  truths:
    - 'Fortune wheel is enabled by default when no remote GitLab flag client is configured'
    - 'When GitLab flags are configured, remote fortune-wheel value remains authoritative'
    - 'Round start still supports both wheel-enabled and wheel-disabled branches without breaking game start'
  artifacts:
    - path: 'apps/game/stores/settings.ts'
      provides: 'Local fallback default settings for fortuneWheelEnabled'
      contains: 'fortuneWheelEnabled: true'
    - path: 'apps/game/composables/useFeatureFlags.ts'
      provides: 'Feature-flag precedence resolution for fortune-wheel'
      contains: "isEnabled('fortune-wheel', true)"
    - path: 'apps/game/tests/unit/use-feature-flags.spec.ts'
      provides: 'Unit coverage for precedence and default behavior'
      contains: 'isFortuneWheelEnabled'
  key_links:
    - from: 'apps/game/composables/useFeatureFlags.ts'
      to: 'apps/game/stores/settings.ts'
      via: 'fallback read of settingsStore.fortuneWheelEnabled when no GitLab client'
      pattern: 'settingsStore\.fortuneWheelEnabled'
    - from: 'apps/game/composables/useFeatureFlags.ts'
      to: 'GitLab Unleash client'
      via: "isEnabled('fortune-wheel', true)"
      pattern: "isEnabled\('fortune-wheel',\s*true\)"
---

<objective>
Set fortune-wheel to default-on at the real local fallback layer while preserving GitLab remote authority.

Purpose: Ensure default behavior is correct in both local/no-flag and remote-flag environments without introducing a parallel mode system.
Output: Updated settings/composable defaults and unit tests proving precedence behavior.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good/18-RESEARCH.md
@apps/game/composables/useFeatureFlags.ts
@apps/game/stores/settings.ts
@apps/game/tests/unit/use-feature-flags.spec.ts
@apps/game/tests/unit/settings-store.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable fortune-wheel default in local fallback and keep precedence explicit</name>
  <files>apps/game/composables/useFeatureFlags.ts, apps/game/stores/settings.ts</files>
  <action>Update `DEFAULT_SETTINGS.fortuneWheelEnabled` to `true` in `settings.ts`. In `useFeatureFlags.ts`, keep GitLab-first precedence but change the fortune-wheel default fallback parameter from false to true (`isEnabled('fortune-wheel', true)`) so behavior is explicit and self-documenting. Do not introduce any new mode state or alternate config object; preserve existing control planes exactly.</action>
  <verify>pnpm --filter @riddle-rush/game typecheck</verify>
  <done>`settings` fallback default is true, and `useFeatureFlags` still uses GitLab as authoritative source when client exists.</done>
</task>

<task type="auto">
  <name>Task 2: Align unit tests with new default and precedence contract</name>
  <files>apps/game/tests/unit/use-feature-flags.spec.ts, apps/game/tests/unit/settings-store.spec.ts</files>
  <action>Update store/unit expectations from "fortune wheel disabled by default" to enabled-by-default where appropriate, and add/adjust assertions to verify: (1) no-client fallback can resolve true from local settings, (2) GitLab value still overrides local store state, and (3) error/default branches for fortune-wheel now assume true fallback where the composable default applies. Keep test intent focused on precedence and regression safety.</action>
  <verify>pnpm --filter @riddle-rush/game test:unit -- --runInBand tests/unit/settings-store.spec.ts tests/unit/use-feature-flags.spec.ts</verify>
  <done>Unit tests pass and explicitly encode the new default-on local behavior while preserving GitLab authority.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @riddle-rush/game typecheck`
- `pnpm --filter @riddle-rush/game test:unit -- --runInBand tests/unit/settings-store.spec.ts tests/unit/use-feature-flags.spec.ts`
</verification>

<success_criteria>

- Local fallback path defaults fortune-wheel to enabled.
- Remote GitLab path remains authoritative and unaffected.
- Unit tests document and protect both behaviors.
  </success_criteria>

<output>
After completion, create `.planning/phases/18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good/18-01-SUMMARY.md`
</output>
