---
phase: 18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/game/stores/index.ts
  - apps/game/stores/hooks/index.ts
  - .planning/phases/18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good/18-01-warning-inventory.md
autonomous: true
requirements: []

must_haves:
  truths:
    - 'Zero duplicated import warnings in typecheck output'
    - 'All build warnings are either fixed or intentionally suppressed with explanation'
    - 'Fortune wheel is the default round-start mode without feature flag'
    - 'All unit tests pass after barrel file removal'
    - 'workspace:check passes with no errors'
  artifacts:
    - path: 'apps/game/stores/index.ts'
      provides: 'Barrel file removed or emptied to eliminate auto-import collisions'
    - path: 'apps/game/stores/hooks/index.ts'
      provides: 'Barrel file removed or emptied to eliminate auto-import collisions'
    - path: '.planning/phases/18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good/18-01-warning-inventory.md'
      provides: 'Warning inventory with per-warning disposition (fixed or intentionally suppressed) and rationale'
  key_links:
    - from: 'apps/game/plugins/zustand.ts'
      to: 'apps/game/stores/gameStore.ts'
      via: 'Direct import (not barrel)'
      pattern: "from '~/stores/gameStore'"
    - from: 'apps/game/plugins/i18n.client.ts'
      to: 'apps/game/stores/settingsStore.ts'
      via: 'Direct import (not barrel)'
      pattern: "from '~/stores/settingsStore'"
---

<objective>
Remove store barrel files that cause 19 duplicated import warnings during typecheck, and verify fortune wheel is already enabled as default.

Purpose: The barrel re-export files (`stores/index.ts` and `stores/hooks/index.ts`) collide with Nuxt's auto-import directory scanning (`imports.dirs: ['stores', 'stores/hooks']`), producing 19 duplicated import warnings. Removing these barrel files eliminates all warnings since no consumer uses the barrel import path -- all explicit imports already target specific store files.

Output: Zero duplicated import warnings, warning-closure inventory with rationale, clean typecheck, confirmed fortune wheel default.
</objective>

<execution_context>
@/Users/markuswagner/projects/riddle-rush-mono-repo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markuswagner/projects/riddle-rush-mono-repo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good/18-RESEARCH.md

<interfaces>
<!-- Key imports verified -- NO consumer uses barrel paths -->
<!-- All explicit imports use direct source file paths: -->

From apps/game/plugins/zustand.ts:

```typescript
import { gameStore } from '~/stores/gameStore'
import { settingsStore } from '~/stores/settingsStore'
import { loadingStore } from '~/stores/loadingStore'
import { migrateFromPinia } from '~/stores/migrate'
```

From apps/game/plugins/i18n.client.ts:

```typescript
import { settingsStore } from '~/stores/settingsStore'
```

From apps/game/composables/useFeatureFlags.ts:

```typescript
import { settingsStore } from '~/stores/settingsStore'
```

From apps/game/nuxt.config.ts (auto-import dirs config):

```typescript
imports: {
  dirs: ['stores', 'stores/hooks', 'composables'],
},
```

Fortune wheel default (already set):

```typescript
// stores/settingsStore.ts line 31
fortuneWheelEnabled: true

// composables/useFeatureFlags.ts line 114
isEnabled('fortune-wheel', true)
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove store barrel files causing duplicated import warnings</name>
  <files>apps/game/stores/index.ts, apps/game/stores/hooks/index.ts</files>
  <action>
Delete both barrel files that cause Nuxt auto-import collisions:

1. **Delete `apps/game/stores/index.ts`** -- This file re-exports `gameStore`, `settingsStore`, `loadingStore`, `migrateFromPinia`, and all hooks from `./hooks`. Every one of these exports is already auto-imported by Nuxt from the original source files in the scanned `stores/` directory. No file in the codebase imports from `~/stores` (the barrel path) -- all use direct paths like `~/stores/gameStore`.

2. **Delete `apps/game/stores/hooks/index.ts`** -- This file re-exports `useGameSession`, `useCategories`, `usePlayerActions`, `useInstallPrompt`, `useSettings`, `useLoading`, and the `GameSettings` type from individual hook files. Nuxt auto-imports all of these from the scanned `stores/hooks/` directory. The composable `useGameActions.ts` imports from `../stores/hooks/useGameSession` (direct path), not the barrel.

3. **Verify no breaking imports:** Before deleting, run a grep to confirm no file imports from `'~/stores'` or `'~/stores/hooks'` (without a subsequent `/specificFile`). The research confirmed zero such imports exist, but verify at execution time.

4. **Run `pnpm run workspace:check`** to confirm zero duplicated import warnings remain and no TypeScript errors were introduced.

5. **Run `pnpm run test`** to confirm all 734 unit tests still pass.
   </action>
   <verify>
   <automated>bash -lc "set -euo pipefail; cd /Users/markuswagner/projects/riddle-rush-mono-repo; pnpm run workspace:check; pnpm run test"</automated>
   </verify>
   <done>Both barrel files deleted. `pnpm run workspace:check` produces zero "duplicated" warnings. `pnpm run typecheck` passes cleanly. All unit tests pass.</done>
   </task>

<task type="auto">
  <name>Task 2: Verify fortune wheel is default and document cleanup</name>
  <files>apps/game/stores/settingsStore.ts, apps/game/composables/useFeatureFlags.ts</files>
  <action>
Verify (read-only, no code changes expected) that the fortune wheel is already the default round-start experience:

1. **Confirm `settingsStore.ts`** has `fortuneWheelEnabled: true` in the default state. This was set during the earlier Phase 18 partial execution.

2. **Confirm `useFeatureFlags.ts`** has `isEnabled('fortune-wheel', true)` as the fallback default when no GitLab client is configured.

3. **Run a repo-/phase-wide Pinia residue audit with explicit allowlist exceptions.** Scan the full repository (not just app source) for `defineStore`, `storeToRefs`, `setActivePinia`, `createPinia`, `@pinia/nuxt`, and direct `pinia` imports. Fail on any non-allowlisted match.

   **Allowlist exceptions (intentional historical/migration references):**
   - `apps/game/stores/migrate.ts` (migration bridge)
   - `apps/game/tests/unit/stores/migrate.spec.ts` (migration test coverage)
   - `.planning/**` (historical phase docs/summaries mentioning Pinia by design)

   Any additional match outside this allowlist must be removed or replaced with Zustand patterns before plan completion.

4. If any of the above checks fail, fix them. If all pass, document the verification in the summary.
   </action>
   <verify>
   <automated>bash -lc "set -euo pipefail; cd /Users/markuswagner/projects/riddle-rush-mono-repo; rg -n \"fortuneWheelEnabled:\\s*true\" apps/game/stores/settingsStore.ts; rg -n \"isEnabled\\('fortune-wheel',\\s*true\\)\" apps/game/composables/useFeatureFlags.ts; python - <<'PY'
   import pathlib
   import re
   import sys

root = pathlib.Path('.')
pattern = re.compile(r\"defineStore|storeToRefs|setActivePinia|createPinia|@pinia/nuxt|from ['\\\"]pinia['\\\"]|from ['\\\"]@pinia/nuxt['\\\"]\")
exclude_dirs = {'.git', 'node_modules', '.turbo', '.nuxt', '.output', 'dist', 'coverage'}
allow_prefixes = [
pathlib.Path('apps/game/stores/migrate.ts'),
pathlib.Path('apps/game/tests/unit/stores/migrate.spec.ts'),
pathlib.Path('.planning'),
]

violations = []
for p in root.rglob('\*'):
if not p.is_file():
continue
if any(part in exclude_dirs for part in p.parts):
continue
rel = p.relative_to(root)
try:
text = p.read_text(encoding='utf-8', errors='ignore')
except Exception:
continue
if not pattern.search(text):
continue
allowed = any(str(rel).startswith(str(prefix)) for prefix in allow_prefixes)
if not allowed:
violations.append(str(rel))

if violations:
print('Unexpected Pinia references outside allowlist:')
for v in sorted(set(violations)):
print(v)
sys.exit(1)
PY"</automated>
</verify>
<done>Fortune wheel confirmed as default (settingsStore default = true, feature flag fallback = true). Repo-/phase-wide Pinia scan passes with only allowlisted migration/historical references. Post-Zustand cleanup verified complete.</done>
</task>

<task type="auto">
  <name>Task 3: Create warning inventory and close every warning with rationale</name>
  <files>.planning/phases/18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good/18-01-warning-inventory.md</files>
  <action>
Create `18-01-warning-inventory.md` as a dedicated warning-closure artifact for this phase requirement:

1. Run warning-producing quality/build commands relevant to this phase scope:
   - `pnpm run workspace:check`
   - `pnpm --filter @riddle-rush/game run build`
2. Inventory every warning in the file with:
   - warning text/category
   - originating command + file/location
   - disposition: `fixed` or `intentional suppression`
   - rationale for disposition
   - proof reference (commit/file change or suppression location)
3. For any intentional suppression, include explicit why-safe rationale and location (for example: specific inline ESLint/TypeScript suppression with narrow scope).
4. End the inventory with a closure summary that states there are no untracked warnings left for this phase.

This task is mandatory to satisfy ROADMAP success criterion #3: all build warnings addressed or intentionally suppressed with explanation.
</action>
<verify>
<automated>bash -lc "set -euo pipefail; cd /Users/markuswagner/projects/riddle-rush-mono-repo; pnpm run workspace:check; pnpm --filter @riddle-rush/game run build; test -f .planning/phases/18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good/18-01-warning-inventory.md"</automated>
</verify>
<done>`18-01-warning-inventory.md` exists and documents each warning with fixed/suppressed disposition and rationale. No warning remains undocumented.</done>
</task>

</tasks>

<verification>
1. `pnpm run workspace:check` passes with zero errors and zero duplicated import warnings
2. `pnpm run test` -- all unit tests pass
3. Fortune wheel defaults verified in settingsStore.ts and useFeatureFlags.ts
4. Repo-/phase-wide Pinia scan passes with explicit allowlist exceptions only
5. `18-01-warning-inventory.md` exists and documents warning closure (fixed or intentionally suppressed with explanation)
</verification>

<success_criteria>

- Zero duplicated import warnings in typecheck output (was 19)
- All build warnings are either fixed or intentionally suppressed with explanation in `18-01-warning-inventory.md`
- All 734+ unit tests pass
- workspace:check passes cleanly
- Fortune wheel is confirmed as the default round-start mode
- No non-allowlisted Pinia residues remain across repo/phase scope
  </success_criteria>

<output>
After completion, create `.planning/phases/18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good/18-01-SUMMARY.md`
</output>
