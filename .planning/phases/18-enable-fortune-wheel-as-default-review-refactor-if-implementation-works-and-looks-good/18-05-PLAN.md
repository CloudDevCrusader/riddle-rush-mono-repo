---
phase: 18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good
plan: 05
type: execute
wave: 3
depends_on: ['18-03', '18-04']
files_modified:
  - apps/game/stores/gameStore.ts
  - apps/game/composables/useGameState.ts
  - apps/game/composables/useGameActions.ts
  - apps/game/composables/usePlayerManager.ts
  - apps/game/pages/game/[[gameId]].vue
  - apps/game/pages/results/[[gameId]].vue
  - apps/game/pages/round-start.vue
  - apps/game/components/PlayerLeaderboard.vue
  - .planning/phases/18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good/18-05-state-flow.md
autonomous: true

must_haves:
  truths:
    - 'Game mode and round flow are resolved from one source of truth, not ad-hoc page logic'
    - 'Multi-round scoring stays consistent across game -> results -> round-start transitions'
    - 'Post-round actions (next round/new game/leaderboard) follow a documented state flow'
    - 'Code-review hygiene issues in touched files are cleaned (logger/lifecycle/i18n-head consistency)'
  artifacts:
    - path: 'apps/game/stores/gameStore.ts'
      provides: 'Single authoritative game flow state/actions'
    - path: 'apps/game/composables/useGameState.ts'
      provides: 'Consumer-facing state projection without conflicting logic duplication'
    - path: 'apps/game/composables/useGameActions.ts'
      provides: 'Action entry points aligned to the same flow contract'
    - path: 'apps/game/composables/usePlayerManager.ts'
      provides: 'Deterministic turn/scoring helpers used by store'
    - path: 'apps/game/pages/game/[[gameId]].vue'
      provides: 'Game-page flow usage aligned with unified store contract'
    - path: 'apps/game/pages/results/[[gameId]].vue'
      provides: 'Results/multi-round flow consumption aligned with unified store contract'
    - path: 'apps/game/pages/round-start.vue'
      provides: 'Round start behavior using same flow contract'
    - path: 'apps/game/components/PlayerLeaderboard.vue'
      provides: 'Leaderboard display synced to unified game state'
    - path: '.planning/phases/18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good/18-05-state-flow.md'
      provides: 'Documented state flow chart for game mode and round transitions'
  key_links:
    - from: 'apps/game/pages/game/[[gameId]].vue'
      to: 'apps/game/stores/gameStore.ts'
      via: 'submit/next transitions'
      pattern: 'submitPlayerAnswer|allPlayersSubmitted|goToResults'
    - from: 'apps/game/pages/results/[[gameId]].vue'
      to: 'apps/game/stores/gameStore.ts'
      via: 'assign score + complete round + next action'
      pattern: 'assignPlayerScore|completeRound|startNextRound|completeGame'
    - from: 'apps/game/pages/round-start.vue'
      to: 'apps/game/stores/gameStore.ts'
      via: 'session bootstrap and next-round entry'
      pattern: 'setupPlayers|startNextRound|resetPlayerSubmissions'
---

<objective>
Refactor post-Zustand game flow to a single source of truth, validate/fix full multi-round workflow, and apply targeted code-review hygiene cleanup in the same flow-critical files.

Purpose: The user explicitly added three scope items that are tightly coupled: full multi-round workflow fixes, single-source-of-truth game mode refactor, and code-review findings bundle. This plan consolidates them into one executable vertical slice to avoid partial fixes and logic drift.

Output: Unified store-driven flow, corrected multi-round behavior, documented state flow chart, and targeted hygiene improvements.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good/18-RESEARCH.md
@.planning/phases/19-move-from-pinia-to-zustand/19-02-SUMMARY.md
@.planning/phases/19-move-from-pinia-to-zustand/19-03-SUMMARY.md

@apps/game/stores/gameStore.ts
@apps/game/composables/useGameState.ts
@apps/game/composables/useGameActions.ts
@apps/game/composables/usePlayerManager.ts
@apps/game/pages/game/[[gameId]].vue
@apps/game/pages/results/[[gameId]].vue
@apps/game/pages/round-start.vue
@apps/game/components/PlayerLeaderboard.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate game-mode/round flow into one store contract</name>
  <files>apps/game/stores/gameStore.ts, apps/game/composables/useGameState.ts, apps/game/composables/useGameActions.ts, apps/game/composables/usePlayerManager.ts</files>
  <action>
Refactor flow-critical logic so game mode and round progression are derived from one canonical source (store), not recomputed inconsistently in pages:

1. Define explicit flow states/transitions in store-facing API (for example: setup, in-round, round-complete, decision, completed) using existing structures rather than adding a new state library.
2. Ensure `submitPlayerAnswer`, `assignPlayerScore`, `completeRound`, `startNextRound`, and `completeGame` produce consistent state transitions for multi-round play.
3. Keep MIGRATE-03/04/05/07 aligned: consumers continue using focused Zustand hooks and no Pinia reintroduction.
4. Update `useGameState`/`useGameActions` to expose the unified flow contract without leaking ad-hoc page workarounds.
5. Adjust `usePlayerManager` only where required for deterministic turn and score progression.

Avoid broad architectural rewrites; keep this as targeted consolidation inside existing Zustand patterns.
</action>
<verify>pnpm --filter @riddle-rush/game run test:unit -- tests/unit/game-store.spec.ts tests/unit/composables/useGameState.spec.ts tests/unit/composables/useGameActions.spec.ts</verify>
<done>Store/composables expose one coherent flow contract; core multi-round actions transition state deterministically and related unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix and verify full multi-round gameplay workflow in pages/components</name>
  <files>apps/game/pages/game/[[gameId]].vue, apps/game/pages/results/[[gameId]].vue, apps/game/pages/round-start.vue, apps/game/components/PlayerLeaderboard.vue</files>
  <action>
Apply the unified flow contract to UI consumers and fix full workflow edge cases:

1. Validate/fix transitions from game -> results -> decision modal -> next round/new game/leaderboard.
2. Ensure multi-round scoring is stable (no score loss/double apply/incorrect projected rank behavior).
3. Ensure answer-input feature-flag behavior does not break round progression.
4. Ensure post-round modal three-choice behavior remains intact and wired to unified store transitions.
5. Update/extend E2E coverage where needed (especially full game workflow / multi-round scoring paths) to prove flow reliability.

Use existing data-testid and helper patterns introduced in plans 18-02/18-03.
</action>
<verify>pnpm --filter @riddle-rush/game run test:e2e -- --grep "full game workflow" && pnpm --filter @riddle-rush/game run test:e2e -- --grep "multi round" && pnpm --filter @riddle-rush/game run test:e2e -- --grep "scoring" && pnpm --filter @riddle-rush/game run test:e2e -- --grep "results"</verify>
<done>End-to-end multi-round flow works through all post-round choices with consistent scoring and stable navigation.</done>
</task>

<task type="auto">
  <name>Task 3: Apply targeted code-review hygiene bundle + publish state flow chart</name>
  <files>apps/game/pages/game/[[gameId]].vue, apps/game/pages/results/[[gameId]].vue, apps/game/pages/round-start.vue, apps/game/components/PlayerLeaderboard.vue, .planning/phases/18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good/18-05-state-flow.md</files>
  <action>
Implement the requested code-review findings bundle, limited to touched flow files:

- Ensure logger usage is consistent (no ad-hoc console logging).
- Ensure lifecycle cleanup is complete (event listeners/timers/watcher side-effects).
- Replace hardcoded user-facing copy with i18n keys where present in touched files.
- Ensure `useHead` values are i18n-backed and consistent with page state.

Then create `18-05-state-flow.md` documenting the final state-flow chart:

- states, transitions, triggering actions, and owning files
- explicit fallback behavior for fortune wheel and answer-input flag
- invariants (e.g., score assignment idempotency and round completion conditions)
  </action>
  <verify>pnpm run workspace:check && pnpm run test:unit && pnpm --filter @riddle-rush/game run test:e2e -- --grep "full game workflow" && pnpm --filter @riddle-rush/game run test:e2e -- --grep "multi round" && pnpm --filter @riddle-rush/game run test:e2e -- --grep "round-start" && pnpm --filter @riddle-rush/game run test:e2e -- --grep "leaderboard"</verify>
  <done>Flow files pass quality checks, hygiene items are resolved in touched scope, and a concrete state-flow chart exists at 18-05-state-flow.md.</done>
  </task>

</tasks>

<verification>
1. Store/composable contract is the only source for game mode/round progression.
2. Multi-round E2E flow passes through results and all decision paths.
3. Scoring remains consistent across rounds and leaderboard.
4. Flow file hygiene pass completed (logger/lifecycle/i18n/useHead).
5. State flow chart file exists and reflects implemented behavior.
</verification>

<success_criteria>

- Single source of truth for game mode and round transitions is in effect.
- Full game multi-round workflow is reliable and test-backed.
- Code-review findings in flow-critical files are closed without broad unrelated churn.
- `pnpm run workspace:check` and unit tests pass.
  </success_criteria>

<output>
After completion, create `.planning/phases/18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good/18-05-SUMMARY.md`
</output>
