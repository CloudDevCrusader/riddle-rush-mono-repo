---
phase: 18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good
plan: 03
type: execute
wave: 2
depends_on: ['18-02']
files_modified:
  - apps/game/tests/e2e/game-complete-flow.spec.ts
  - apps/game/tests/e2e/full-game-workflow.spec.ts
  - apps/game/tests/e2e/results.spec.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - 'All E2E spec files with game flow use shared helpers from game-flow.ts'
    - 'No local duplicated helper functions remain across the E2E test suite'
    - 'All game-flow E2E specs use data-testid selectors exclusively'
    - 'All E2E specs handle answer input as feature-flag-aware'
    - 'E2E test suite compiles and workspace:check passes'
  artifacts:
    - path: 'apps/game/tests/e2e/game-complete-flow.spec.ts'
      provides: 'Refactored to use shared helpers, no local duplicates'
    - path: 'apps/game/tests/e2e/full-game-workflow.spec.ts'
      provides: 'Refactored to use shared helpers, no local duplicates'
    - path: 'apps/game/tests/e2e/results.spec.ts'
      provides: 'Refactored to use data-testid selectors and shared helpers where applicable'
  key_links:
    - from: 'apps/game/tests/e2e/game-complete-flow.spec.ts'
      to: 'apps/game/tests/e2e/helpers/game-flow.ts'
      via: 'import'
      pattern: "from './helpers/game-flow'"
    - from: 'apps/game/tests/e2e/full-game-workflow.spec.ts'
      to: 'apps/game/tests/e2e/helpers/game-flow.ts'
      via: 'import'
      pattern: "from './helpers/game-flow'"
---

<objective>
Refactor the remaining E2E spec files (game-complete-flow, full-game-workflow, results) to use shared game-flow helpers, completing the E2E test suite rework.

Purpose: After Plan 02 creates the shared helpers and refactors the 4 HIGH priority specs, these 3 remaining spec files still contain locally duplicated helper functions and fragile CSS class selectors. This plan completes the consolidation so the entire E2E suite uses consistent, feature-flag-aware, data-testid-based helpers.

Output: All game-flow E2E specs use shared helpers. Zero locally duplicated game-flow helpers across the entire test suite.
</objective>

<execution_context>
@/Users/markuswagner/projects/riddle-rush-mono-repo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markuswagner/projects/riddle-rush-mono-repo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good/18-RESEARCH.md
@.planning/phases/18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good/18-02-SUMMARY.md

<interfaces>
<!-- Shared helpers created in Plan 02 (game-flow.ts) -->
```typescript
export async function hideDevtools(page: Page): Promise<void>
export async function submitPlayerAnswers(page: Page, count: number, answers?: string[]): Promise<void>
export async function navigateToResults(page: Page): Promise<void>
export async function assignScores(page: Page, scores: number[]): Promise<void>
export async function confirmScoresAndWaitForModal(page: Page): Promise<void>
export async function goToNextRound(page: Page): Promise<void>
export async function finishGame(page: Page): Promise<void>
export async function setupMultiplayerGame(page: Page, playerNames: string[]): Promise<void>
export async function startGameWithDefaults(page: Page): Promise<void>
export async function startGameAndGoToResults(page: Page, playerCount?: number): Promise<void>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor game-complete-flow.spec.ts to use shared helpers</name>
  <files>apps/game/tests/e2e/game-complete-flow.spec.ts</files>
  <action>
Refactor `game-complete-flow.spec.ts` which currently defines 10 local helper functions (lines 9-208) that duplicate shared logic.

**Remove these local functions and replace with shared helper imports:**

- `navigateToGameWithDefaults(page)` -> `startGameWithDefaults(page)` from game-flow
- `setupMultiplayerGame(page, names)` -> `setupMultiplayerGame(page, names)` from game-flow
- `submitAllPlayerAnswers(page, answers)` -> `submitPlayerAnswers(page, answers.length, answers)` from game-flow
- `navigateToResults(page)` -> `navigateToResults(page)` from game-flow
- `assignScores(page, scores)` -> `assignScores(page, scores)` from game-flow
- `confirmScoresAndWaitForModal(page)` -> `confirmScoresAndWaitForModal(page)` from game-flow
- `goToNextRound(page)` -> `goToNextRound(page)` from game-flow
- `finishGame(page)` -> `finishGame(page)` from game-flow
- `startNewGameFromModal(page)` -- Keep locally OR add to shared helpers if useful (unique to this file, tests the "New Game" button path which uses `[data-testid="new-game-button"]`)
- `hideDevtools(page)` -> `hideDevtools(page)` from game-flow

**Import statement:**

```typescript
import {
  hideDevtools,
  submitPlayerAnswers,
  navigateToResults,
  assignScores,
  confirmScoresAndWaitForModal,
  goToNextRound,
  finishGame,
  setupMultiplayerGame,
  startGameWithDefaults,
} from './helpers/game-flow'
```

**Test-specific adjustments:**

- `submitAllPlayerAnswers(page, ['', ''])` becomes `submitPlayerAnswers(page, 2)` (empty answers = no fill, just clicks)
- `submitAllPlayerAnswers(page, ['', '', ''])` becomes `submitPlayerAnswers(page, 3)`
- The `answerInput.fill('')` + `submitBtn.click()` patterns in "should show NEXT button only when all players submitted" test need to use the feature-flag-aware pattern: check if input visible before filling. Either use `submitPlayerAnswers(page, 1)` per step, or inline the flag check.
- Keep all test assertion logic unchanged
  </action>
  <verify>
  <automated>cd /Users/markuswagner/projects/riddle-rush-mono-repo && pnpm run workspace:check</automated>
  </verify>
  <done>game-complete-flow.spec.ts refactored. 9 local helper functions removed and replaced with shared imports. Zero locally duplicated helpers remain. All tests use data-testid selectors. workspace:check passes.</done>
  </task>

<task type="auto">
  <name>Task 2: Refactor full-game-workflow.spec.ts and results.spec.ts</name>
  <files>apps/game/tests/e2e/full-game-workflow.spec.ts, apps/game/tests/e2e/results.spec.ts</files>
  <action>
**Refactor `full-game-workflow.spec.ts`:**

This file has 14 local helper functions (lines 28-294). Remove all that have shared equivalents:

- `navigateToPlayersPage(page)` -- Replace: inline the menu navigation or add to game-flow if useful. This is the only file with menu-to-players navigation.
- `setupThreePlayerGame(page, names)` -> `setupMultiplayerGame(page, names)` from game-flow (handles any player count, not just 3)
- `submitThreePlayerAnswers(page, answers?)` -> `submitPlayerAnswers(page, 3, answers)` from game-flow
- `navigateToResults(page)` -> `navigateToResults(page)` from game-flow
- `assignScoresToPlayers(page, scores)` -> `assignScores(page, scores)` from game-flow
- `confirmScoresAndWaitForDecision(page)` -> `confirmScoresAndWaitForModal(page)` from game-flow
- `continueToNextRound(page)` -> `goToNextRound(page)` from game-flow
- `finishGameFromModal(page)` -> `finishGame(page)` from game-flow
- `_verifyLeaderboardScores(page, scores)` -- Keep locally (test-specific assertion helper, only used in this file)
- `returnToMenuFromLeaderboard(page)` -- Keep locally (test-specific, only used in this file)
- `hideDevtools(page)` -> `hideDevtools(page)` from game-flow
- `playCompleteRound(page, round, scores, isLast)` -- Keep locally (test-specific orchestration), but update it to use shared `submitPlayerAnswers`, `navigateToResults`, `assignScores`, `confirmScoresAndWaitForModal`, `goToNextRound`, `finishGame`.

**Critical:** The `submitThreePlayerAnswers` function assumes answer input is visible (line 87: `await expect(answerInput).toBeVisible()`). The shared `submitPlayerAnswers` handles this via feature-flag detection. Also, the "should preserve player names" test (line 389) manually fills the answer input -- this needs the same feature-flag check (only fill if input visible).

**Refactor `results.spec.ts`:**

This file uses old CSS class selectors throughout:

- `.start-btn` -> `[data-testid="players-start-button"]`
- `.add-btn` -> `[data-testid="players-increase-button"]` (and remove the dialog-based player adding pattern)
- `.answer-input` -> `[data-testid="game-answer-input"]`
- `.submit-answer-btn` -> `[data-testid="game-submit-button"]`
- `.game-background` -> Keep or replace (this is a component class, not fragile if it's on the component itself)
- `.game-header` -> Keep or replace
- `.scoring-page__list` -> Keep (scoped component class)
- `.scoring-page__player-entry` -> Keep (scoped component class)
- `.scoring-page__score-controls` -> Keep
- `.scoring-page__score-value` -> Keep (used for score assertions)
- `.player-leaderboard` -> Keep (component class)

The `beforeEach` in "Results Page - Multi-Player" (lines 187-226) uses the old dialog-based player adding pattern and old CSS selectors. Refactor to use `setupMultiplayerGame` + `submitPlayerAnswers` + navigate to results. Or at minimum replace fragile selectors.

The first `beforeEach` (lines 5-23) is simpler -- navigates to results via `/players` start, but uses `.start-btn` (replace with data-testid) and the old `await expect(page).toHaveURL(/\/round-start/)` strict assertion (should allow either round-start or game URL since fortune wheel may be fast).

Make answer submission feature-flag-aware in both `beforeEach` blocks.

**Run final verification:**
After all changes: `pnpm run workspace:check`
</action>
<verify>
<automated>cd /Users/markuswagner/projects/riddle-rush-mono-repo && pnpm run workspace:check</automated>
</verify>
<done>full-game-workflow.spec.ts refactored: 11 local helpers removed, replaced with shared imports. playCompleteRound updated to use shared helpers internally. results.spec.ts refactored: old CSS class selectors replaced with data-testid, dialog-based player adding removed, answer input made feature-flag-aware. workspace:check passes.</done>
</task>

<task type="auto">
  <name>Task 3: Final E2E suite audit and verification</name>
  <files>apps/game/tests/e2e/helpers/game-flow.ts</files>
  <action>
Perform a final audit of the entire E2E test suite to ensure consistency:

1. **Grep for remaining local duplicates:** Search all `*.spec.ts` files for `async function hideDevtools`, `async function navigateToResults`, `async function submitAllPlayerAnswers`, `async function setupMultiplayerGame`. Expected: zero matches (all moved to shared helpers). If any remain in untouched specs (credits, debug-console, language, etc.), those are OK if they are truly different functions.

2. **Grep for old CSS selectors:** Search for `.answer-input`, `.submit-answer-btn`, `.start-btn`, `.add-btn` in all `*.spec.ts` files. Expected: zero matches (all replaced with data-testid).

3. **Grep for dialog-based player adding:** Search for `page.once('dialog'` in all `*.spec.ts` files. Expected: zero matches (old pattern removed).

4. **Verify helpers/game-flow.ts exports are consistent:** Ensure all exported functions have JSDoc comments, use consistent parameter types, and handle edge cases (e.g., playerCount = 1).

5. **Run `pnpm run workspace:check`** to confirm everything compiles.

6. **Run `pnpm run test`** to confirm unit tests still pass (should not be affected but verify).

If any issues are found, fix them in this task.
</action>
<verify>
<automated>cd /Users/markuswagner/projects/riddle-rush-mono-repo && pnpm run workspace:check && pnpm run test</automated>
</verify>
<done>Full E2E suite audit complete. Zero locally duplicated game-flow helpers. Zero old CSS class selectors in game-flow specs. Zero dialog-based player adding patterns. All helpers have consistent JSDoc and data-testid usage. workspace:check passes. All unit tests pass.</done>
</task>

</tasks>

<verification>
1. `pnpm run workspace:check` passes
2. `pnpm run test` -- all unit tests pass
3. Zero `async function hideDevtools` in spec files (only in helpers/game-flow.ts)
4. Zero `async function navigateToResults` in spec files (only in helpers/game-flow.ts)
5. Zero `.answer-input`, `.submit-answer-btn`, `.start-btn`, `.add-btn` CSS selectors in spec files
6. Zero `page.once('dialog'` patterns in spec files
7. All 7 refactored spec files import from `./helpers/game-flow`
</verification>

<success_criteria>

- All 7 game-flow E2E specs use shared helpers (scoring-flow, scoring-ui, scoring-multi-round, leaderboard, game-complete-flow, full-game-workflow, results)
- Zero locally duplicated game-flow helper functions across the test suite
- Zero fragile CSS class selectors for game navigation elements
- All answer input handling is feature-flag-aware
- workspace:check and unit tests pass
  </success_criteria>

<output>
After completion, create `.planning/phases/18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good/18-03-SUMMARY.md`
</output>
