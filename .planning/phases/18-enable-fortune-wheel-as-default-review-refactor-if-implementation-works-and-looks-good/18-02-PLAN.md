---
phase: 18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good
plan: 02
type: execute
wave: 2
depends_on: ['18-01']
files_modified:
  - apps/game/tests/e2e/round-start.spec.ts
  - docs/GAME-STATE-FLOW.md
  - .planning/STATE.md
autonomous: false

must_haves:
  truths:
    - 'With default settings, users see the wheel flow on round-start before entering the game'
    - 'Round-start flow remains stable (no regressions in round counter behavior on refresh/back)'
    - 'Wheel-default behavior is documented as a contract for future refactors'
  artifacts:
    - path: 'apps/game/tests/e2e/round-start.spec.ts'
      provides: 'End-to-end coverage for default wheel behavior and round counter safeguards'
      contains: 'round-wheels-container'
    - path: 'docs/GAME-STATE-FLOW.md'
      provides: 'Documented round-start semantics (wheel default with random fallback branch)'
      contains: 'RoundStart --> Game'
    - path: '.planning/STATE.md'
      provides: 'Recorded phase decision and remaining follow-up concerns'
      contains: 'fortune wheel default'
  key_links:
    - from: 'apps/game/tests/e2e/round-start.spec.ts'
      to: 'apps/game/pages/round-start.vue'
      via: 'data-testid selectors for wheel/results/loading states'
      pattern: 'data-testid="round-'
    - from: 'docs/GAME-STATE-FLOW.md'
      to: 'apps/game/composables/useFeatureFlags.ts'
      via: 'documented mode gate and default semantics'
      pattern: 'wheel spin or random'
---

<objective>
Harden behavior verification for the new wheel-default contract and confirm UX quality before finalizing phase state.

Purpose: Prevent silent regressions in round-start flow and make "works and looks good" measurable and repeatable.
Output: Stable E2E checks, updated state-flow docs, and a final human verification gate.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good/18-RESEARCH.md
@.planning/phases/18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good/18-01-SUMMARY.md
@apps/game/pages/round-start.vue
@apps/game/tests/e2e/round-start.spec.ts
@docs/GAME-STATE-FLOW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor round-start E2E to assert wheel-default path with stable selectors</name>
  <files>apps/game/tests/e2e/round-start.spec.ts</files>
  <action>Replace fragile class/time-based assertions with branch-aware checks using existing `data-testid` selectors in `round-start.vue` (`round-wheels-container`, `round-results-display`, `round-loading`, round indicator). Ensure the default path asserts wheels are visible before transition to `/game`, and preserve regression checks for refresh/back not incrementing rounds. Avoid broad test-suite rewrites; only harden this spec for deterministic default-flow behavior.</action>
  <verify>pnpm --filter @riddle-rush/game test:e2e -- round-start.spec.ts</verify>
  <done>E2E verifies wheel-default round-start behavior and round-counter safety without brittle selectors/timeouts.</done>
</task>

<task type="auto">
  <name>Task 2: Update flow docs and planning state to capture new default contract</name>
  <files>docs/GAME-STATE-FLOW.md, .planning/STATE.md</files>
  <action>Update `GAME-STATE-FLOW.md` to explicitly state that round-start uses fortune-wheel by default and random immediate start only when the feature is disabled via authoritative flags/settings fallback. Update `.planning/STATE.md` decisions with the final precedence/default contract and, if needed, keep any larger refactor work as future todo (do not start single-source-of-truth redesign here).</action>
  <verify>pnpm run workspace:check</verify>
  <done>Docs and state reflect the implemented default semantics and constraints for future phases.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Wheel-default round-start behavior with hardened regression coverage and updated flow contract</what-built>
  <how-to-verify>
1. Run the app (`pnpm --filter @riddle-rush/game dev`) and start a new game from `/players`.
2. Confirm `/round-start` shows both wheels first (not immediate random jump) and then transitions to `/game`.
3. Confirm UI quality on at least one mobile viewport (~390x844) and one desktop viewport (>=1280px): no clipped wheel labels/results, no obvious jank during transitions.
4. During an active round, refresh `/round-start` and confirm round number does not increment unexpectedly.
5. If behavior or visuals are off, provide concrete issue notes (step + observed result) so a focused refactor follow-up plan can be created.
  </how-to-verify>
  <resume-signal>Type "approved" to finalize phase, or provide issues found.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @riddle-rush/game test:e2e -- round-start.spec.ts`
- `pnpm run workspace:check`
- Human verification approved for behavior and visuals
</verification>

<success_criteria>

- Default game start path visibly uses fortune wheels.
- Round-start regression checks remain green and deterministic.
- State-flow/docs explicitly encode precedence and fallback semantics.
  </success_criteria>

<output>
After completion, create `.planning/phases/18-enable-fortune-wheel-as-default-review-refactor-if-implementation-works-and-looks-good/18-02-SUMMARY.md`
</output>
