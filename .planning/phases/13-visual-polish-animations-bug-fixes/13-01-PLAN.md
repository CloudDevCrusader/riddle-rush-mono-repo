---
phase: 13-visual-polish-animations-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/game/assets/css/design-system.scss
  - apps/game/pages/credits.vue
  - apps/game/pages/round-start.vue
  - apps/game/components/SplashScreen.vue
  - apps/game/components/SettingsModal.vue
autonomous: true
requirements:
  - POLISH-04
  - POLISH-01

must_haves:
  truths:
    - 'No scoped @keyframes duplicates exist in any Vue SFC (fadeIn, scaleIn, slideUp only in design-system.scss)'
    - 'credits.vue uses GameBackground, GameHeader, GamePanel, GameButton instead of PNG images'
    - 'round-start.vue uses GameBackground instead of BACKGROUND.png image for its page wrapper'
    - 'A .page-shell utility class exists in design-system.scss providing min-height:100dvh + position:relative + overflow:hidden'
    - 'A global @media (prefers-reduced-motion) rule disables all animations'
  artifacts:
    - path: 'apps/game/assets/css/design-system.scss'
      provides: '.page-shell class, .animate-* utility classes, reduced-motion media query, consolidated @keyframes'
      contains: '.page-shell'
    - path: 'apps/game/pages/credits.vue'
      provides: 'Credits page using design system components'
      contains: 'GameBackground'
    - path: 'apps/game/pages/round-start.vue'
      provides: 'Round start page using GameBackground'
      contains: 'GameBackground'
  key_links:
    - from: 'apps/game/pages/credits.vue'
      to: 'GameBackground, GameHeader, GamePanel, GameButton'
      via: 'component imports (auto-imported by Nuxt)'
      pattern: '<GameBackground'
    - from: 'apps/game/assets/css/design-system.scss'
      to: 'all pages'
      via: 'global CSS loaded by nuxt.config.ts'
      pattern: '.page-shell'
---

<objective>
Extract shared CSS patterns and migrate old-pattern pages to the design system.

Purpose: Eliminate duplicated CSS code (keyframes in 4+ scoped components, layout boilerplate in 9 pages) and migrate the two remaining image-based pages (credits.vue, round-start.vue) to use design system components. This creates the shared foundation that all subsequent CSS gap-fix and animation work builds on.

Output: Updated design-system.scss with shared utilities, refactored credits.vue and round-start.vue using GameBackground/GameHeader/GamePanel/GameButton.
</objective>

<execution_context>
@/Users/markuswagner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markuswagner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-visual-polish-animations-bug-fixes/13-RESEARCH.md
@apps/game/assets/css/design-system.scss
@apps/game/pages/credits.vue
@apps/game/pages/round-start.vue
@apps/game/components/SplashScreen.vue
@apps/game/components/SettingsModal.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared CSS utilities and consolidate keyframes in design-system.scss</name>
  <files>apps/game/assets/css/design-system.scss</files>
  <action>
Add the following to design-system.scss:

1. **`.page-shell` utility class** (after the existing utility classes section):

```scss
.page-shell {
  min-height: 100vh;
  min-height: 100dvh;
  position: relative;
  overflow: hidden;
  width: 100%;
}
```

2. **`.animate-*` utility classes** for common animation patterns (if not already present):

```scss
.animate-fade-in {
  animation: fadeIn 0.5s ease-out forwards;
}
.animate-scale-in {
  animation: scaleIn 0.4s ease-out forwards;
}
.animate-slide-up {
  animation: slideUp 0.4s ease-out forwards;
}
```

3. **Global `prefers-reduced-motion` rule** (at the very end of the file):

```scss
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

4. **Verify `@keyframes fadeIn`, `scaleIn`, `slideUp` exist globally** — if any are missing, add them. They should be the single source of truth. Check existing keyframes in the file and ensure all three are present.

Do NOT remove any existing content. Only ADD the new utilities and the reduced-motion rule.
</action>
<verify>Run `grep -n 'page-shell\|prefers-reduced-motion\|animate-fade-in' apps/game/assets/css/design-system.scss` and confirm all three patterns are present.</verify>
<done>design-system.scss contains .page-shell class, .animate-\* utilities, prefers-reduced-motion rule, and all common @keyframes are defined globally.</done>
</task>

<task type="auto">
  <name>Task 2: Remove scoped keyframe duplicates and migrate credits.vue + round-start.vue to design system</name>
  <files>
    apps/game/pages/credits.vue
    apps/game/pages/round-start.vue
    apps/game/components/SplashScreen.vue
    apps/game/components/SettingsModal.vue
  </files>
  <action>
**Part A: Remove scoped @keyframes duplicates**

In each of these files, find and REMOVE any scoped `@keyframes` definitions that duplicate the global ones (fadeIn, scaleIn, slideUp, slideInUp, fadeInScale, etc.). The global versions in design-system.scss are the source of truth. Keep any keyframes that are truly unique to that component (e.g., a loading bar animation specific to SplashScreen).

Files to check:

- `apps/game/components/SplashScreen.vue` — remove duplicate fadeIn, scaleIn, slideUp if present
- `apps/game/components/SettingsModal.vue` — remove duplicate fadeIn, scaleIn if present
- `apps/game/pages/credits.vue` — remove duplicate keyframes if present
- `apps/game/pages/round-start.vue` — remove duplicate keyframes if present

**Part B: Migrate credits.vue to design system**

Refactor credits.vue to stop using PNG images for UI and instead use design system components:

- Replace `BACKGROUND.png` background image with `<GameBackground>` component wrapper
- Replace `CREDITS.png` title image with `<GameHeader>` component with text "CREDITS"
- Replace `ok.png` button image with `<GameButton variant="primary">OK</GameButton>` (or appropriate variant)
- Replace `.page-bg` CSS with `.page-shell` utility class on the root element (or rely on GameBackground which already provides full-height)
- Keep the actual credits content (names, roles, etc.) — just restyle the chrome around it
- Use `<GamePanel>` for the content area if it fits the design pattern of other settings/language pages
- Follow the same component composition pattern as `settings.vue` or `language.vue` (GameBackground > GameHeader + GamePanel + GameButton)

**Part C: Migrate round-start.vue page wrapper**

- Replace `BACKGROUND.png` image background with `<GameBackground>` component
- Replace `.page-bg` pattern with the GameBackground component wrapper
- Keep the FortuneWheel component and its custom styling intact — only change the page wrapper/background
- Apply `.page-shell` if GameBackground doesn't already provide full-height layout
- Do NOT alter FortuneWheel internals or its animation logic

For both pages, ensure all `<style scoped>` sections are cleaned up — remove CSS rules that referenced the old PNG images (.page-bg, background-image, etc.).
</action>
<verify>

1. Run `grep -rn '@keyframes fadeIn\|@keyframes scaleIn\|@keyframes slideUp' apps/game/components/SplashScreen.vue apps/game/components/SettingsModal.vue apps/game/pages/credits.vue apps/game/pages/round-start.vue` — should return NO matches (duplicates removed).
2. Run `grep -n 'GameBackground\|GameHeader\|GameButton' apps/game/pages/credits.vue` — should show design system component usage.
3. Run `grep -n 'GameBackground' apps/game/pages/round-start.vue` — should show GameBackground usage.
4. Run `grep -n 'BACKGROUND.png\|CREDITS.png\|ok.png' apps/game/pages/credits.vue apps/game/pages/round-start.vue` — should return NO matches (old images removed).
5. Run `pnpm run workspace:check` from project root — must pass.
   </verify>
   <done>All scoped keyframe duplicates removed. credits.vue uses GameBackground + GameHeader + GamePanel + GameButton. round-start.vue uses GameBackground for page wrapper. No references to old PNG image assets for UI chrome remain in these files. workspace:check passes.</done>
   </task>

</tasks>

<verification>
1. `pnpm run workspace:check` passes (TypeScript + ESLint + Syncpack)
2. No duplicate @keyframes definitions exist outside design-system.scss for fadeIn/scaleIn/slideUp
3. credits.vue and round-start.vue use design system components
4. .page-shell utility class exists and is usable
5. prefers-reduced-motion global rule exists
</verification>

<success_criteria>

- Zero scoped @keyframes duplicates for standard animations
- credits.vue fully migrated from PNG images to CSS design system components
- round-start.vue page wrapper migrated to GameBackground
- .page-shell class available globally
- Reduced motion support added globally
- All quality checks pass
  </success_criteria>

<output>
After completion, create `.planning/phases/13-visual-polish-animations-bug-fixes/13-01-SUMMARY.md`
</output>
