---
phase: 12-app-optimization-refactoring
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - apps/game/tests/unit/composables/useAnswerCheck.spec.ts
  - apps/game/tests/unit/composables/useGameActions.spec.ts
  - apps/game/tests/unit/composables/useGameState.spec.ts
autonomous: true

must_haves:
  truths:
    - 'useAnswerCheck has unit tests covering cache, API, offline fallback, fuzzy matching'
    - 'useGameActions has unit tests covering action orchestration and error handling'
    - 'useGameState has unit tests covering computed properties and reactive updates'
    - 'All tests pass with >80% coverage'
  artifacts:
    - path: 'apps/game/tests/unit/composables/useAnswerCheck.spec.ts'
      provides: 'Tests for answer validation logic'
      min_lines: 150
    - path: 'apps/game/tests/unit/composables/useGameActions.spec.ts'
      provides: 'Tests for game action orchestration'
      min_lines: 100
    - path: 'apps/game/tests/unit/composables/useGameState.spec.ts'
      provides: 'Tests for game state derivation'
      min_lines: 80
  key_links:
    - from: 'apps/game/tests/unit/composables/useAnswerCheck.spec.ts'
      to: 'apps/game/composables/useAnswerCheck.ts'
      via: 'test coverage'
      pattern: 'describe.*useAnswerCheck'
    - from: 'apps/game/tests/unit/composables/useGameActions.spec.ts'
      to: 'apps/game/composables/useGameActions.ts'
      via: 'test coverage'
      pattern: 'describe.*useGameActions'
---

<objective>
Add comprehensive unit tests for 3 critical composables (useAnswerCheck, useGameActions, useGameState).

Purpose: Test high-risk game logic to prevent regressions during refactoring. Focus on composables with complex behavior.
Output: 3 test files with >80% coverage for critical paths.
</objective>

<execution_context>
@/Users/markuswagner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markuswagner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-app-optimization-refactoring/12-CONTEXT.md
@.planning/phases/12-app-optimization-refactoring/12-RESEARCH.md
@apps/game/composables/useAnswerCheck.ts
@apps/game/composables/useGameActions.ts
@apps/game/composables/useGameState.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit tests for useAnswerCheck composable</name>
  <files>apps/game/tests/unit/composables/useAnswerCheck.spec.ts</files>
  <action>
Create comprehensive test suite for useAnswerCheck.ts covering:

**1. Cache behavior:**

- Cache hit returns cached data
- Cache miss triggers API call
- Cache expires after 5 minutes
- Cache key based on category ID

**2. API response parsing:**

- Valid PetScan response parsed correctly
- Response with missing fields handled gracefully
- Empty category returns empty array
- Malformed JSON throws error

**3. Offline fallback:**

- API failure triggers offline mode
- Offline data loaded from public/data/offlineAnswers.json
- Offline mode caches in-memory

**4. Fuzzy matching:**

- Exact match returns true
- Case-insensitive match works
- Umlaut normalization (ä→a, ö→o, ü→u)
- Space/punctuation normalization
- Partial matches rejected

**5. Error handling:**

- Network timeout returns offline data
- Invalid category ID returns empty
- API rate limit handled gracefully

Use Vitest with vi.mock() for fetch, setup/teardown for cache cleanup.
Follow existing test patterns from apps/game/tests/unit/stores/game.spec.ts.
</action>
<verify>cd apps/game && pnpm run test:unit useAnswerCheck.spec.ts</verify>
<done>useAnswerCheck has >80% line coverage with passing tests for cache, API, offline, fuzzy matching, and errors</done>
</task>

<task type="auto">
  <name>Task 2: Create unit tests for useGameActions composable</name>
  <files>apps/game/tests/unit/composables/useGameActions.spec.ts</files>
  <action>
Create comprehensive test suite for useGameActions.ts covering:

**1. Action orchestration:**

- submitAnswer calls store, validates, scores, persists, navigates
- startRound initializes round, resets state
- endGame finalizes scores, saves history, navigates to results
- Action sequencing maintains correct order

**2. Loading state management:**

- Loading state true during action
- Loading state false after completion
- Loading state false on error
- Multiple concurrent actions handled

**3. Error propagation:**

- Store errors bubble to UI
- Network errors show toast notification
- Validation errors return early
- Critical errors logged

**4. Navigation side effects:**

- Submit navigates to next player turn
- End round navigates to results
- End game navigates to leaderboard
- Navigation respects loading state

Use Pinia test helpers (setActivePinia, createPinia).
Mock useRouter() with vi.mock('vue-router').
Mock toast notifications.
</action>
<verify>cd apps/game && pnpm run test:unit useGameActions.spec.ts</verify>
<done>useGameActions has >80% line coverage with passing tests for orchestration, loading states, error handling, and navigation</done>
</task>

<task type="auto">
  <name>Task 3: Create unit tests for useGameState composable</name>
  <files>apps/game/tests/unit/composables/useGameState.spec.ts</files>
  <action>
Create comprehensive test suite for useGameState.ts covering:

**1. Computed properties:**

- currentRound returns correct round info
- activePlayer returns current turn player
- gamePhase returns correct phase (setup, playing, results, ended)
- scoreLeaders returns sorted top 3 players

**2. Reactive updates:**

- Computed updates when store state changes
- Multiple subscribers receive updates
- Stale computed values prevented

**3. Edge cases:**

- No players returns null/empty
- No categories returns empty array
- Empty session returns default state
- Single player game handled
- Tie scores handled in leaders

**4. Game phase detection:**

- Setup phase before first round
- Playing phase during active round
- Results phase after round end
- Ended phase after all rounds

Use Pinia store with test fixtures for game sessions.
Test reactivity with nextTick() and watch().
</action>
<verify>cd apps/game && pnpm run test:unit useGameState.spec.ts</verify>
<done>useGameState has >80% line coverage with passing tests for computed properties, reactivity, edge cases, and phase detection</done>
</task>

</tasks>

<verification>
1. All test files exist: `ls apps/game/tests/unit/composables/use*.spec.ts`
2. All tests pass: `cd apps/game && pnpm run test:unit composables/`
3. Coverage >80%: `cd apps/game && pnpm run test:unit:coverage composables/`
4. No test failures in CI: Verify test output shows 0 failures
</verification>

<success_criteria>

- [ ] useAnswerCheck.spec.ts exists with >150 lines
- [ ] useGameActions.spec.ts exists with >100 lines
- [ ] useGameState.spec.ts exists with >80 lines
- [ ] All tests pass with 0 failures
- [ ] Line coverage >80% for all 3 composables
- [ ] Edge cases covered (empty data, errors, null values)
- [ ] Tests follow Vitest best practices
      </success_criteria>

<output>
After completion, create `.planning/phases/12-app-optimization-refactoring/12-02-SUMMARY.md`
</output>
