---
phase: 12-app-optimization-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/game/pages/index.vue
  - apps/game/pages/players.vue
  - apps/game/pages/round-start.vue
  - apps/game/pages/game/[[gameId]].vue
  - apps/game/pages/results/[[gameId]].vue
  - apps/game/pages/leaderboard.vue
  - apps/game/tests/e2e/game-complete-flow.spec.ts
  - apps/game/tests/e2e/players.spec.ts
  - apps/game/tests/e2e/leaderboard.spec.ts
autonomous: true

must_haves:
  truths:
    - 'E2E tests use data-testid selectors for all interactive elements'
    - 'E2E tests pass with 100% success rate'
    - 'Tests are resilient to i18n text changes'
    - 'Tests are resilient to component refactoring'
  artifacts:
    - path: 'apps/game/pages/index.vue'
      provides: 'Main menu with testable buttons'
      contains: 'data-testid'
    - path: 'apps/game/pages/players.vue'
      provides: 'Player setup with testable inputs'
      contains: 'data-testid'
    - path: 'apps/game/pages/round-start.vue'
      provides: 'Round start with testable wheel'
      contains: 'data-testid'
    - path: 'apps/game/pages/game/[[gameId]].vue'
      provides: 'Game page with testable controls'
      contains: 'data-testid'
    - path: 'apps/game/pages/results/[[gameId]].vue'
      provides: 'Results page with testable actions'
      contains: 'data-testid'
    - path: 'apps/game/pages/leaderboard.vue'
      provides: 'Leaderboard with testable elements'
      contains: 'data-testid'
  key_links:
    - from: 'apps/game/tests/e2e/game-complete-flow.spec.ts'
      to: 'apps/game/pages/*.vue'
      via: 'data-testid selectors'
      pattern: "\\[data-testid=.*\\]"
    - from: 'apps/game/tests/e2e/players.spec.ts'
      to: 'apps/game/pages/players.vue'
      via: 'data-testid selectors'
      pattern: "\\[data-testid=.*\\]"
---

<objective>
Fix E2E tests by adding data-testid attributes to all interactive elements and updating test selectors for resilience.

Purpose: Create a regression safety net before any refactoring begins. E2E tests validate that current system works correctly.
Output: 100% passing E2E test suite using resilient data-testid selectors.
</objective>

<execution_context>
@/Users/markuswagner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markuswagner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-app-optimization-refactoring/12-CONTEXT.md
@.planning/phases/12-app-optimization-refactoring/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data-testid attributes to main menu page</name>
  <files>apps/game/pages/index.vue</files>
  <action>
Add data-testid attributes to all interactive elements in main menu:
- Start Game button: `data-testid="menu-start-button"`
- Settings button: `data-testid="menu-settings-button"`
- Language button: `data-testid="menu-language-button"`
- Credits button: `data-testid="menu-credits-button"`

Follow naming convention: `{page}-{element}-{type}` (e.g., `menu-start-button`)
</action>
<verify>grep -c 'data-testid=' apps/game/pages/index.vue (should show 4+)</verify>
<done>Main menu page has data-testid on all interactive buttons</done>
</task>

<task type="auto">
  <name>Task 2: Add data-testid attributes to players page</name>
  <files>apps/game/pages/players.vue</files>
  <action>
Add data-testid attributes to player management elements:
- Add Player button: `data-testid="players-add-button"`
- Player name input: `data-testid="players-name-input"`
- Remove Player button: `data-testid="players-remove-button-{index}"` (use dynamic index)
- Next/Continue button: `data-testid="players-next-button"`

Use v-bind for dynamic testids: `:data-testid="\`players-remove-button-${index}\`"`
</action>
<verify>grep -c 'data-testid=' apps/game/pages/players.vue (should show 4+)</verify>
<done>Players page has data-testid on all interactive elements with dynamic indexing for player list</done>
</task>

<task type="auto">
  <name>Task 3: Add data-testid attributes to round start page</name>
  <files>apps/game/pages/round-start.vue</files>
  <action>
Add data-testid attributes to round start elements:
- Fortune wheel container: `data-testid="round-wheel-container"`
- Spin button: `data-testid="round-spin-button"`
- Selected category display: `data-testid="round-category-display"`
- Selected letter display: `data-testid="round-letter-display"`
- Start Round button: `data-testid="round-start-button"`
  </action>
  <verify>grep -c 'data-testid=' apps/game/pages/round-start.vue (should show 5+)</verify>
  <done>Round start page has data-testid on fortune wheel and start controls</done>
</task>

<task type="auto">
  <name>Task 4: Add data-testid attributes to game page</name>
  <files>apps/game/pages/game/[[gameId]].vue</files>
  <action>
Add data-testid attributes to game controls:
- Player turn indicator: `data-testid="game-player-turn"`
- Answer input field: `data-testid="game-answer-input"`
- Submit Answer button: `data-testid="game-submit-button"`
- Skip Turn button: `data-testid="game-skip-button"`
- Pause button: `data-testid="game-pause-button"`
- Quit button: `data-testid="game-quit-button"`
- Category info: `data-testid="game-category-info"`
- Letter info: `data-testid="game-letter-info"`
  </action>
  <verify>grep -c 'data-testid=' apps/game/pages/game/[[gameId]].vue (should show 8+)</verify>
  <done>Game page has data-testid on all controls and info displays</done>
</task>

<task type="auto">
  <name>Task 5: Add data-testid attributes to results page</name>
  <files>apps/game/pages/results/[[gameId]].vue</files>
  <action>
Add data-testid attributes to results elements:
- Score display container: `data-testid="results-scores-container"`
- Individual player score: `data-testid="results-player-score-{index}"` (dynamic index)
- Next Round button: `data-testid="results-next-button"`
- End Game button: `data-testid="results-end-button"`
- Round summary: `data-testid="results-round-summary"`

Use v-bind for dynamic player scores: `:data-testid="\`results-player-score-${index}\`"`
</action>
<verify>grep -c 'data-testid=' apps/game/pages/results/[[gameId]].vue (should show 5+)</verify>
<done>Results page has data-testid on score displays and navigation buttons</done>
</task>

<task type="auto">
  <name>Task 6: Add data-testid attributes to leaderboard page</name>
  <files>apps/game/pages/leaderboard.vue</files>
  <action>
Add data-testid attributes to leaderboard elements:
- Leaderboard container: `data-testid="leaderboard-container"`
- Leaderboard entry: `data-testid="leaderboard-entry-{index}"` (dynamic index)
- Player name in entry: `data-testid="leaderboard-player-name-{index}"`
- Player rank in entry: `data-testid="leaderboard-player-rank-{index}"`
- Player score in entry: `data-testid="leaderboard-player-score-{index}"`
- New Game button: `data-testid="leaderboard-new-game-button"`
- Back to Menu button: `data-testid="leaderboard-menu-button"`

Use v-bind for dynamic entries: `:data-testid="\`leaderboard-entry-${index}\`"`
</action>
<verify>grep -c 'data-testid=' apps/game/pages/leaderboard.vue (should show 7+)</verify>
<done>Leaderboard page has data-testid on all entries and navigation buttons</done>
</task>

<task type="auto">
  <name>Task 7: Update E2E tests to use data-testid selectors</name>
  <files>
apps/game/tests/e2e/game-complete-flow.spec.ts
apps/game/tests/e2e/players.spec.ts
apps/game/tests/e2e/leaderboard.spec.ts
  </files>
  <action>
Update all E2E tests to use data-testid selectors instead of text-based or role-based selectors:

**game-complete-flow.spec.ts:**
Replace selectors like:

- `page.getByRole('button', { name: 'Start Game' })` → `page.locator('[data-testid="menu-start-button"]')`
- `page.getByRole('button', { name: 'Next' })` → `page.locator('[data-testid="players-next-button"]')`
- Text-based selectors → data-testid selectors following naming convention

**players.spec.ts:**
Replace selectors for player management:

- Add player → `page.locator('[data-testid="players-add-button"]')`
- Name input → `page.locator('[data-testid="players-name-input"]')`
- Remove player → `page.locator('[data-testid="players-remove-button-0"]')`

**leaderboard.spec.ts:**
Replace selectors for leaderboard:

- Leaderboard entries → `page.locator('[data-testid^="leaderboard-entry-"]')`
- New game → `page.locator('[data-testid="leaderboard-new-game-button"]')`

Update ALL selectors to use data-testid pattern consistently.
</action>
<verify>pnpm run test:e2e (from apps/game/)</verify>
<done>All E2E tests pass with 100% success rate using data-testid selectors</done>
</task>

</tasks>

<verification>
1. All page files contain data-testid attributes: `grep -r 'data-testid=' apps/game/pages/`
2. E2E tests use data-testid selectors: `grep -r 'data-testid=' apps/game/tests/e2e/`
3. E2E test suite passes: `cd apps/game && pnpm run test:e2e`
4. No test uses text-based selectors: `grep -L 'getByRole.*name:' apps/game/tests/e2e/*.spec.ts`
</verification>

<success_criteria>

- [ ] All 6 pages have data-testid attributes on interactive elements
- [ ] E2E tests updated to use data-testid selectors exclusively
- [ ] Test suite passes with 100% success rate
- [ ] Tests are resilient to i18n changes (no text-based selectors)
- [ ] Tests are resilient to component refactoring (no role-based ambiguity)
- [ ] Naming convention followed: `{page}-{element}-{type}`
      </success_criteria>

<output>
After completion, create `.planning/phases/12-app-optimization-refactoring/12-01-SUMMARY.md`
</output>
