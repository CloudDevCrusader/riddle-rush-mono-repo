---
phase: 12-app-optimization-refactoring
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - apps/game/tests/unit/composables/useStatistics.spec.ts
  - apps/game/tests/unit/composables/useAudio.spec.ts
  - apps/game/tests/unit/composables/useIndexedDB.spec.ts
autonomous: true

must_haves:
  truths:
    - 'useStatistics has unit tests covering aggregation logic and IndexedDB reads'
    - 'useAudio has unit tests covering sound loading, volume, mute, and browser compatibility'
    - 'useIndexedDB has unit tests covering CRUD operations, indexes, and error handling'
    - 'All tests pass with >80% coverage'
  artifacts:
    - path: 'apps/game/tests/unit/composables/useStatistics.spec.ts'
      provides: 'Tests for statistics aggregation'
      min_lines: 100
    - path: 'apps/game/tests/unit/composables/useAudio.spec.ts'
      provides: 'Tests for audio management'
      min_lines: 120
    - path: 'apps/game/tests/unit/composables/useIndexedDB.spec.ts'
      provides: 'Tests for data persistence'
      min_lines: 200
  key_links:
    - from: 'apps/game/tests/unit/composables/useStatistics.spec.ts'
      to: 'apps/game/composables/useStatistics.ts'
      via: 'test coverage'
      pattern: 'describe.*useStatistics'
    - from: 'apps/game/tests/unit/composables/useAudio.spec.ts'
      to: 'apps/game/composables/useAudio.ts'
      via: 'test coverage'
      pattern: 'describe.*useAudio'
---

<objective>
Add comprehensive unit tests for remaining 3 critical composables (useStatistics, useAudio, useIndexedDB).

Purpose: Complete test coverage for high-risk areas (data aggregation, audio, persistence) to enable safe refactoring.
Output: 3 test files with >80% coverage for critical paths.
</objective>

<execution_context>
@/Users/markuswagner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markuswagner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-app-optimization-refactoring/12-CONTEXT.md
@.planning/phases/12-app-optimization-refactoring/12-RESEARCH.md
@apps/game/composables/useStatistics.ts
@apps/game/composables/useAudio.ts
@apps/game/composables/useIndexedDB.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit tests for useStatistics composable</name>
  <files>apps/game/tests/unit/composables/useStatistics.spec.ts</files>
  <action>
Create comprehensive test suite for useStatistics.ts covering:

**1. Aggregation logic:**

- Total games count from history
- Total score sum across all games
- Average score calculation
- Win rate percentage (wins / total games)
- Player stats aggregated correctly

**2. IndexedDB reads:**

- Reads from gameHistory store
- Handles empty history gracefully
- Handles corrupt data gracefully
- Performance with large datasets (1000+ games)

**3. Edge cases:**

- Zero games returns default stats (0 games, 0 score, 0% win rate)
- Single game calculates correctly
- Ties handled in win calculation
- Missing fields in game records
- Incomplete game sessions excluded

**4. Data transformation:**

- Raw game sessions → statistics format
- Player stats grouped by player ID
- Leaderboard rankings calculated
- Time-based filtering (last 7 days, last 30 days)

Use fake-indexeddb to mock IndexedDB.
Create test fixtures for game history data.
Test aggregation with various dataset sizes.
</action>
<verify>cd apps/game && pnpm run test:unit useStatistics.spec.ts</verify>
<done>useStatistics has >80% line coverage with passing tests for aggregation, IndexedDB reads, edge cases, and transformations</done>
</task>

<task type="auto">
  <name>Task 2: Create unit tests for useAudio composable</name>
  <files>apps/game/tests/unit/composables/useAudio.spec.ts</files>
  <action>
Create comprehensive test suite for useAudio.ts covering:

**1. Sound loading:**

- Sound files load successfully
- Loading failure handled gracefully
- Multiple sounds loaded in parallel
- Preload vs lazy load

**2. Volume control:**

- Volume set correctly (0-100)
- Volume persists via settings store
- Volume affects all sounds
- Volume changes apply immediately

**3. Mute functionality:**

- Mute silences all sounds
- Unmute restores previous volume
- Mute state persists
- Mute/unmute toggles correctly

**4. Audio sprite management:**

- Correct sprite selected by ID
- Sprite timing accurate
- Multiple sprites in single file
- Sprite overlap handled

**5. Browser compatibility:**

- Web Audio API support detected
- Fallback to HTML5 Audio
- iOS autoplay restrictions handled
- User interaction required for first play

Mock Audio and AudioContext APIs with vi.mock().
Test volume scaling (0-1 range internally, 0-100 externally).
Simulate browser API unavailability.
</action>
<verify>cd apps/game && pnpm run test:unit useAudio.spec.ts</verify>
<done>useAudio has >80% line coverage with passing tests for loading, volume, mute, sprites, and browser compatibility</done>
</task>

<task type="auto">
  <name>Task 3: Create unit tests for useIndexedDB composable</name>
  <files>apps/game/tests/unit/composables/useIndexedDB.spec.ts</files>
  <action>
Create comprehensive test suite for useIndexedDB.ts covering:

**1. CRUD operations for each store:**

- gameSession: save, load, delete
- gameHistory: save, load, query by timestamp
- statistics: save, load, update
- leaderboard: save, load, query by score
- settings: save, load, update

**2. Index queries:**

- Query gameHistory by startTime index
- Query leaderboard by score index (descending)
- Range queries (e.g., last 7 days)
- Composite index queries

**3. Error handling:**

- Quota exceeded error
- Permission denied error
- Database corruption recovery
- Transaction conflicts
- Invalid data types

**4. Concurrent operations:**

- Multiple reads in parallel
- Read during write
- Write conflicts handled
- Transaction isolation

**5. Database initialization:**

- Schema version migration
- Store creation on first use
- Index creation
- Upgrade path from v1 to v2

Use fake-indexeddb for all tests.
Create helper functions for test data insertion.
Test with realistic data volumes (100+ records).
Verify error messages are user-friendly.
</action>
<verify>cd apps/game && pnpm run test:unit useIndexedDB.spec.ts</verify>
<done>useIndexedDB has >80% line coverage with passing tests for CRUD, indexes, errors, concurrency, and initialization</done>
</task>

</tasks>

<verification>
1. All test files exist: `ls apps/game/tests/unit/composables/use*.spec.ts | wc -l` (should be 6 total)
2. All tests pass: `cd apps/game && pnpm run test:unit composables/`
3. Coverage >80%: `cd apps/game && pnpm run test:unit:coverage composables/`
4. No warnings or errors in test output
</verification>

<success_criteria>

- [ ] useStatistics.spec.ts exists with >100 lines
- [ ] useAudio.spec.ts exists with >120 lines
- [ ] useIndexedDB.spec.ts exists with >200 lines
- [ ] All tests pass with 0 failures
- [ ] Line coverage >80% for all 3 composables
- [ ] Edge cases covered (empty data, errors, null values)
- [ ] Browser API mocks work correctly
- [ ] Tests follow Vitest best practices
      </success_criteria>

<output>
After completion, create `.planning/phases/12-app-optimization-refactoring/12-03-SUMMARY.md`
</output>
