---
phase: 22-add-fortune-wheel-for-alphabet-selection-using-vue-fortunewh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/game/package.json
  - apps/game/components/game/FortuneAlphabetWheel.vue
  - apps/game/composables/useFortuneWheelSelection.ts
  - apps/game/types/fortune-wheel.ts
  - apps/game/tests/unit/composables/use-fortune-wheel-selection.spec.ts
  - apps/game/tests/unit/components/fortune-alphabet-wheel.spec.ts
autonomous: true
requirements:
  - PAGE-04
user_setup: []

must_haves:
  truths:
    - Round-start has a reusable, typed fortune wheel module that can emit a valid category+letter selection.
    - Wheel selection logic is deterministic under test and rejects invalid payloads.
  artifacts:
    - path: apps/game/components/game/FortuneAlphabetWheel.vue
      provides: Wheel UI wrapper with explicit test IDs and event contract.
      exports: [FortuneAlphabetWheel]
    - path: apps/game/composables/useFortuneWheelSelection.ts
      provides: Typed selection adapter and validation utilities.
      exports: [useFortuneWheelSelection]
    - path: apps/game/types/fortune-wheel.ts
      provides: Internal type contract for wheel segments, selection payload, and emits.
      exports: [FortuneWheelSegment, FortuneWheelSelection]
  key_links:
    - from: apps/game/components/game/FortuneAlphabetWheel.vue
      to: apps/game/composables/useFortuneWheelSelection.ts
      via: validated selection adapter before emit
      pattern: useFortuneWheelSelection
    - from: apps/game/tests/unit/components/fortune-alphabet-wheel.spec.ts
      to: apps/game/components/game/FortuneAlphabetWheel.vue
      via: event/assertion coverage for spin and selection output
      pattern: fortune-wheel-selected-letter
---

<objective>
Build the Fortune Wheel foundation layer (library wiring + internal contracts + component wrapper) so round-start can safely consume it without coupling business flow to third-party internals.

Purpose: Establish a stable, testable wheel module before route integration.
Output: Typed wheel contracts, reusable component, and unit tests proving selection validity.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-add-fortune-wheel-for-alphabet-selection-using-vue-fortunewh/22-RESEARCH.md
@.planning/phases/22-add-fortune-wheel-for-alphabet-selection-using-vue-fortunewh/22-VALIDATION.md
@.planning/phases/22-add-fortune-wheel-for-alphabet-selection-using-vue-fortunewh/22-UI-SPEC.md
@apps/game/pages/round-start.vue
@apps/game/composables/useFeatureFlags.ts
@apps/game/stores/gameStore.ts
</context>

<interfaces>
From apps/game/stores/gameStore.ts:
```ts
async advanceToConfiguredRound(category: Category, letter: string): Promise<GameSession | null>
```

New internal interface contract to create in `apps/game/types/fortune-wheel.ts`:

```ts
export interface FortuneWheelSegment {
  id: number
  categoryId: number
  categoryKey: string
  categoryName: string
  letter: string
  weight?: number
}

export interface FortuneWheelSelection {
  categoryId: number
  letter: string
}
```

</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add wheel dependency and define internal type contracts</name>
  <files>apps/game/package.json, apps/game/types/fortune-wheel.ts</files>
  <read_first>
    - apps/game/package.json
    - apps/game/nuxt.config.ts
    - .planning/phases/22-add-fortune-wheel-for-alphabet-selection-using-vue-fortunewh/22-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: Type exports compile without `any` and are consumable by component/composable code.
    - Test 2: Dependency is resolvable by workspace scripts after install.
  </behavior>
  <action>Install `vue-fortune-wheel` in `apps/game` dependencies and create `apps/game/types/fortune-wheel.ts` with explicit `FortuneWheelSegment` and `FortuneWheelSelection` interfaces (letter constrained to a single uppercase character at runtime validation stage). Keep all route/store contracts unchanged per existing round-start flow. Do not add fallback aliases or duplicate shapes.</action>
  <acceptance_criteria>
    - `apps/game/package.json` contains `"vue-fortune-wheel"` in dependencies.
    - `apps/game/types/fortune-wheel.ts` exports `FortuneWheelSegment` and `FortuneWheelSelection`.
    - `pnpm --filter @riddle-rush/game typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @riddle-rush/game typecheck</automated>
  </verify>
  <done>Dependency is installed and typed wheel contracts exist with no TS regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement selection adapter composable and reusable FortuneAlphabetWheel component</name>
  <files>apps/game/composables/useFortuneWheelSelection.ts, apps/game/components/game/FortuneAlphabetWheel.vue</files>
  <read_first>
    - apps/game/pages/round-start.vue
    - apps/game/composables/useFeatureFlags.ts
    - apps/game/stores/gameStore.ts
    - apps/game/types/fortune-wheel.ts
    - .planning/phases/22-add-fortune-wheel-for-alphabet-selection-using-vue-fortunewh/22-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: Adapter accepts known category+letter inputs and returns normalized selection payload.
    - Test 2: Adapter rejects invalid letter/category combinations.
    - Test 3: Component emits one validated selection payload only after spin completion.
  </behavior>
  <action>Create `useFortuneWheelSelection.ts` with pure helpers to map categories into wheel segments and validate a selected segment into `{ categoryId, letter }`. Create `FortuneAlphabetWheel.vue` wrapper around `vue-fortune-wheel` that exposes testids (`fortune-wheel-container`, `fortune-wheel-spin-button`, `fortune-wheel-selected-category`, `fortune-wheel-selected-letter`, `fortune-wheel-confirm-button`), uses copy from UI-SPEC (`Spin Letter`), and emits a typed `selection-ready` event only when adapter validation passes. Keep component client-safe and avoid direct navigation/store writes inside the wrapper.</action>
  <acceptance_criteria>
    - `useFortuneWheelSelection.ts` exports `useFortuneWheelSelection` with normalization + validation functions.
    - `FortuneAlphabetWheel.vue` includes all required `data-testid` values.
    - Component emits `selection-ready` with `{ categoryId, letter }` payload.
    - No direct calls to `goToGame` or `advanceToConfiguredRound` appear in component file.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @riddle-rush/game test:unit -- tests/unit/composables/use-fortune-wheel-selection.spec.ts tests/unit/components/fortune-alphabet-wheel.spec.ts</automated>
  </verify>
  <done>Reusable wheel wrapper and adapter are implemented and verifiably emit safe selection payloads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add unit tests for adapter and component emit contract</name>
  <files>apps/game/tests/unit/composables/use-fortune-wheel-selection.spec.ts, apps/game/tests/unit/components/fortune-alphabet-wheel.spec.ts</files>
  <read_first>
    - apps/game/tests/unit/composables/use-answer-check.spec.ts
    - apps/game/tests/unit/game-store.spec.ts
    - apps/game/composables/useFortuneWheelSelection.ts
    - apps/game/components/game/FortuneAlphabetWheel.vue
  </read_first>
  <behavior>
    - Test 1: Invalid selections are rejected and do not emit.
    - Test 2: Valid selection emits exactly once with normalized uppercase letter.
    - Test 3: Required wheel test IDs render consistently.
  </behavior>
  <action>Create focused Vitest specs that exercise adapter transformation/validation and the wrapper component emit lifecycle. Stub external wheel internals at the component boundary; verify only this app’s contract (IDs, emits, payload normalization). Use project naming/test patterns from existing unit specs.</action>
  <acceptance_criteria>
    - Both new spec files exist and execute in CI mode.
    - Adapter tests cover valid + invalid category/letter cases.
    - Component tests assert `selection-ready` payload shape and `data-testid` rendering.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @riddle-rush/game test:unit -- tests/unit/composables/use-fortune-wheel-selection.spec.ts tests/unit/components/fortune-alphabet-wheel.spec.ts</automated>
  </verify>
  <done>Foundational wheel behavior is locked by deterministic unit tests.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                 |
| --------------------------------------- | ----------------------------------------------------------- |
| third-party component -> app state      | Untrusted UI event payload enters selection logic           |
| user interaction -> round configuration | User-triggered spin/confirm influences game category+letter |

## STRIDE Threat Register

| Threat ID | Category              | Component                       | Disposition | Mitigation Plan                                                                                                           |
| --------- | --------------------- | ------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------- |
| T-22-01   | T (Tampering)         | useFortuneWheelSelection.ts     | mitigate    | Validate selected category id against loaded category list and reject unknown ids before emit.                            |
| T-22-02   | D (Denial of Service) | FortuneAlphabetWheel.vue        | mitigate    | Disable repeat spin/confirm actions while processing and re-enable only after state reset.                                |
| T-22-03   | E (Elevation)         | FortuneAlphabetWheel.vue events | mitigate    | Prevent direct navigation/store mutation in wrapper; only emit typed payload and let route-level controller enforce flow. |

</threat_model>

<verification>
Run:
- `pnpm --filter @riddle-rush/game test:unit -- tests/unit/composables/use-fortune-wheel-selection.spec.ts tests/unit/components/fortune-alphabet-wheel.spec.ts`
- `pnpm --filter @riddle-rush/game typecheck`
</verification>

<success_criteria>

- Foundation files for wheel integration exist and compile.
- Wrapper component emits only validated selection payloads.
- Unit tests for adapter + wrapper pass with no flake.
  </success_criteria>

<output>
After completion, create `.planning/phases/22-add-fortune-wheel-for-alphabet-selection-using-vue-fortunewh/22-01-SUMMARY.md`
</output>
