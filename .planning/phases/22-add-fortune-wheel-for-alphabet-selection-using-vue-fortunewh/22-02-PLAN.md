---
phase: 22-add-fortune-wheel-for-alphabet-selection-using-vue-fortunewh
plan: 02
type: execute
wave: 2
depends_on:
  - '01'
files_modified:
  - apps/game/pages/round-start.vue
  - apps/game/composables/useGameActions.ts
  - apps/game/tests/e2e/helpers/game-flow.ts
autonomous: true
requirements:
  - PAGE-04
user_setup: []

must_haves:
  truths:
    - User can spin and confirm a fortune-wheel-based category+letter selection on `/round-start`.
    - Confirmed selection starts/resumes the round through existing game store flow without regressions.
    - Feature-disabled path still bypasses wheel and reaches game route.
  artifacts:
    - path: apps/game/pages/round-start.vue
      provides: Round-start UI integrates new wheel wrapper and preserves fallback contract.
      exports: []
    - path: apps/game/tests/e2e/helpers/game-flow.ts
      provides: Helper supports new wheel interaction test IDs and route progression.
      exports: [completeFortuneWheel]
  key_links:
    - from: apps/game/pages/round-start.vue
      to: apps/game/stores/gameStore.ts
      via: advanceToConfiguredRound(category, letter)
      pattern: advanceToConfiguredRound
    - from: apps/game/tests/e2e/helpers/game-flow.ts
      to: apps/game/pages/round-start.vue
      via: fortune-wheel test IDs for completion flow
      pattern: fortune-wheel-spin-button
---

<objective>
Wire the new wheel foundation into the actual `/round-start` user flow, preserving existing game session/round transition behavior and fallback paths.

Purpose: Deliver the phase’s user-visible behavior where alphabet/category are selected through an interactive fortune wheel.
Output: End-to-end route integration with fallback-safe behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-add-fortune-wheel-for-alphabet-selection-using-vue-fortunewh/22-RESEARCH.md
@.planning/phases/22-add-fortune-wheel-for-alphabet-selection-using-vue-fortunewh/22-UI-SPEC.md
@apps/game/pages/round-start.vue
@apps/game/composables/useGameActions.ts
@apps/game/stores/gameStore.ts
@apps/game/tests/e2e/helpers/game-flow.ts
</context>

<interfaces>
From apps/game/composables/useGameActions.ts:
```ts
const startConfiguredRound = async (category: Category, letter: string) => Promise<GameSession | null>
```

From apps/game/tests/e2e/helpers/game-flow.ts:

```ts
export async function completeFortuneWheel(page: Page): Promise<void>
```

Round-start must keep output contract:

```ts
await gameStore.advanceToConfiguredRound(selectedCategory, selectedLetter)
await goToGame(gameId?)
```

</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace flip-through UI path with FortuneAlphabetWheel integration in round-start page</name>
  <files>apps/game/pages/round-start.vue</files>
  <read_first>
    - apps/game/pages/round-start.vue
    - apps/game/components/game/FortuneAlphabetWheel.vue
    - apps/game/composables/useFeatureFlags.ts
    - .planning/phases/22-add-fortune-wheel-for-alphabet-selection-using-vue-fortunewh/22-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: When feature is enabled, round-start renders wheel container and allows spin+confirm path.
    - Test 2: When feature is disabled, round-start follows fallback random selection path.
    - Test 3: Invalid selection does not trigger game transition.
  </behavior>
  <action>Refactor `round-start.vue` to render `FortuneAlphabetWheel` as the primary enabled-path interaction, replacing flip-through animation logic. Keep the existing loading state and error toast flow. Preserve fallback logic (`!isFortuneWheelEnabled`) to select random category+letter and call existing start path. Ensure selected payload from wheel maps back to a real `Category` object before invoking start. Keep data-testid compatibility for route-level observability and add `fortune-wheel-container` path markers.</action>
  <acceptance_criteria>
    - `round-start.vue` no longer depends on flip-track animation state for the enabled path.
    - Fortune wheel path calls existing configured-round start flow only after valid selection.
    - Disabled-flag path still bypasses wheel and reaches start flow.
    - `data-testid="fortune-wheel-container"` appears in round-start output when enabled.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @riddle-rush/game test:e2e -- tests/e2e/round-start.spec.ts --project=chromium</automated>
  </verify>
  <done>Round-start user path is wheel-driven while preserving fallback and navigation contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align startConfiguredRound orchestration and E2E helper behavior with wheel interaction</name>
  <files>apps/game/composables/useGameActions.ts, apps/game/tests/e2e/helpers/game-flow.ts</files>
  <read_first>
    - apps/game/composables/useGameActions.ts
    - apps/game/tests/e2e/helpers/game-flow.ts
    - apps/game/tests/e2e/round-start.spec.ts
    - apps/game/stores/gameStore.ts
  </read_first>
  <behavior>
    - Test 1: Helper can complete wheel flow through explicit wheel control selectors.
    - Test 2: Helper still works when route transitions quickly from `/round-start` to `/game`.
    - Test 3: startConfiguredRound retains null-guard behavior for invalid/no-session starts.
  </behavior>
  <action>Update `completeFortuneWheel` helper to support the new wheel interaction test IDs (`fortune-wheel-spin-button`, `fortune-wheel-confirm-button`) and wait for deterministic completion conditions. Keep existing tolerant URL polling (`/round-start|/game`) and final `/game` assertion. In `useGameActions.ts`, ensure `startConfiguredRound` remains the sole route-level orchestration API with unchanged return semantics and toast behavior, explicitly avoiding duplicate route push logic in the helper layer.</action>
  <acceptance_criteria>
    - `completeFortuneWheel` no longer assumes flip-container-only behavior.
    - Helper uses new wheel selectors and still passes on fast transitions.
    - `startConfiguredRound` API signature/behavior remains compatible with existing callers.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @riddle-rush/game test:e2e -- tests/e2e/round-start.spec.ts tests/e2e/scoring-flow.spec.ts --project=chromium</automated>
  </verify>
  <done>Game-flow helpers and orchestration are consistent with new wheel UX and existing contracts.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                    |
| -------------------------------- | -------------------------------------------------------------- |
| UI selection -> store transition | User-facing wheel payload enters session configuration logic   |
| E2E helper -> page controls      | Automated control flow must not mask broken user-path behavior |

## STRIDE Threat Register

| Threat ID | Category              | Component                         | Disposition | Mitigation Plan                                                                                |
| --------- | --------------------- | --------------------------------- | ----------- | ---------------------------------------------------------------------------------------------- |
| T-22-04   | T (Tampering)         | round-start.vue selection mapping | mitigate    | Resolve selected category by trusted in-memory category list and block transition if no match. |
| T-22-05   | R (Repudiation)       | startConfiguredRound path         | mitigate    | Keep existing logger/error-toast path for failed configured-round start attempts.              |
| T-22-06   | D (Denial of Service) | completeFortuneWheel helper       | mitigate    | Use bounded polling timeouts and explicit fallback route checks instead of infinite waits.     |

</threat_model>

<verification>
Run:
- `pnpm --filter @riddle-rush/game test:e2e -- tests/e2e/round-start.spec.ts --project=chromium`
- `pnpm --filter @riddle-rush/game test:e2e -- tests/e2e/scoring-flow.spec.ts --project=chromium`
- `pnpm --filter @riddle-rush/game typecheck`
</verification>

<success_criteria>

- `/round-start` uses wheel interaction as primary enabled path.
- Existing start/resume flow stays intact via `advanceToConfiguredRound`.
- E2E helpers and key specs pass with new interaction model.
  </success_criteria>

<output>
After completion, create `.planning/phases/22-add-fortune-wheel-for-alphabet-selection-using-vue-fortunewh/22-02-SUMMARY.md`
</output>
