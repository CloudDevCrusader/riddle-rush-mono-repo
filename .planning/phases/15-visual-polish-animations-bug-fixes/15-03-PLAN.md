---
phase: 15-visual-polish-animations-bug-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/game/composables/useGameActions.ts
  - apps/game/composables/usePlayerManager.ts
  - apps/game/tests/unit/composables/useGameActions.spec.ts
  - apps/game/components/PageTransition.vue
autonomous: true
requirements:
  - POLISH-05

must_haves:
  truths:
    - 'With 2 players, both players complete round 1 without the last player being skipped'
    - 'A unit test reproduces the 2-player round 1 sequence and verifies allPlayersSubmitted returns true after both submit'
    - 'PageTransition.vue is either removed (if confirmed unused) or integrated — no orphaned unused component'
    - 'pnpm run workspace:check passes and unit tests pass'
  artifacts:
    - path: 'apps/game/tests/unit/composables/useGameActions.spec.ts'
      provides: 'Regression test for 2-player round 1 sequence'
      contains: 'allPlayersSubmitted'
    - path: 'apps/game/composables/useGameActions.ts'
      provides: 'Fixed multiplayer round flow if bug is reproduced in tests'
  key_links:
    - from: 'apps/game/composables/useGameActions.ts'
      to: 'apps/game/composables/usePlayerManager.ts'
      via: 'submitPlayerAnswer calling allPlayersSubmitted'
      pattern: 'allPlayersSubmitted'
---

<objective>
Fix the multiplayer round flow bug where the last player is skipped in round 1, and remove dead code.

Purpose: The multiplayer round skip bug is the highest-severity known issue (POLISH-05). With 2-3 players, the last player in round 1 is skipped, breaking core gameplay. Fix strategy: write a targeted unit test that reproduces the exact 2-player sequence, then fix the root cause. Additionally clean up PageTransition.vue (created in Phase 5 but never integrated — confirmed unused). The intermittent nuxi typecheck error is LOW priority and intermittent — investigate but do not force a fix.

Output: Fixed multiplayer flow with regression test, PageTransition.vue removed or integrated.
</objective>

<execution_context>
@/Users/markuswagner/projects/riddle-rush-mono-repo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markuswagner/projects/riddle-rush-mono-repo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-visual-polish-animations-bug-fixes/15-CONTEXT.md
@.planning/phases/15-visual-polish-animations-bug-fixes/15-RESEARCH.md
@apps/game/composables/useGameActions.ts
@apps/game/composables/usePlayerManager.ts
@apps/game/stores/game.ts
@apps/game/tests/unit/composables/useGameActions.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write regression test and fix multiplayer round flow bug</name>
  <files>
    apps/game/tests/unit/composables/useGameActions.spec.ts
    apps/game/composables/useGameActions.ts
    apps/game/composables/usePlayerManager.ts
  </files>
  <behavior>
    - Test: with 2 players and 1 round, player 1 submits → allPlayersSubmitted returns false
    - Test: with 2 players and 1 round, both players submit → allPlayersSubmitted returns true
    - Test: after startNextRound(), both players have hasSubmitted = false (state correctly reset)
    - Test: submitPlayerAnswer for non-current player does not advance turn incorrectly
  </behavior>
  <action>
**Step 1 (RED): Write the failing test**

In `apps/game/tests/unit/composables/useGameActions.spec.ts`, add a describe block for "multiplayer round 1 flow":

```ts
describe('multiplayer round 1 flow - 2 players', () => {
  it('allPlayersSubmitted is false after player 1 submits', () => {
    /* ... */
  })
  it('allPlayersSubmitted is true after both players submit', () => {
    /* ... */
  })
  it('hasSubmitted resets for all players after startNextRound', () => {
    /* ... */
  })
})
```

Set up the Pinia store with 2 players using `setActivePinia(createPinia())`. Use `gameStore.setState()` or `gameStore.$patch()` to initialize player state. Call `submitPlayerAnswer()` and check `allPlayersSubmitted` getter.

Run the test: `cd apps/game && pnpm run test:unit -- --filter="multiplayer round 1"` — tests should FAIL (RED).

**Step 2 (GREEN): Fix the bug**

Based on the research analysis, the likely issue is in `usePlayerManager.ts` or the interaction between `submitPlayerAnswer` and `allPlayersSubmitted`. Look for:

- Race condition where `resetPlayerRoundState()` is called before the last player's submission is checked
- `allPlayersSubmitted` checking stale state during async operations
- `currentPlayerIndex` not advancing correctly after submission

Trace through `submitPlayerAnswer()` → `allPlayersSubmitted` → `startNextRound()` → `resetPlayerRoundState()` sequence.

Apply the minimal fix that makes the regression test pass. Prefer fixing the state mutation ordering over architectural changes.

Run tests: should now PASS (GREEN).

**Step 3: Investigate nuxi typecheck error (document findings)**

Run `cd apps/game && npx nuxi typecheck 2>&1 | head -50`. If the error appears:

- Check if a newer `@vite-pwa/nuxt` version is available: `pnpm outdated @vite-pwa/nuxt`
- If a fix is available without breaking changes, apply it
- If the error cannot be reproduced or requires risky dependency changes, document the current behavior in the task output and skip

Do NOT spend more than 15 minutes on the typecheck error investigation.
</action>
<verify>
<automated>cd /Users/markuswagner/projects/riddle-rush-mono-repo/apps/game && pnpm run test:unit -- --reporter=verbose 2>&1 | grep -E "PASS|FAIL|multiplayer"</automated>
</verify>
<done>Regression tests for 2-player round 1 flow pass. allPlayersSubmitted returns correct values. Player state resets after round transitions. All existing tests continue to pass.</done>
</task>

<task type="auto">
  <name>Task 2: Remove orphaned PageTransition.vue component</name>
  <files>apps/game/components/PageTransition.vue</files>
  <action>
**Step 1: Confirm PageTransition.vue is unused**

Run: `grep -rn "PageTransition" apps/game/ --include="*.vue" --include="*.ts" | grep -v "PageTransition.vue"` — if no results, the component is confirmed unused.

**Step 2: Remove the component**

If confirmed unused:

```bash
git rm apps/game/components/PageTransition.vue
```

If it IS referenced somewhere (unexpected), do NOT remove it — instead, note the location in the SUMMARY.

**Step 3: Verify build still works**

Run `pnpm run workspace:check` to confirm removal doesn't break TypeScript or ESLint.
</action>
<verify>
<automated>cd /Users/markuswagner/projects/riddle-rush-mono-repo && grep -rn "PageTransition" apps/game/ --include="_.vue" --include="_.ts" | grep -v "node_modules" | grep -v "PageTransition.vue" && echo "WARN: still referenced" || echo "PASS: no references remain"</automated>
</verify>
<done>PageTransition.vue is either removed (if unused) or its usage is documented. pnpm run workspace:check passes. cd apps/game && pnpm run test:unit passes.</done>
</task>

</tasks>

<verification>
1. `cd apps/game && pnpm run test:unit` passes — including new multiplayer round flow tests
2. `pnpm run workspace:check` passes
3. `grep -rn "PageTransition" apps/game/ --include="*.vue" --include="*.ts"` returns no consumer references
4. New test file contains describe block for "multiplayer round 1 flow"
</verification>

<success_criteria>

- 2-player round 1 flow works correctly (no last-player skip)
- Regression test documents and proves correct behavior
- PageTransition.vue removed (dead code eliminated)
- All existing unit tests continue to pass
- workspace:check passes
  </success_criteria>

<output>
After completion, create `.planning/phases/15-visual-polish-animations-bug-fixes/15-03-SUMMARY.md`
</output>
