---
phase: 15-visual-polish-animations-bug-fixes
plan: 04
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - apps/game/components/FortuneWheel.vue
  - apps/game/pages/index.vue
  - apps/game/pages/splash.vue
  - apps/game/pages/players.vue
  - apps/game/pages/leaderboard.vue
  - apps/game/app.vue
autonomous: true
requirements:
  - POLISH-01
  - POLISH-03

must_haves:
  truths:
    - 'FortuneWheel segments use alternating blue/teal/dark-blue tones from game palette — no rainbow colors'
    - 'FortuneWheel has a gold/orange embossed outer ring using --color-border-gold (#ffd54f)'
    - 'FortuneWheel pointer is a CSS-drawn gold triangle (clip-path or border trick) — no ▼ emoji'
    - 'FortuneWheel center hub is a gold gradient circle (linear-gradient 135deg #ffd700 → #ffa500)'
    - 'FortuneWheel container uses min(90vw, 90dvh, 420px) — on screens ≤ 400px uses min(88vw, 88dvh, 320px)'
    - 'Segment text uses clamp(8px, 2.5vw, 14px) — readable at 300px screen width'
    - 'Segment icons use clamp(16px, 5vw, 32px) — visible at 300px screen width'
    - 'Center circle uses clamp(60px, 18vw, 120px)'
    - 'index.vue shows text title RIDDLE RUSH via GameHeader instead of LOGO.png'
    - 'index.vue CREDITS button uses variant="warning" (orange) not variant="secondary" (yellow)'
    - 'Page transitions use directional slide-left (forward nav) and slide-right (back nav) using translateX with out-in mode'
    - 'Menu buttons on index.vue have staggered v-motion entrance (delays: 0, 80, 160, 240ms)'
  artifacts:
    - path: 'apps/game/components/FortuneWheel.vue'
      provides: 'Redesigned fortune wheel with game design system palette, CSS pointer, gold hub, fluid sizing'
      min_lines: 400
    - path: 'apps/game/pages/index.vue'
      provides: 'Main menu with GameHeader text title and correct button variants'
      contains: 'GameHeader'
    - path: 'apps/game/app.vue'
      provides: 'Page transition configuration defining slide-left and slide-right CSS transitions'
      contains: 'slide-left-enter-active'
  key_links:
    - from: 'apps/game/components/FortuneWheel.vue'
      to: '--color-border-gold, --color-bg-blue-dark'
      via: 'CSS custom properties from design-system.scss'
      pattern: 'var(--color-border-gold)'
    - from: 'apps/game/pages/index.vue'
      to: 'GameHeader'
      via: 'component replacing LOGO.png img element'
      pattern: '<GameHeader'
    - from: 'apps/game/app.vue'
      to: 'all pages'
      via: 'NuxtPage transition wrapper + named slide-left/slide-right CSS rules'
      pattern: 'slide-left-enter-active'
---

<objective>
Redesign the FortuneWheel to match the game design system and fix remaining mockup CSS gaps with smooth directional page animations.

Purpose: The FortuneWheel currently uses rainbow colors and an emoji ▼ pointer that look out of place next to the polished GamePanel/GameButton components. This plan applies the game's blue/teal palette and gold embossed treatment to make the wheel feel like it was built as part of the same design. Simultaneously, index.vue's LOGO.png is replaced with GameHeader text, CREDITS button gets the correct orange variant, and page transitions are made smooth using slide-left/slide-right directional transitions. Staggered entrance animations are added to key list elements. Depends on Plan 01 for round-start.vue's GameBackground wrapper.

Output: FortuneWheel with game-system visual design and 300px-fluid sizing, index.vue CSS gap fixes, directional slide page transitions, staggered mount animations.
</objective>

<execution_context>
@/Users/markuswagner/projects/riddle-rush-mono-repo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markuswagner/projects/riddle-rush-mono-repo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-visual-polish-animations-bug-fixes/15-CONTEXT.md
@.planning/phases/15-visual-polish-animations-bug-fixes/15-RESEARCH.md
@.planning/phases/15-visual-polish-animations-bug-fixes/15-01-SUMMARY.md
@apps/game/components/FortuneWheel.vue
@apps/game/pages/index.vue
@apps/game/pages/splash.vue
@apps/game/pages/players.vue
@apps/game/pages/leaderboard.vue
@apps/game/app.vue
@apps/game/assets/scss/design-system.scss

<interfaces>
<!-- CSS custom properties available from design-system.scss (use these, do not hardcode) -->
:root {
  --color-border-gold: #ffd54f;
  --color-border-gold-dark: #c8a243;
  --color-border-gold-darker: #a67e2f;
  --color-bg-blue-light: #1cc6ff;
  --color-bg-blue-mid: #0b7ad6;
  --color-bg-blue-dark: #0a4cc7;
  --color-btn-blue-light: #44c8ff;
  --color-btn-blue-dark: #0a7bda;
  --color-white: #ffffff;
  --color-text-yellow: #ffd54f;
  --spacing-xs: clamp(4px, 1vw, 8px);
}

<!-- @vueuse/motion v-motion directive usage (already installed) -->

v-motion
:initial="{ opacity: 0, y: 30 }"
:enter="{ opacity: 1, y: 0, transition: { duration: 300, delay: 0 } }"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign FortuneWheel with game design system palette, CSS pointer, fluid 300px sizing, and ≤ 400px override</name>
  <files>apps/game/components/FortuneWheel.vue</files>
  <action>
Restyle the `<style scoped>` section of FortuneWheel.vue to match the game design system. Do NOT change the `<template>` or `<script setup>` logic — only CSS changes.

**1. Replace defaultColors array in script with game palette (3 alternating tones)**

In the `<script setup>` section, replace `defaultColors` with game-system blues/teals:

```ts
const defaultColors = [
  '#0a4cc7', // dark blue (--color-bg-blue-dark)
  '#0b7ad6', // mid blue (--color-bg-blue-mid)
  '#44c8ff', // light blue (--color-btn-blue-light)
  '#0a7bda', // blue (--color-btn-blue-dark)
  '#1cc6ff', // bright blue (--color-bg-blue-light)
  '#0756a0', // deeper blue (btn-blue-shadow)
]
```

This cycles through 6 tones so adjacent segments always contrast. Keep the array cycling via `index % defaultColors.length`.

**2. Wheel container — fluid sizing with ≤ 400px media query override**

Replace `.wheel-container` and ALL existing `@media` breakpoint rules with:

```css
.wheel-container {
  position: relative;
  width: min(90vw, 90dvh, 420px);
  height: min(90vw, 90dvh, 420px);
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0 auto;
}

/* Smaller override for very narrow viewports (per locked decision) */
@media (max-width: 400px) {
  .wheel-container {
    width: min(88vw, 88dvh, 320px);
    height: min(88vw, 88dvh, 320px);
  }
}
```

DELETE the `@media (max-width: 768px)`, `@media (max-width: 640px)`, and `@media (max-width: 480px)` blocks entirely. The only allowed media query breakpoint is the `max-width: 400px` override above. All other sizing is handled via clamp() on individual elements below.

**3. Fortune wheel — gold embossed outer ring**

Update `.fortune-wheel` to use a thick gold border ring with glow instead of the current subtle box-shadow:

```css
.fortune-wheel {
  position: relative;
  width: 100%;
  height: 100%;
  border-radius: 50%;
  transition: transform 1.8s cubic-bezier(0.25, 0.1, 0.25, 1);
  will-change: transform;
  /* Gold embossed ring matching GamePanel border treatment */
  border: clamp(6px, 2vw, 12px) solid var(--color-border-gold);
  box-shadow:
    0 0 0 clamp(3px, 1vw, 6px) var(--color-border-gold-dark),
    0 0 30px rgba(255, 213, 79, 0.5),
    0 8px 30px rgba(0, 0, 0, 0.5),
    inset 0 0 20px rgba(255, 255, 255, 0.08);
}
```

**4. CSS pointer — replace emoji ▼ with CSS gold triangle**

The `<template>` has `.pointer-arrow` rendering `▼`. Replace the CSS for `.pointer-arrow` to visually appear as a CSS-drawn triangle (the emoji character can stay in HTML as a fallback — just make the CSS override its appearance):

```css
.wheel-pointer {
  position: absolute;
  top: calc(-1 * clamp(20px, 5vw, 32px));
  left: 50%;
  transform: translateX(-50%);
  z-index: 10;
}

.pointer-arrow {
  /* CSS triangle using clip-path — overrides emoji appearance */
  font-size: 0; /* hide emoji character */
  display: block;
  width: clamp(20px, 5vw, 32px);
  height: clamp(24px, 6vw, 40px);
  background: linear-gradient(160deg, #ffd700, #ffa500);
  clip-path: polygon(50% 100%, 0% 0%, 100% 0%);
  filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.6)) drop-shadow(0 0 12px rgba(255, 215, 0, 0.6));
  animation: pointer-bounce 1.5s ease-in-out infinite;
}

@keyframes pointer-bounce {
  0%,
  100% {
    transform: translateY(0);
  }
  50% {
    transform: translateY(4px);
  }
}
```

Note: The existing `@keyframes pulse` in FortuneWheel.vue should be replaced with `pointer-bounce` since `pulse` may conflict with global keyframes. Rename it.

**5. Center hub — gold gradient (already partially correct, verify clamp sizing)**

The existing center circle already uses `linear-gradient(135deg, #ffd700, #ffa500)`. Verify these clamp values match the spec:

```css
.center-circle {
  width: clamp(60px, 18vw, 120px);
  height: clamp(60px, 18vw, 120px);
  border-radius: 50%;
  background: linear-gradient(135deg, #ffd700, #ffa500);
  border: clamp(3px, 0.8vw, 5px) solid var(--color-white);
  box-shadow:
    0 0 20px rgba(255, 215, 0, 0.8),
    0 4px 15px rgba(0, 0, 0, 0.4),
    inset 0 0 20px rgba(255, 255, 255, 0.3);
  display: flex;
  align-items: center;
  justify-content: center;
}
```

**6. Segment text and icon — fluid clamp for 300px screens**

```css
.segment-icon {
  font-size: clamp(16px, 5vw, 32px);
  filter: drop-shadow(0 2px 4px rgba(0, 0, 0, 0.3));
}

.segment-text {
  font-family: var(--font-display);
  font-size: clamp(8px, 2.5vw, 14px);
  font-weight: var(--font-weight-bold);
  color: var(--color-white);
  text-shadow: 0 2px 4px rgba(0, 0, 0, 0.6);
  text-align: center;
  max-width: clamp(40px, 12vw, 80px);
  line-height: 1.2;
}
```

**7. Selected icon sizing**

```css
.selected-icon,
.center-icon {
  font-size: clamp(24px, 6vw, 40px);
  animation: bounce 0.5s ease-out;
}
```

After all CSS changes, run `pnpm run workspace:check` to confirm no ESLint errors.
</action>
<verify>
<automated>cd /Users/markuswagner/projects/riddle-rush-mono-repo && grep -n "@media (max-width: 768px)\|@media (max-width: 640px)\|@media (max-width: 480px)" apps/game/components/FortuneWheel.vue && echo "FAIL: obsolete media query breakpoints remain" || echo "PASS: no obsolete px breakpoints" && grep -n "@media (max-width: 400px)" apps/game/components/FortuneWheel.vue && echo "PASS: ≤400px override present" || echo "FAIL: ≤400px override missing" && grep -n "var(--color-border-gold)\|clip-path.\*polygon" apps/game/components/FortuneWheel.vue && echo "PASS: gold ring and CSS pointer present" || echo "FAIL: gold ring or CSS pointer missing"</automated>
</verify>
<done>
FortuneWheel uses game-system blues (no rainbow). Gold embossed border ring using --color-border-gold. CSS triangle pointer replacing emoji (font-size:0 + clip-path). Gold gradient center hub with clamp(60px, 18vw, 120px). Container uses min(90vw, 90dvh, 420px) by default; @media (max-width: 400px) overrides to min(88vw, 88dvh, 320px). Old 480/640/768px breakpoints deleted. All other sizing via clamp(). Segment text: clamp(8px, 2.5vw, 14px). Icons: clamp(16px, 5vw, 32px). pnpm run workspace:check passes.
</done>
</task>

<task type="auto">
  <name>Task 2: Fix index.vue CSS gaps, add directional slide page transitions, and staggered animations</name>
  <files>
    apps/game/pages/index.vue
    apps/game/pages/splash.vue
    apps/game/pages/players.vue
    apps/game/pages/leaderboard.vue
    apps/game/app.vue
  </files>
  <action>
**Part A: Fix index.vue mockup gaps (per D-01 context decisions)**

1. **Replace LOGO.png with GameHeader** (per user decision in 15-CONTEXT.md):
   - Remove `<div class="logo-container"><img :src="..." alt="Logo" class="logo-image" /></div>`
   - Add `<GameHeader color="gold">RIDDLE RUSH</GameHeader>` above the menu buttons section
   - Remove `.logo-container` and `.logo-image` CSS rules from `<style scoped>`

2. **Fix CREDITS button variant** (per user decision in 15-CONTEXT.md):
   - Find the CREDITS GameButton: `variant="secondary"` → change to `variant="warning"` (orange, matching mockup)
   - Verify all 4 buttons: PLAY = primary (green), MENU = secondary (blue/yellow — check mockup), OPTIONS = warning (orange), CREDITS = warning (orange)

**Part B: Add staggered v-motion entrance animations to index.vue menu buttons**

The `@vueuse/motion` module is already installed. Apply manual stagger delays to the 4 GameButton elements (they are NOT in a v-for, so delays are hardcoded):

```vue
<GameButton
  v-motion
  :initial="{ opacity: 0, y: 30 }"
  :enter="{ opacity: 1, y: 0, transition: { duration: 300, delay: 0 } }"
  variant="primary"
  ...
>PLAY</GameButton>

<GameButton
  v-motion
  :initial="{ opacity: 0, y: 30 }"
  :enter="{ opacity: 1, y: 0, transition: { duration: 300, delay: 80 } }"
  variant="secondary"
  ...
>MENU</GameButton>
```

Delays: 0ms, 80ms, 160ms, 240ms for the 4 buttons respectively (per CONTEXT.md decision: 80ms stagger).

**Part C: Add staggered v-motion to players.vue player inputs**

In players.vue, find the player name input rows (likely in a v-for loop). Add v-motion to the row wrapper:

```vue
<div
  v-for="(player, index) in players"
  v-motion
  :initial="{ opacity: 0, x: -20 }"
  :enter="{ opacity: 1, x: 0, transition: { duration: 300, delay: index * 80 } }"
>
```

**Part D: Add staggered v-motion to leaderboard.vue items**

In leaderboard.vue, find where leaderboard rows are rendered. If inside GameScrollList (internal rendering), apply v-motion to the GameScrollList wrapper instead (single fade-in):

```vue
<GameScrollList
  v-motion
  :initial="{ opacity: 0, y: 20 }"
  :enter="{ opacity: 1, y: 0, transition: { duration: 300 } }"
  ...
/>
```

If items are rendered directly with v-for, apply index-based stagger to each item element.

**Part E: Add directional slide page transitions in app.vue (per locked decision)**

The CONTEXT.md locked decision specifies **slide-left/right directional transitions using `definePageMeta`**, NOT a generic `page` transition. Implement as follows:

In `app.vue`, add CSS rules for the two named transitions. Use `translateX` (horizontal slide), NOT `translateY`:

```css
/* Forward navigation: slide left */
.slide-left-enter-active,
.slide-left-leave-active {
  transition: all 250ms ease;
}
.slide-left-enter-from {
  opacity: 0;
  transform: translateX(30px);
}
.slide-left-leave-to {
  opacity: 0;
  transform: translateX(-30px);
}

/* Back navigation: slide right */
.slide-right-enter-active,
.slide-right-leave-active {
  transition: all 250ms ease;
}
.slide-right-enter-from {
  opacity: 0;
  transform: translateX(-30px);
}
.slide-right-leave-to {
  opacity: 0;
  transform: translateX(30px);
}
```

Remove any existing generic `.page-enter-active` / `.page-leave-active` rules from `app.vue` if present — they conflict with the named transitions.

In `app.vue`, remove any global `transition` prop from `<NuxtPage>` so that per-page `definePageMeta` controls the transition name:

```vue
<NuxtPage />
```

Then add `definePageMeta` to forward-navigation pages (slides left — the default forward direction):

- `apps/game/pages/players.vue`: `definePageMeta({ pageTransition: { name: 'slide-left', mode: 'out-in' } })`
- `apps/game/pages/round-start.vue`: `definePageMeta({ pageTransition: { name: 'slide-left', mode: 'out-in' } })`
- `apps/game/pages/game/[[gameId]].vue`: `definePageMeta({ pageTransition: { name: 'slide-left', mode: 'out-in' } })`
- `apps/game/pages/leaderboard.vue`: `definePageMeta({ pageTransition: { name: 'slide-left', mode: 'out-in' } })`

And back-navigation pages (slides right):

- `apps/game/pages/index.vue`: `definePageMeta({ pageTransition: { name: 'slide-right', mode: 'out-in' } })`
- `apps/game/pages/settings.vue`: `definePageMeta({ pageTransition: { name: 'slide-right', mode: 'out-in' } })`

Pages without a clear direction (splash.vue, credits.vue) can omit `definePageMeta` or use `slide-left` as default.

**Part F: splash.vue loading bar styling (minor)**

Check `splash.vue` loading bar. If the track uses white/transparent borders, add orange/gold border styling to match the mockup's orange bordered loading bar. Only make changes if there's a clear gap — do not over-engineer.

After all changes, run `pnpm run workspace:check` and `cd apps/game && pnpm run test:unit`.
</action>
<verify>
<automated>cd /Users/markuswagner/projects/riddle-rush-mono-repo && grep -n "LOGO.png" apps/game/pages/index.vue && echo "FAIL: LOGO.png still referenced" || echo "PASS: LOGO.png removed" && grep -n "GameHeader" apps/game/pages/index.vue && echo "PASS: GameHeader present" || echo "FAIL: GameHeader missing" && grep -n "v-motion" apps/game/pages/index.vue && echo "PASS: v-motion animations present" || echo "FAIL: v-motion missing" && grep -n "slide-left-enter-active" apps/game/app.vue && echo "PASS: slide-left transition present" || echo "FAIL: slide-left transition missing" && grep -n "slide-right-enter-active" apps/game/app.vue && echo "PASS: slide-right transition present" || echo "FAIL: slide-right transition missing"</automated>
</verify>
<done>
index.vue: LOGO.png removed, GameHeader renders "RIDDLE RUSH" text. CREDITS button uses variant="warning" (orange). 4 menu buttons have v-motion stagger (0, 80, 160, 240ms delays). players.vue: player inputs have index-based 80ms stagger. leaderboard.vue: items have entrance animation. app.vue: defines slide-left and slide-right CSS transitions using translateX (250ms, out-in). Forward-nav pages have definePageMeta({ pageTransition: { name: 'slide-left', mode: 'out-in' } }), back-nav pages have slide-right. pnpm run workspace:check passes. pnpm run test:unit passes.
</done>
</task>

</tasks>

<verification>
1. `pnpm run workspace:check` passes
2. `cd apps/game && pnpm run test:unit` passes
3. FortuneWheel: `grep -c "@media (max-width: 768px)\|@media (max-width: 640px)\|@media (max-width: 480px)" apps/game/components/FortuneWheel.vue` returns 0
4. FortuneWheel: `grep "@media (max-width: 400px)" apps/game/components/FortuneWheel.vue` returns match (≤400px override)
5. FortuneWheel: `grep "var(--color-border-gold)" apps/game/components/FortuneWheel.vue` returns match
6. FortuneWheel: `grep "clip-path.*polygon" apps/game/components/FortuneWheel.vue` returns match (CSS pointer)
7. `grep -n "LOGO.png" apps/game/pages/index.vue` returns no matches
8. `grep "v-motion" apps/game/pages/index.vue` returns 4+ matches (one per button)
9. `grep "slide-left-enter-active" apps/game/app.vue` returns match
10. `grep "slide-right-enter-active" apps/game/app.vue` returns match
</verification>

<success_criteria>

- FortuneWheel visually belongs to the game design system (blue/teal palette, gold embossed ring, CSS triangle pointer)
- FortuneWheel works at 300px screen width — container uses min(90vw, 90dvh, 420px) default and min(88vw, 88dvh, 320px) on ≤ 400px screens
- index.vue title is text (GameHeader "RIDDLE RUSH"), not LOGO.png image
- CREDITS button is orange (warning variant) matching mockup
- Page transitions use named slide-left (forward nav) and slide-right (back nav) using translateX, with out-in mode — directional slide as per locked decision
- Menu buttons have 80ms-staggered entrance animations
- Player inputs and leaderboard items have entrance animations
- All quality checks and unit tests pass
  </success_criteria>

<output>
After completion, create `.planning/phases/15-visual-polish-animations-bug-fixes/15-04-SUMMARY.md`
</output>
