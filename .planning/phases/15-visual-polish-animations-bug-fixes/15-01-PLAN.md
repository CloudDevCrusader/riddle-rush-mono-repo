---
phase: 15-visual-polish-animations-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/game/pages/round-start.vue
  - apps/game/components/SplashScreen.vue
  - apps/game/components/SettingsModal.vue
  - apps/game/assets/scss/design-system.scss
  - apps/game/pages/settings.vue
autonomous: true
requirements:
  - POLISH-04
  - POLISH-01

must_haves:
  truths:
    - 'round-start.vue uses GameBackground component instead of BACKGROUND.png NuxtImg'
    - 'No scoped @keyframes fadeIn, scaleIn, or slideUp remain in SplashScreen.vue or SettingsModal.vue'
    - 'round-start.vue has no scoped @keyframes that duplicate design-system.scss globals'
    - '.game-back-btn utility class is defined in design-system.scss'
    - 'Back-button duplicate CSS in game/[[gameId]].vue and settings.vue is replaced with .game-back-btn'
    - 'pnpm run workspace:check passes'
  artifacts:
    - path: 'apps/game/pages/round-start.vue'
      provides: 'Round start page using GameBackground instead of PNG background image'
      contains: '<GameBackground'
    - path: 'apps/game/components/SplashScreen.vue'
      provides: 'SplashScreen without duplicate fadeIn/slideUp keyframes'
    - path: 'apps/game/components/SettingsModal.vue'
      provides: 'SettingsModal without duplicate scaleIn keyframe'
    - path: 'apps/game/assets/scss/design-system.scss'
      provides: 'Global .game-back-btn utility class'
      contains: '.game-back-btn'
  key_links:
    - from: 'apps/game/pages/round-start.vue'
      to: 'GameBackground'
      via: 'component wrapper replacing NuxtImg page-bg pattern'
      pattern: '<GameBackground'
    - from: 'apps/game/pages/game/[[gameId]].vue'
      to: '.game-back-btn'
      via: 'utility class replacing duplicate scoped back-button CSS'
      pattern: 'game-back-btn'
    - from: 'apps/game/pages/settings.vue'
      to: '.game-back-btn'
      via: 'utility class replacing duplicate scoped back-button CSS'
      pattern: 'game-back-btn'
---

<objective>
Remove duplicate @keyframes from scoped component styles, migrate round-start.vue from BACKGROUND.png to GameBackground, and extract back-button styles into a shared .game-back-btn utility class.

Purpose: The design-system.scss already defines .page-shell, @keyframes fadeIn/scaleIn/slideUp, animate utilities, and the prefers-reduced-motion rule globally. Three scoped component files still redefine keyframes that exist globally, and round-start.vue still uses a BACKGROUND.png image instead of the GameBackground component. Additionally, back-button styles are duplicated across multiple pages — extracting them into a utility class reduces maintenance surface. This plan removes duplication and brings round-start.vue into the design system pattern.

Note: credits.vue is already fully migrated to GameBackground + design system components. design-system.scss already has .page-shell and all shared utilities. Only round-start.vue, SplashScreen.vue, and SettingsModal.vue need keyframe work.

Output: round-start.vue using GameBackground, duplicate keyframes removed from 3 files, .game-back-btn utility class in design-system.scss.
</objective>

<execution_context>
@/Users/markuswagner/projects/riddle-rush-mono-repo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/markuswagner/projects/riddle-rush-mono-repo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-visual-polish-animations-bug-fixes/15-CONTEXT.md
@.planning/phases/15-visual-polish-animations-bug-fixes/15-RESEARCH.md
@apps/game/pages/round-start.vue
@apps/game/components/SplashScreen.vue
@apps/game/components/SettingsModal.vue
@apps/game/assets/scss/design-system.scss
@apps/game/pages/game/[[gameId]].vue
@apps/game/pages/settings.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove duplicate scoped @keyframes from SplashScreen.vue, SettingsModal.vue, and round-start.vue</name>
  <files>
    apps/game/components/SplashScreen.vue
    apps/game/components/SettingsModal.vue
    apps/game/pages/round-start.vue
  </files>
  <action>
Remove scoped @keyframes that duplicate globals already in design-system.scss. The global file defines: fadeIn, slideUp, scaleIn, glow (plus animate-fade-in, animate-scale-in, animate-slide-up utility classes).

**SplashScreen.vue:** Remove the scoped `@keyframes fadeIn` (line ~185) and `@keyframes slideUp` (line ~200). The global versions in design-system.scss are the source of truth. Keep any keyframes that are unique to SplashScreen (e.g., loading bar progress animations specific to that component).

**SettingsModal.vue:** Remove the scoped `@keyframes scaleIn` (line ~182). Keep anything unique.

**round-start.vue:** Remove the scoped `@keyframes scaleIn` (line ~538). Keep any keyframes unique to the wheel animation or page-specific effects that are NOT already in design-system.scss.

After removing, verify the visual classes that USED these keyframes still work — they rely on the global keyframe names which are the same, so CSS animations using `animation: fadeIn 0.5s` will automatically find the global definition.

Do NOT touch anything else in these files in this task.
</action>
<verify>
<automated>grep -n "@keyframes fadeIn\|@keyframes scaleIn\|@keyframes slideUp" /Users/markuswagner/projects/riddle-rush-mono-repo/apps/game/components/SplashScreen.vue /Users/markuswagner/projects/riddle-rush-mono-repo/apps/game/components/SettingsModal.vue /Users/markuswagner/projects/riddle-rush-mono-repo/apps/game/pages/round-start.vue && echo "FAIL: duplicates found" || echo "PASS: no duplicates"</automated>
</verify>
<done>No duplicate @keyframes fadeIn, scaleIn, or slideUp exist in the three target files. All component animations still reference the global keyframe names defined in design-system.scss.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate round-start.vue page background from BACKGROUND.png to GameBackground</name>
  <files>apps/game/pages/round-start.vue</files>
  <action>
Replace the `<NuxtImg>` BACKGROUND.png pattern with the `<GameBackground>` component in round-start.vue.

**Current pattern (remove):**

```html
<div class="round-start-page">
  <!-- Background Image -->
  <NuxtImg
    :src="`${baseUrl}assets/alphabets/BACKGROUND.png`"
    alt="Background"
    class="page-bg"
    ...
  />
  <!-- page content... -->
</div>
```

**Target pattern (apply):**

```html
<GameBackground>
  <div class="round-start-page">
    <!-- page content (no NuxtImg background) -->
  </div>
</GameBackground>
```

Steps:

1. Wrap the root `<div class="round-start-page">` with `<GameBackground>`.
2. Remove the `<NuxtImg>` background element entirely.
3. In the `<style scoped>` section, remove the `.page-bg` CSS rule (the one setting position:absolute, top:0, width:100%, height:100%, etc. for the background image). Keep all other styles.
4. The `.round-start-page` may still need `min-height: 100dvh` or can rely on GameBackground for full-height. Check if GameBackground provides this — if so, simplify `.round-start-page` accordingly. If it duplicates `min-height: 100vh; min-height: 100dvh; position: relative; overflow: hidden`, replace with `.page-shell` class from design-system.scss.
5. Do NOT alter the FortuneWheel component, the wheels layout, or any wheel-specific styles.
6. Run `pnpm run workspace:check` to confirm no TypeScript or ESLint errors.

The Fortune Wheel's visual redesign is handled in Plan 04 — this task only changes the page wrapper background.
</action>
<verify>
<automated>cd /Users/markuswagner/projects/riddle-rush-mono-repo && grep -n "BACKGROUND.png\|class=\"page-bg\"" apps/game/pages/round-start.vue && echo "FAIL: old pattern remains" || echo "PASS: old pattern removed" && grep -n "GameBackground" apps/game/pages/round-start.vue && echo "PASS: GameBackground used" || echo "FAIL: GameBackground missing"</automated>
</verify>
<done>round-start.vue wraps content with GameBackground component. BACKGROUND.png NuxtImg element removed. .page-bg CSS rule removed. Wheel layout and animations untouched. pnpm run workspace:check passes.</done>
</task>

<task type="auto">
  <name>Task 3: Extract .game-back-btn utility class and replace duplicate back-button CSS</name>
  <files>
    apps/game/assets/scss/design-system.scss
    apps/game/pages/game/[[gameId]].vue
    apps/game/pages/settings.vue
  </files>
  <action>
The locked decision in 15-CONTEXT.md specifies: "Extract back-button styles into a `.game-back-btn` utility class."

**Step 1 — Audit existing back-button CSS**

Read the scoped `<style>` sections of `apps/game/pages/game/[[gameId]].vue` and `apps/game/pages/settings.vue`. Find the CSS rules that style the back button (typically classes like `.back-btn`, `.back-button`, or similar). Note the common properties shared between both files.

**Step 2 — Add .game-back-btn to design-system.scss**

In `apps/game/assets/scss/design-system.scss`, add a `.game-back-btn` utility class in the utilities section (near `.page-shell` and other utility classes). The class should capture the shared back-button styles. A typical implementation looks like:

```scss
.game-back-btn {
  position: absolute;
  top: var(--spacing-md, 16px);
  left: var(--spacing-md, 16px);
  z-index: 10;
  background: rgba(0, 0, 0, 0.3);
  border: 2px solid var(--color-border-gold);
  border-radius: 8px;
  color: var(--color-white);
  cursor: pointer;
  padding: var(--spacing-xs, 8px) var(--spacing-sm, 12px);
  font-family: var(--font-display);
  font-size: var(--font-size-sm, 14px);
  transition:
    background 200ms ease,
    transform 100ms ease;

  &:hover {
    background: rgba(0, 0, 0, 0.5);
    transform: translateY(-1px);
  }

  &:active {
    transform: translateY(0);
  }
}
```

Adjust the exact property values to match what is actually in the source files — the above is a guide, not a mandate. The goal is to faithfully represent the existing shared styles, not invent new ones.

**Step 3 — Replace duplicate CSS in game/[[gameId]].vue**

In `apps/game/pages/game/[[gameId]].vue`:

1. Find the template element that is the back button. Add `game-back-btn` to its class list.
2. In `<style scoped>`, remove the back-button CSS rules that are now covered by `.game-back-btn`. Keep any page-specific overrides that genuinely differ.

**Step 4 — Replace duplicate CSS in settings.vue**

In `apps/game/pages/settings.vue`:

1. Find the template element that is the back button. Add `game-back-btn` to its class list.
2. In `<style scoped>`, remove the back-button CSS rules that are now covered by `.game-back-btn`. Keep any page-specific overrides that genuinely differ.

**Step 5 — Verify**

Run `pnpm run workspace:check` to confirm no TypeScript or ESLint errors.
</action>
<verify>
<automated>cd /Users/markuswagner/projects/riddle-rush-mono-repo && grep -n "\.game-back-btn" apps/game/assets/scss/design-system.scss && echo "PASS: .game-back-btn defined in design-system" || echo "FAIL: .game-back-btn missing from design-system" && grep -n "game-back-btn" apps/game/pages/game/[[gameId]].vue && echo "PASS: game-back-btn used in game page" || echo "FAIL: game-back-btn missing from game page" && grep -n "game-back-btn" apps/game/pages/settings.vue && echo "PASS: game-back-btn used in settings" || echo "FAIL: game-back-btn missing from settings"</automated>
</verify>
<done>.game-back-btn utility class defined in design-system.scss. Both apps/game/pages/game/[[gameId]].vue and apps/game/pages/settings.vue use the .game-back-btn class on their back button elements. Duplicate scoped back-button CSS removed from both pages. pnpm run workspace:check passes.</done>
</task>

</tasks>

<verification>
1. `pnpm run workspace:check` passes (TypeScript + ESLint + Syncpack)
2. `grep -rn "@keyframes fadeIn\|@keyframes scaleIn\|@keyframes slideUp" apps/game/components/SplashScreen.vue apps/game/components/SettingsModal.vue apps/game/pages/round-start.vue` returns no matches
3. `grep -n "GameBackground" apps/game/pages/round-start.vue` shows GameBackground usage
4. `grep -n "BACKGROUND.png" apps/game/pages/round-start.vue` returns no matches
5. `grep -n "\.game-back-btn" apps/game/assets/scss/design-system.scss` returns match
6. `grep -n "game-back-btn" apps/game/pages/game/\[\[gameId\]\].vue apps/game/pages/settings.vue` returns matches in both files
</verification>

<success_criteria>

- round-start.vue uses GameBackground for page background (no PNG image)
- Zero duplicate scoped @keyframes for fadeIn, scaleIn, slideUp in target files
- Design system globally provides all shared animation keyframes
- .game-back-btn utility class defined in design-system.scss, used in game/[[gameId]].vue and settings.vue
- workspace:check passes with no errors
  </success_criteria>

<output>
After completion, create `.planning/phases/15-visual-polish-animations-bug-fixes/15-01-SUMMARY.md`
</output>
