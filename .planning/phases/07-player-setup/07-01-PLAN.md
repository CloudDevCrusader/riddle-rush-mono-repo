---
phase: 07-player-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/game/pages/players.vue
  - apps/game/i18n/locales/en.json
  - apps/game/i18n/locales/de.json
autonomous: true

must_haves:
  truths:
    - 'Player count stepper adjusts between 1 and 6 and updates visible input fields'
    - 'Name inputs show placeholder text and mockup-aligned styling'
    - 'START GAME button is visible, styled, and triggers navigation to round start'
    - 'Players page contains no coin assets or coin-related UI'
  artifacts:
    - path: 'apps/game/pages/players.vue'
      provides: 'Player setup layout, stepper controls, inputs, and start button'
    - path: 'apps/game/i18n/locales/en.json'
      provides: 'Players page strings and placeholders'
    - path: 'apps/game/i18n/locales/de.json'
      provides: 'Players page strings and placeholders'
  key_links:
    - from: 'apps/game/pages/players.vue'
      to: 'MAX_PLAYERS'
      via: 'player count clamp'
      pattern: 'MAX_PLAYERS'
    - from: 'apps/game/pages/players.vue'
      to: 'gameStore.pendingPlayerNames'
      via: 'startGame'
      pattern: 'pendingPlayerNames'
    - from: 'apps/game/pages/players.vue'
      to: 'GameButton'
      via: 'START GAME button'
      pattern: '<GameButton'
---

<objective>
Rebuild the players setup page to match the mockup using the design system.

Purpose: Deliver the phase 7 UI with correct stepper, inputs, and start action.
Output: Updated players page layout, styling, and localized strings.
</objective>

<execution_context>
@/home/cloudcrusader/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/cloudcrusader/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-player-setup/07-RESEARCH.md

@apps/game/pages/players.vue
@apps/game/components/layout/GameBackground.vue
@apps/game/components/layout/GamePanel.vue
@apps/game/components/game/GameHeader.vue
@apps/game/components/game/GameButton.vue
@apps/game/components/game/GameScrollList.vue
@packages/shared/src/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild players page structure and state</name>
  <files>
apps/game/pages/players.vue
apps/game/i18n/locales/en.json
apps/game/i18n/locales/de.json
  </files>
  <action>
- Replace image-based background/title/list markup with GameBackground + GamePanel layout and a GameHeader title.
- Implement a player count stepper (minus/plus) that clamps between 1 and MAX_PLAYERS; expose the current count in the UI.
- Drive the input list from a reactive array whose length always matches the count; when count increases append empty strings, when it decreases trim the array.
- Render inputs with v-for, bind with v-model, and use localized placeholder text per row (ex: "Player 1", "Player 2") using t().
- Keep navigation logic: on START GAME, build a list of names (fallback to "Player {n}" when blank), validate length and uniqueness, set gameStore.pendingPlayerNames, then goToRoundStart().
- Keep existing back navigation (goBack) and page meta, but remove any coin or image asset references in template/script.
- Add or update i18n keys for title, placeholder text, stepper label, and start button in both en.json and de.json (use clear dot-path keys under players.*).
  </action>
  <verify>
- Confirm players.vue no longer references assets/players or coin imagery.
- Confirm template renders GameBackground/GamePanel/GameHeader and stepper buttons with MAX_PLAYERS clamp.
- Confirm new i18n keys exist in both locale files.
  </verify>
  <done>
- Stepper updates player count from 1 to 6 and input list length follows without stale entries.
- Inputs show localized placeholder text and accept values.
- START GAME builds pendingPlayerNames and navigates to round start.
  </done>
</task>

<task type="auto">
  <name>Task 2: Style players layout to match mockup</name>
  <files>apps/game/pages/players.vue</files>
  <action>
- Replace the existing scoped styles with mockup-aligned layout styles using design tokens and mockup-clamp() values for spacing, sizes, and typography.
- Style the stepper as a compact control: round buttons, embossed/gradient treatment, and a centered count display using display font.
- Style the input list rows (inside GameScrollList or a panel section) with gold border, subtle inner glow, and consistent row spacing.
- Style inputs with rounded corners, gold border, dark text, and focused glow; ensure full-width on mobile.
- Position the START GAME GameButton at the bottom of the panel, full-width or mockup width, with correct spacing from inputs.
- Keep motion minimal: optional single fade/scale on load, and ensure prefers-reduced-motion disables animations.
  </action>
  <verify>
- Check responsive behavior at 360x640 and 1080x1920: layout stays centered, inputs readable, stepper accessible.
- Confirm no image-based UI elements remain in players page styles.
  </verify>
  <done>
- Players page visually matches the mockup structure with CSS-first styling and no coin assets.
- Stepper, inputs, and start button align consistently across small and large screens.
  </done>
</task>

</tasks>

<verification>
- Run `pnpm --filter @riddle-rush/game lint` if styles or template changes introduce lint errors.
- Optional: `pnpm --filter @riddle-rush/game typecheck` if script changes are non-trivial.
</verification>

<success_criteria>

- Players page uses GameBackground/GamePanel/GameHeader and the new stepper-driven input list.
- START GAME uses GameButton styling and preserves game start behavior.
- No image assets or coins are referenced in players.vue.
  </success_criteria>

<output>
After completion, create `.planning/phases/07-player-setup/07-01-SUMMARY.md`
</output>
