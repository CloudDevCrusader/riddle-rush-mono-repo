---
phase: 07-player-setup
plan: 02
type: execute
wave: 2
depends_on: ['07-01']
files_modified: []
autonomous: false

must_haves:
  truths:
    - 'Player count stepper adjusts between 1 and 6 and updates visible input fields'
    - 'Name inputs show placeholder text and mockup-aligned styling'
    - 'START GAME button is visible, styled, and triggers navigation to round start'
  artifacts:
    - path: 'apps/game/pages/players.vue'
      provides: 'Player setup layout, stepper controls, inputs, and start button'
  key_links:
    - from: 'apps/game/pages/players.vue'
      to: 'MAX_PLAYERS'
      via: 'player count clamp'
      pattern: 'MAX_PLAYERS'
    - from: 'apps/game/pages/players.vue'
      to: 'gameStore.pendingPlayerNames'
      via: 'startGame'
      pattern: 'pendingPlayerNames'
---

<objective>
Verify the players setup page visually and functionally against mockup requirements.

Purpose: Ensure UI matches the mockup and core interactions work as expected.
Output: Human-verified confirmation or issues list for follow-up.
</objective>

<execution_context>
@/home/cloudcrusader/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/cloudcrusader/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-player-setup/07-RESEARCH.md

@apps/game/pages/players.vue
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Players page UI rebuild with stepper-driven inputs and START GAME button</what-built>
  <how-to-verify>
    1) Start the dev server: `pnpm --filter @riddle-rush/game dev`.
    2) Open `http://localhost:3000/players`.
    3) Verify the stepper adjusts player count between 1 and 6 and inputs appear/disappear immediately.
    4) Verify inputs show placeholder text and styling matches the mockup (gold border, rounded, readable text).
    5) Verify START GAME button is positioned per mockup and clicking it navigates to round start.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/behavior issues</resume-signal>
</task>

</tasks>

<verification>
- Visual confirmation from the human checkpoint.
</verification>

<success_criteria>

- Human confirms the players page matches the mockup and interactions work.
  </success_criteria>

<output>
After completion, create `.planning/phases/07-player-setup/07-02-SUMMARY.md`
</output>
