# This file is a template for orb development and is not currently used.
# Since we're using GitLab for CI/CD, this CircleCI orb deployment configuration
# has been disabled. The actual CI/CD pipeline is configured in .gitlab/ directory.

version: 2.1

# Empty workflow - this file is kept for reference but not actively used
workflows:
  empty:
    jobs:
      - empty-job

jobs:
  empty-job:
    docker:
      - image: cimg/base:current
    steps:
      - run:
          name: This workflow is disabled
          command: echo "CircleCI orb deployment is disabled. Using GitLab CI/CD instead."
