{
  "name": "langchain",
  "version": "1.0.0",
  "description": "LangChain tools for Riddle Rush, but can be used for other projects as well.",
  "license": "ISC",
  "author": "Markus Wagner",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build": "tsc",
    "lint": "echo \"No source files to lint\"",
    "format": "prettier --write .",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:unit": "jest --testPathPatterns=src/unit --passWithNoTests",
    "test:e2e": "jest --testPathPatterns=src/e2e --passWithNoTests",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@langchain/anthropic": "^1.3.12",
    "@langchain/core": "^1.1.17",
    "@langchain/openai": "^1.2.3",
    "@langchain/tavily": "^1.2.0",
    "@openai/codex": "^0.92.0",
    "deepagents": "^1.6.0",
    "langchain": "^1.2.11"
  }
}
